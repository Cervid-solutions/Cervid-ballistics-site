# Cervid Ballistics — Project Brief

Use this to brief a new chat session. Attach `CervidBallistics.html` alongside it.

---

## What it is

A single-file HTML ballistics calculator for air rifles, rimfire, and centrefire. No server, no dependencies — open in any browser. Works on Safari on macOS and iOS.

## File

`CervidBallistics.html` — the complete, self-contained app (~920 lines).

Installed copies (after running `install.sh`):
- `~/Documents/Cervid Ballistics/CervidBallistics.html` (Spotlight-searchable)
- `~/Applications/Cervid Ballistics.app` (launcher, opens in Safari)

---

## Features

**Ballistics engine**
- RK4 integration in x-space (not time), so range-card rows hit exact yardage/metre marks
- G1 and G7 drag tables
- Spin drift (Miller formula), Coriolis effect, shooting angle (cosine rule)
- Wind deflection with clock-position input (3 = full crosswind from right, 9 = full left)

**Trajectory Truing**
- Fire at a known distance, enter the observed holdover
- Binary-search BC multiplier until the model matches reality
- Holdover input accepts mils, MOA, inches, or cm

**Environmental**
- Manual entry: altitude, temp (°C), humidity, pressure (inHg), wind speed, wind clock
- 🌍 Weather button: browser geolocation → open-meteo API → auto-fills all fields
- Works on Safari/iOS (no Bluetooth required)
- "Pressure from altitude" button auto-fills ISA pressure

**Unit system**
- Imperial / Metric toggle (all labels and values convert)
- Internal storage always in imperial; conversion on display

**Quick Presets**
- Built-in presets for air rifle (.177, .22, .25) and centrefire (.243, 6.5CM, .308); Rimfire mode ships empty (no built-in loads) — add your own via "+ Save"
- ✏ button on any preset (built-in or custom) loads it into the form for editing; saving hides the original and replaces it with the edited version
- Custom presets saved to localStorage
- Drag-and-drop reordering (all presets, built-in and custom, can be mixed)
- "Restore defaults" resets hidden/custom/order for the current mode

**Range card output**
- Columns: range, drop (in / cm), holdover (mils or MOA), velocity (fps / m/s), energy (ft·lbs / J), wind drift, spin drift, time of flight
- Optional "Spot Range" row — a single extra distance inserted anywhere in the table
- Interval auto or manual

---

## Architecture

Single HTML file: all CSS, JS, and markup inline. No build step.

Key JS sections (in order):
1. CSS variables / dark theme
2. Unit-system toggle (`setUnitSystem`, `syncLayout`)
3. Preset data (`PRESETS` object — air + cf arrays)
4. localStorage helpers (`loadCustom`, `saveCustom`, `loadHidden`, `saveHidden`, `loadOrder`, `saveOrder`)
5. Preset rendering (`getOrderedPresets`, `renderPresets`, `cancelPresetEdit`, `saveCurrentAsPreset`, `restoreDefaults`)
6. Preset loading (`loadPreset` — always stored in imperial, converts on load)
7. Ballistics engine (`dragCoeff`, `calculate`, `renderResults`)
8. Trajectory Truing (`applyTruing`, `resetTruing`)
9. Live weather (`fetchWeather` — geolocation + open-meteo)

State variables: `currentMode` ('air'|'cf'), `unitSystem` ('imperial'|'metric'), `twistDir` (1|-1), `editingCustomIdx`, `editingBuiltinIdx`, `lastResults`, `bcMultiplier`, `unitDrop` ('mils'|'moa').

---

## What has been deliberately removed

Kestrel 5700X BLE integration was built and then removed. It required Web Bluetooth (Chrome-only, not Safari/iOS) and wind speed encoding was never successfully decoded from the proprietary binary protocol. The live weather button is the replacement — it works everywhere.

---

## Known gaps / possible next steps

- No iOS home-screen icon / PWA manifest
- Presets are per-mode (air / centrefire); no cross-mode presets
- No export (PDF range card, CSV) — actually already covered by `exportCSV()`; a PDF range card is still missing
- Input `min`/`max`/`step` on `fPress` are static across both unit systems by design (already generous enough); everything else rescales via `BASE_BOUNDS`/`applyBounds()`
- Gyro stability (Sg) still has no velocity correction term. Miller's formula is referenced to 2800 fps; airgun loads run 700–950 fps. The new shape factor (see changelog below) only corrects for boat-tail/diabolo geometry, not the missing velocity term — this is the likely reason some slug loads still read "Over-stabilised" even after applying the boat-tail correction. Fixing this properly means adding a `(V/2800)^(1/3)` term, but that also needs validating against pellets (which already read plausibly without it) before trusting it for airgun velocities — flagged here rather than guessed at.

## Changelog

**2026-09-12 — Probability of Hit target sizes now travel with preset export/import, and can be reordered by drag (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html; guide updated in both locations).** User asked whether the target-size presets (added in the editable/deletable feature above) could be transferred to other devices, and whether they could be reordered by drag — answered "no to both, but both are straightforward" and then built both. Export: `exportPresets()`'s payload gains a `targets` key (`{air,cf,rf}`, each a full `getTargetList()` array) alongside the existing custom-preset and hidden-built-in data; a new `mergeImportedTargets()` mirrors `mergeImportedPresets()`'s never-overwrite-by-name rule exactly (a name already present locally — built-in or one you've since renamed/resized — is left alone; only non-colliding entries are added) and additionally rejects malformed entries (blank/missing name, non-positive or non-numeric diameter) so a hand-edited or corrupted file can't inject garbage. `importPresetsFile()` only attempts the target merge when the file actually has a `targets` key, so an older export (from before this feature) is correctly treated as "nothing to merge" rather than "every mode should have zero targets" — it would otherwise have wiped every target list on import. The Export/Import link text is unchanged but each now has a `title` tooltip noting it also covers target sizes. Reorder: `reorderPhTarget(fromIdx,toIdx)` splices the mode's flat array and saves it — no separate order-tracking object is needed the way load presets require, since targets have no built-in-vs-custom split to keep stable across a reorder. Wired into `renderPhTargetChips()` via the same mouse HTML5 drag-and-drop plus touch press-and-hold pattern already proven on the main preset grid (`renderPresets()`), reusing `.preset-btn`'s `drag-over`/`[draggable]` CSS treatment via the shared `.compare-tile` class. A reorder while a tile's "..." menu is open or an edit is in progress simply closes/cancels both rather than trying to re-derive a shifted index, matching how delete/reset already handle the same situation. Verified: 8 new checks in `test_probability_of_hit.js` (calling `reorderPhTarget()` directly, exercising the move itself, persistence, no-op-on-same-index, an out-of-range index, and menu/edit-state clearing) and 8 new checks in `test_export_amended_builtins.js` (a fresh export carrying both a newly-added custom target and a renamed built-in one, `mergeImportedTargets()`'s never-overwrite and malformed-entry rejection, and — via a minimal synchronous `FileReader` stand-in — a full `importPresetsFile()` round trip confirming an old-format file leaves targets untouched while a current-format one merges them in and the confirmation alert mentions target sizes). Full regression suite (46 of 46 on CervidBallistics.html, 45 of 45 on the other three, same one pre-existing exclusion as always) re-run clean on all four canonical files; byte-identical pairs confirmed unchanged. Updated the guide's Probability of Hit topic (drag-to-reorder, and "Reset to defaults" now also mentioned as discarding a reorder) and its Export/Import topic (target sizes now included); the Math Models docx was not touched, consistent with the other recent UI/data-management features above, since this adds no new mathematical content.

**2026-09-12 — Slightly enlarged four easy-to-miss Input page controls: Sort by calibre, Restore defaults (Clear all in General Release), New Load, and Weather (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User asked to bump these up in size where space allows. "Sort by calibre" and "Restore defaults"/"Clear all" share the app-wide `.restore-link` class (10px, used elsewhere for the Undo link and the Probability of Hit target-reset link), so rather than resizing that shared class and affecting those other, unrelated uses, added a `font-size:12px` inline override to just these two spans in the Quick Presets header. "New Load" and "Weather" are standalone buttons with their own inline styles, each already sitting alone on the right side of their section header row (`.preset-section-hdr` uses `justify-content:space-between`, so there was headroom); bumped both from 11px/padding 3px 10px to 13px/padding 5px 12px. No id/class/onclick attributes touched, so no JS wiring was affected. Verified: full regression suite (46 of 46 test files on CervidBallistics.html; 45 of 45 on the other three, excluding the one pre-existing exclusion documented above) re-run clean on all four canonical files; both byte-identical pairs (CervidBallistics.html/Personal/index.html and CervidBallistics_GeneralRelease.html/index.html) confirmed still identical after the edit.

**2026-09-12 — Made the Probability of Hit page's "Biggest contributor" line larger and white (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User said the line was too easy to miss on the page. It previously rendered at 12px in `var(--muted)` (a dim grey used for de-emphasised captions elsewhere in the app), the same visual weight as incidental hint text nearby. Bumped `#phBiggestContributor`'s inline style to 15px, `color:#fff`. No JS logic changed — `updatePhBudget()` still sets the same text via `.textContent`. Verified: full `test_probability_of_hit.js` suite (89 checks) re-run clean on all four canonical files after the change; the four files' shared markup confirmed still byte-identical in pairs.

**2026-09-11 — Corrected the Math Models document's title-page date to September 2026 (Cervid Ballistics - Mathematical Models and Design Methodology.docx/.pdf).** User asked to ensure the document is dated for September 2026. Its title page's Date-styled paragraph still read "August 2026" despite every substantive edit made to it this month (Shooter Proficiency's unit toggle, the Probability of Hit target-size CRUD, and the Live Weather recalculation fix, all logged above) — a stale date left behind because none of those changes happened to touch that specific paragraph. Corrected the two `<w:t>` runs making up that date field to read "September 2026," left the four inline historical fix-annotation dates elsewhere in the document (`2026-08-14`, three at `2026-08-31`) untouched since those are factual records of when specific past fixes actually landed, not the document's own overall date. Verified the corrected docx's zip integrity and exact entry-set match against the prior version, rendered it to PDF, and confirmed the regenerated PDF's text matches the docx exactly with no other unintended differences.

**2026-09-11 — Live Weather now auto-recalculates and refreshes HUD/Reticle View/Probability of Hit's wind, instead of leaving them showing a stale pre-fetch reading (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User asked whether HUD/Reticle View/Probability of Hit's own wind override could populate from the Environmental section, including a Live Weather lookup. Investigation found the plumbing already existed but had a timing gap: `fetchWeather()` writes straight into `fWind`/`fWindDir` but never called `calculate()`, and all three pages' wind-suggestion functions (`updateHudWindSuggestion()`/`updateReticleWindSuggestion()`/`updatePhWindSuggestion()`) deliberately read from `lastResults.params` — the frozen snapshot from the last Calculate — rather than the live fields, by explicit prior design (so a page's wind override always stays consistent with the drop/trajectory numbers already shown on it, the same source of truth everything else on that page is frozen from). So a shooter who fetched weather and went straight to one of those three pages without pressing Calculate first would keep seeing the old wind. Confirmed via `AskUserQuestion` which of two fixes was wanted — auto-recalculate after Live Weather (chosen) vs. having those three pages instead always mirror the live Environmental fields the way Compare Loads already does (which would have made their wind override potentially disagree with the page's own displayed numbers until a recalculation) — and built the first. `fetchWeather()`'s success handler now calls `calculate()` if `lastResults` already exists (mirroring the exact `if(lastResults)calculate();` guard already used by `resetTruing()`/`resetWindTarget()`/`applyFxChronoVelocity()` — never auto-calculates before a first load has ever been run), then calls a new `refreshOpenAdvancedPagesAfterCalc()` helper that checks each of `hudModal`/`reticleModal`/`phModal`'s `style.display` and, for whichever is currently open, re-runs its wind/velocity suggestion plus a full render — since `calculate()` itself only ever touches the Results page and has no idea those other pages exist. While tracing this, found and fixed a related pre-existing inconsistency: `calculate()`'s reset block already cleared `hudWindCustomized`/`hudVelocityCustomized`/`reticleWindCustomized`/`reticleVelocityCustomized` on every fresh calculation, but `phWindCustomized`/`phVelocityCustomized` were never included, despite Probability of Hit's own code comment explicitly claiming it "mirrors HUD's own pattern exactly." Added the missing two resets so all three pages now behave identically. Wrote a new `test_live_weather_recalc.js` (25 checks) — the first test coverage `fetchWeather()` has ever had — mocking `navigator.geolocation.getCurrentPosition` and `fetch` against canned Open-Meteo-shaped responses, covering: no auto-recalculate before any load has been calculated; auto-recalculate and all-three-latches-reset once one has; the open page's fields refreshing immediately (checked for HUD, then again for Probability of Hit after closing HUD and opening that instead, confirming only the currently-open page gets touched); and that a failed fetch or a denied geolocation permission leaves `lastResults` untouched and shows the appropriate error status without throwing. Full regression suite (44 of 46 test files, excluding the two pre-existing, unrelated `test_chart_energy_smoothing.js`/`test_export_amended_builtins.js` failures already documented below) re-run clean on `CervidBallistics.html`, propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and the full suite re-run clean against all four. Updated the guide's Environmental Conditions topic to describe the new auto-recalculate/refresh behaviour and that it resets any of the three pages' own wind overrides back to auto-following, exactly like pressing Calculate already does; the Math Models docx was not touched since this is a recalculation-timing/UI fix with no new mathematical content.

**2026-09-11 — Made every Probability of Hit target-size tile editable and deletable, including the app's own built-in sizes, with a per-discipline "Reset to defaults" (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User asked to make the target-size boxes editable and deletable. Clarified two open design questions via `AskUserQuestion` before building: whether this should cover only user-added targets or the built-in defaults too (user chose: all boxes, built-in and custom), and whether there should be a way back if a built-in gets changed or removed (user chose: yes, add a "Reset to defaults" option). Replaced the previous two-tier model — a fixed, in-code `TARGET_SIZE_DEFAULTS` list always displayed ahead of a separate per-mode custom-additions array under the `ballistics_targets_v1` localStorage key — with a single, flat, fully-editable list per discipline: `TARGET_SIZE_DEFAULTS` now only ever seeds a mode's list the first time it's used (via new `getTargetList()`/`loadTargetsRaw()`/`saveTargetsRaw()`/`cloneTargetDefaults()` helpers) or reseeds it later through the new `resetPhTargets()` action, rather than being consulted on every render. Each target chip gained a "⋯" overlay button (mirroring the main preset system's own overflow-menu pattern) opening a small Edit/Delete menu; Edit loads that tile's name and diameter into the existing add-target name/diameter row, which relabels itself "✓ Update" with a "cancel" link exactly like editing a saved load preset does further up the same file, via new `startEditPhTarget()`/`cancelPhTargetEdit()`/`saveOrUpdatePhTarget()` functions (the last replacing the old `addPhCustomTarget()`, now handling both the add and update paths depending on `phTargetEditingIdx`). Deleting a tile (new `deletePhTarget()`) asks for confirmation, correctly shifts a later in-progress edit index down by one so it doesn't end up pointing at the wrong entry, and deliberately leaves whatever result is already on screen alone rather than forcing a reset to "nothing selected" as a side effect of tidying up an unrelated list. A new "Reset to defaults" link per discipline (`resetPhTargets()`) discards every edit/addition/deletion for that mode and reseeds it from the original built-in list, after a confirmation dialog. A dedicated one-time migration (`migrateTargetsIfNeeded()`, gated on a new `ballistics_targets_migrated_v2` flag rather than just an emptiness check, since an intentionally-emptied mode list must stay empty) folds any pre-existing custom targets a shooter had already added under the old scheme into the new combined shape ahead of the built-in defaults, so nobody's own prior additions go missing on first load after this update. Target names are now HTML-escaped when rendering chips (`phEscHtml()`), closing a latent risk that existed even before this change since names were always free-text but were never escaped on display. Extended `test_probability_of_hit.js` with 20 new checks (89 total) covering: editing and deleting a built-in entry specifically (not just a custom one), the currently-selected tile's diameter following an in-place edit immediately, edit-index shifting on an earlier deletion, deleting the tile currently being edited clearing that state entirely, reset requiring and respecting a declined `confirm()`, and the old-style-custom-list migration producing the correct combined result exactly once. Full regression suite (44 of 46 test files, excluding the two pre-existing, unrelated `test_chart_energy_smoothing.js`/`test_export_amended_builtins.js` failures already documented below) re-run clean on `CervidBallistics.html`, propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and the full suite re-run clean against all four. Updated the guide's "Probability of Hit" topic to describe the new editable/deletable/reset behaviour; the Math Models docx was not touched since this is a UI/data-management change with no new mathematical content and no existing docx text describing the old fixed-list behaviour to correct.

**2026-09-11 — Gave Shooter Proficiency the same mils/MOA/inches/cm entry options as Rifle Mechanical Precision, and renamed the "Squirrel-size" air-target preset to "3\" Gong" (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User asked for Shooter Proficiency's plain MOA-only field to gain the identical unit-toggle mechanism Rifle Mechanical Precision already has, plus the unrelated target-name rename. Replaced the single `phShooterProficiencyMoa` global with `phShooterUnit`/`phShooterSizeVal`/`phShooterDistanceVal`, and mirrored Rifle Mechanical Precision's HTML block exactly: a four-button mils/MOA/inches/cm `.unit-toggle`, a value field, and a conditional "Measured at" distance row shown only for the two linear units — new IDs `btnPhShooterUnit{Mils,Moa,In,Cm}`/`fPhShooterVal`/`phShooterUnitLbl`/`phShooterDistanceRow`/`fPhShooterDistance`/`phShooterDistanceUnitLbl`, alongside new `setPhShooterUnit()`/`readPhShooterDistanceYd()`/`onPhShooterChange()`/`refreshPhShooterFields()`/`phShooterMoa()` functions that reuse the same `groupSizeToMoa()` conversion Rifle Mechanical Precision's own fields already call. Shooter Proficiency stays a GLOBAL setting exactly as before — still never captured by `buildPresetEntryFromForm()`/`loadPreset()`, still persisted directly via `savePrefs`/`loadPrefs` — the only change is the entry mechanism, not the scoping. Added an explicit migration path in the startup-restore block: an existing user's old bare `shooterMoa` value is carried forward under the new `moa` unit rather than silently discarded. Also fixed a small pre-existing bug found while touching this code: toggling Rifle Mechanical Precision's own unit buttons wasn't triggering a live recompute of the page (`setPhGroupUnit()` now calls `renderProbHit()` at the end, matching the value-field's `onGroupChange()` behaviour). Separately, renamed the `air`-mode `TARGET_SIZE_DEFAULTS` entry `"Squirrel-size"` (3in diameter) to `"3\" Gong"` — the `rf`-mode "Squirrel / Small Game" entry was left untouched since it wasn't the one named. Rewrote and extended the Shooter Proficiency section of `test_probability_of_hit.js` for the new API (unit switching, mils/inches conversions matching `groupSizeToMoa()`, savePrefs persistence of the new keys, immediate recompute on a unit-only change, and the `shooterMoa`-to-new-keys migration logic) and fixed two other now-stale direct references to the removed `phShooterProficiencyMoa` variable elsewhere in the same file; full suite now passes 74 of 74 checks. Ran the complete regression suite (44 of 46 test files, excluding the two pre-existing, unrelated `test_chart_energy_smoothing.js`/`test_export_amended_builtins.js` failures already documented below) clean on `CervidBallistics.html`, propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs reconfirmed, and the full suite re-run clean against all four. Updated the guide's "Probability of Hit" topic to describe the now-identical entry mechanism for both fields, and extended the Math Models docx's Section 18 paragraph the same way — both validated by rendering to PDF and checking the text reads cleanly.

**2026-09-11 — Added an illustrative shot-scatter graphic to the Probability of Hit page (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User posted the same reference-app screenshot again, now showing its circular scatter-plot visualisation, and asked for the equivalent graphic. Clarified two open questions via `AskUserQuestion` before building — build it into the app now vs. preview first (user chose: build it now), and whether the dispersion should be shown as randomized scatter dots matching the screenshot, a deterministic ellipse/ring only, or both (user chose: randomized dots matching the screenshot) — since this was exactly the "Monte Carlo scatter-plot visualisation" flagged during the original feature's design discussion as a defensible phase-two addition rather than a v1 requirement. Added `phRandNormal()` (a standard Box-Muller transform) and `renderPhScatter(sigmaElevIn,sigmaWindIn,targetDiameterIn)`, which draws 24 points sampled from the same elevation/windage sigmas `renderProbHit()` already computes, as an inline SVG: a faint crosshair and dashed field-of-view ring for framing, the target boundary drawn to scale as a solid circle, and the sampled points as small dots (23 green, one highlighted white) — auto-scaled so the field of view covers whichever is larger, the target itself or the furthest sampled outlier, so a dispersion much wider than the target still shows its outliers rather than clipping them off-canvas. Degenerate inputs (zero sigma, zero or negative target size) are handled by either drawing a valid but tight/empty-looking graphic or clearing it entirely, never throwing or drawing bogus geometry. Deliberately kept independent from the headline percentage: the graphic is a single random draw and, at only 24 points, won't always land inside the target ring in exactly the proportion the exact analytic figure (from `phHitProbability()`'s numerical integration) states — rather than compute and show a second, sample-based percentage that could occasionally disagree with the correct one, the graphic is captioned "Illustrative shot scatter" with no percentage readout of its own, consistent with this project's established transparency-about-approximations principle. Placed beside the existing headline percentage in the page's hero row. Extended `test_probability_of_hit.js` (9 new checks, 68 total) covering: SVG structure and dot/highlight counts; safe handling of zero dispersion, zero, and negative target size; that the on-screen target radius correctly shrinks as dispersion grows well past the target size (keeping outliers on-canvas rather than silently clipping them); and that `renderProbHit()` itself calls through to the new renderer as part of its normal render pass. Full existing regression suite (44 of 46 test files, excluding the two pre-existing, unrelated `test_chart_energy_smoothing.js`/`test_export_amended_builtins.js` failures already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and the regression suite re-run clean against all four. Added a paragraph on the scatter graphic to the guide's "Probability of Hit" topic (Section 10, Field Tools) and to the Math Models docx's Section 18, both explaining the illustrative-only, sampling-variance distinction — validated clean and visually confirmed by rendering the docx to PDF.

**2026-09-11 — Added a "Probability of Hit" feature: an honest, single hit-probability figure combining wind-call uncertainty, this load's velocity spread, rifle mechanical precision, and shooter proficiency against a chosen target size and range (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Followed a reference-app screenshot the user posted of a comparable "Probability of Hit" feature, then an extended, roughly ten-turn design discussion working out a full spec before any code was written: wind and velocity point values default from the live Inputs-tab fields with an HUD/Reticle-style per-page override (dial + stepper); two genuinely new figures — wind-call uncertainty and velocity SD (round-to-round muzzle-velocity spread) — were confirmed as un-derivable from anything already in the app; shooter proficiency was confirmed as a single GLOBAL setting (describes the shooter, not any one rifle, adjustable at any time, never preset-scoped), while rifle mechanical precision (group size) was confirmed as saved PER-PRESET like BC/weight, entered in mils/MOA or as a linear group at a stated distance; range-estimation error and atmospheric uncertainty were explicitly dropped from this first version (nothing in the app can honestly source either yet, and published breakdowns show both contributing a small fraction of the total budget); target size was confirmed as user-selectable with different built-in defaults per discipline, using a single circular-equivalent diameter even though that is the mathematically harder case than a rectangular target; and a live NOW/HOLD telemetry mode and a Monte-Carlo scatter visualisation were both scoped out as reasonable future additions rather than required for v1.

New page ("Probability of Hit", gated behind an Advanced badge like Doppler fit/FX chrono import, with its own button in the Results header row beside Range Card/Reticle View/HUD): a range selector; wind override (dial + speed field, HUD's exact override/auto-suggest pattern) plus a new wind-call-uncertainty field; velocity override (stepper, HUD's exact pattern) plus a new velocity-SD field labelled "manual" or "from chronograph log"; a global Shooter Proficiency field (MOA, persisted via `savePrefs`/`loadPrefs` exactly like `unitSystem`/`currentMode`); a per-preset Rifle Mechanical Precision field with a mils/MOA/inches/cm unit toggle mirroring Trajectory Truing's `trueUnit`/`setTrueUnit` pattern (a new `groupSizeToMoa(raw,unit,distanceYd)` helper handles the two linear units, since — unlike Zero Offset's pre-converted click/angle values — these are genuine measurements taken at a real, stated distance: `MOA = inches/(yards×0.010472)`, the same relationship `computeTrajectory()`'s own moa/mils row fields already use, just solved for a standalone measurement); and a lightweight target-size picker (chip-style, `TARGET_SIZE_DEFAULTS` per mode plus a small user-editable custom list persisted via a new `ballistics_targets_v1` localStorage key) — deliberately simpler than the main preset system (no drag-reorder, no hide/restore).

Math: `renderProbHit()` builds an effective params object the same way `renderHud()` does (a fresh `Object.assign` over `lastResults.params` with the page's own overrides, never a cached-row rescale, with the same `safeMaxYd=Math.max(targetYd, zeroYd+20)` guard), computes a baseline row via `computeTrajectory()`, then for each of wind and velocity nudges the input by ±1σ and takes the averaged (central-difference) shift in Drop and total windage (crosswind + spin drift + Coriolis) as that source's 1σ linear dispersion — a genuine recompute each time, never a linear approximation off the baseline table, for the same second-order-effects reasoning as every other override in the app. Rifle group size and shooter proficiency, already angular, convert straight to inches-at-range via a new `moaToInchesAtRange(moa,rangeYd)` helper instead of going through `computeTrajectory()` at all. All four 1σ figures combine by root-sum-square into one elevation sigma and one windage sigma (independent uncertainties add in quadrature, not linearly), generally unequal — describing an elliptical, not circular, dispersion. A new `phHitProbability(sigmaElevIn,sigmaWindIn,targetDiameterIn)` numerically integrates the uncorrelated bivariate-normal density over the target disk on a 220-division Cartesian grid, since an elliptical dispersion against a circular target has no closed form (unlike the simpler isotropic case, which reduces to the Rayleigh CDF `P=1-exp(-R²/2σ²)` — used as the cross-check for the numeric integration, agreeing to within 0.5% across several sigma/radius combinations tested). Results show a headline percentage, a per-source error-budget table (elevation and windage contribution for each of the four sources plus a combined RSS row), and a callout naming the single biggest contributor.

Extended the FX chronograph shot-log import (Trajectory Truing tab) to compute each log's sample velocity SD (`σ=√(Σ(vᵢ-v̄)²/(n-1))`, requiring 2+ shots) alongside its existing averages, with a new "Apply Velocity SD" button (disabled with an explanatory label for a single-shot log) wired to a new `applyFxChronoSd()` handler that writes the figure into Probability of Hit's velocity-SD field and relabels its source from "manual" to "from log" — reverting back to "manual" automatically if the field is hand-edited afterwards, so a stale log reading can never keep being presented as measured. `clearTruingState()` extended to reset velocity SD back to its rough manual placeholder (10fps) on every fresh load, matching the existing pattern for `bcMultiplier`/`lastFxChronoResult`.

New `test_probability_of_hit.js` (59 checks): pure math (`groupSizeToMoa`, `moaToInchesAtRange`, `phHitProbability` cross-checked against the closed-form Rayleigh CDF for the isotropic case, monotonicity in target size and dispersion, edge cases); target-size CRUD (built-in defaults, custom add with metric conversion, per-mode isolation, blank/invalid-input guards); rifle-group-size per-preset save/load round-trip via `buildPresetEntryFromForm`/`loadPreset`, including the pre-feature-preset fallback case; shooter-proficiency global persistence via `savePrefs`, confirmed untouched by loading a different preset; the chronograph SD extension (known-sample SD value, single-shot-log null case, `applyFxChronoSd()`'s label flip and hand-edit reversion, `clearTruingState()`'s reset); and a full `showProbHit()`/`renderProbHit()` end-to-end pass (auto-suggest-then-override lifecycle mirroring HUD's proven pattern, headline percentage rendering, error-budget table population, and monotonicity of the displayed probability against shooter proficiency and target size). Full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to `Personal/index.html` and `CervidBallistics_GeneralRelease.html` (one hunk needed a manual reapply against General Release's extra `updateSimpleModeUI()` line, unrelated to this feature) with zero `.rej` files remaining and byte-identical pairs confirmed both ways; `index.html` re-synced from `CervidBallistics_GeneralRelease.html`. (General Release's own pre-existing, unrelated `test_export_amended_builtins.js` failure — present before this change too, caused by that build's intentionally empty `PRESETS` arrays — is untouched by this feature.) Added a new "Probability of Hit" guide topic (Section 10, Field Tools) directly after HUD; added a new Section 18 to the Math Models docx (appended after the chronograph-import section, following this document's established practice of adding new sections at the end of Part 1 rather than renumbering existing ones) covering the sensitivity method, the RSS combination and its independence assumption, the angular/linear group-size conversion, and the elliptical hit-probability integration with its Rayleigh cross-check — validated clean and visually confirmed by rendering to PDF.

**2026-09-09 — Added a bold "Getting Started" title to the empty-state screen, and enlarged it and the instruction line beneath it (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User request: a bold "Getting Started" title under the icon graphic, above the existing "Select a Quick Preset or configure your load, then tap Calculate Trajectory" line, with both made larger than the body text. Added `<p style="font-weight:700;font-size:17px;margin-bottom:6px;">Getting Started</p>` immediately after the icon `<div>` and before the existing instruction paragraph, and bumped that instruction paragraph itself to `font-size:15px` (from the inherited 14px body default) so it reads clearly larger than both the 14px body default and the 11px fine print in the guide/disclaimer block further down, while "Getting Started" at 17px and bold reads as the more prominent of the two. Same two-places-in-the-file pattern as every other empty-state edit — the static markup and the JS `setMode()` reset string both updated identically via a single find/replace. Pure markup/CSS addition; full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Moved the "guide" sentence to below the Safety & Disclaimer notice on the empty-state screen (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User asked to relocate "The guide within the app contains valuable insights into the features contained within the ballistic calculator and how the physics engine creates firing solutions." from above the Safety & Disclaimer block to below it. New order top-to-bottom: Safety & Disclaimer heading and its two paragraphs, then the guide sentence, then "Send feedback to...", then the copyright line — keeping the same `<br><br><br>` spacing already used around the disclaimer on both sides of its new position. Same two-places-in-the-file pattern as every prior empty-state edit — the static markup and the JS `setMode()` reset string both updated identically via a single find/replace. Pure reordering, no wording changed; full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Added an extra line break before and after the Safety & Disclaimer notice on the empty-state screen (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User asked for more visual separation around the notice added earlier the same day. Changed the single `<br><br>` on each side of the "Safety & Disclaimer" block to `<br><br><br>`, giving it a bit more breathing room from both the guide blurb above it and the "Send feedback to..." line below it. Same two-places-in-the-file caveat as the original addition — the static markup and the JS `setMode()` reset string both updated identically via a single find/replace. Pure spacing tweak; full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Restyled the "Apply Avg Velocity" button in the chronograph shot-log import from muted secondary styling to the app's blue accent colour (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback: the button was easy to miss, unlike **Verify Second Point** just above it in the same Trajectory Truing section, which uses the same blue `--accent` colour every other primary action button in the app uses. Swapped `class="btn btn-secondary"` (grey background, bordered, low-contrast) for `class="btn" style="width:auto;background:var(--accent);"` — same blue as Verify Second Point, True Trajectory, and Calculate Trajectory, while keeping `width:auto` so it still sits compactly rather than stretching to fill the flex column it shares with **Apply Avg BC**. Only the velocity button was changed, per the specific request; **Apply Avg BC** was deliberately left as `btn-secondary` for now since it wasn't raised as an issue — worth revisiting for visual consistency if it turns out to have the same "easy to miss" problem once the drag-model gating lets it show enabled. No test asserts button class/style (only text content), so nothing needed updating there; full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Renamed the "Parse Chronograph Log" button to "Read Chronograph Log" (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User asked directly whether users would understand the term "parse" on the FX chronograph import button; agreed it's unnecessary developer jargon sitting right under a section already titled "Import Chronograph Shot Log," and that plainer wording ("read the log") describes the action just as precisely without assuming technical vocabulary. Renamed only the user-visible button label and its resulting status message ("Parsed N shots" → "Read N shots") — left every internal function/variable name (`parseFxChronoLogUI()`, `parseFxChronoLog()`, `parsed`, etc.) untouched, since those are implementation detail with no bearing on what a user sees. Updated the two matching guide sentences in Section 7 ("press **Parse Chronograph Log**" → "press **Read Chronograph Log**"; "Once parsed, you'll see..." → "Once it's read, you'll see..."). The Math Models docx's Section 17 already used "parse"/"parser" only in its technical, developer-facing sense (describing the parsing algorithm, not quoting the UI button) and needed no change. Updated the one test assertion in `test_fx_chrono_import.js` that checked for the literal status text "Parsed 6 shots," to "Read 6 shots" instead — full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files and the guide's Personal/ copy, with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Added a Safety & Disclaimer notice to the empty-state screen shown before a trajectory is calculated (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User-requested legal/safety text: "This ballistic calculator provides estimates for informational purposes only. Results may vary due to ammunition, equipment, environmental conditions and other factors. Do not rely on calculated data as a substitute for verified range data or safe shooting practices. Always handle firearms and airguns responsibly, follow established safety procedures, positively identify your target and what lies beyond it, and comply with all applicable local laws and regulations." Inserted into the existing empty-state blurb directly after "...how the physics engine creates firing solutions." and before the "Send feedback to..." line, under a bolded "Safety & Disclaimer" heading, using the same `<br><br>` paragraph-break style already used throughout that block. This text exists in two identical places in the file — the static markup baked into `<div id="mainArea">` and the JS string `setMode()` rewrites `mainArea.innerHTML` with on every category (air/rimfire/centrefire) switch — both updated identically via a single find/replace so they can't drift out of sync with each other. Pure copy addition, no logic touched; full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed. No guide/docx changes — this is app-copy/legal text, not a feature the guide or Math Models docx describes.

**2026-09-09 — Corrected an inaccurate claim about the FX True Ballistics Chronograph in the guide, docx, and code comments for the shot-log import feature (cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx, CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User flagged that the guide's "Import Chronograph Shot Log" topic (Section 7) stated the FX device "don't measure velocity at multiple points down range the way a Doppler unit does" — factually wrong: the FX True Ballistics Chronograph is itself Doppler-based hardware and genuinely does measure velocity at multiple points down range from a single shot, the same underlying technology as a LabRadar. The real distinction driving this feature is about the *exported log*, not the hardware: the FX device's own shot log doesn't expose that full point-by-point velocity string — it reports just one summarised velocity and one already-computed BC estimate per independent shot. Rewrote the guide topic's opening paragraph to make this explicit (it isn't about Doppler vs. non-Doppler hardware, it's about what the export exposes), and added the related, previously-unstated point that many other chronographs go further still and report only muzzle velocity with no BC of any kind. Made the equivalent correction to the Math Models docx's Section 17 opening paragraph (regenerated the companion PDF) and lightly tightened two related code comments in `CervidBallistics.html` that used the same loose "structurally different kind of chronograph data/data shape" phrasing, which could be misread the same way — clarified both are about the exported shape, not the underlying hardware, then propagated the comment-only change to the other three canonical files. No functional/behavioural change; parsing, averaging, and gating logic are untouched. Full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html` after the comment edit, then propagated via diff/patch to all three other canonical files with zero `.rej` files and byte-identical pairs confirmed.

**2026-09-09 — Added FX True Ballistics Chronograph shot-log import to the Trajectory Truing section — averages a multi-shot log's own velocity and BC readings, rather than fitting anything (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to the Doppler BC fit shipped earlier the same day: user asked whether a sample CSV from an FX True Ballistics Chronograph could be supported "without a drama," then uploaded a real exported log. Reading that sample revealed this device's data has a fundamentally different shape from the Doppler case the previous feature assumes — there is no downrange distance at all; instead the device fires a string of independent shots and prints, for each one, its own velocity alongside its own already-computed BC estimate (a "T-BC(G1): value" reading). That meant this isn't a second Doppler-style regression target; the correct operation is a plain average of already-computed answers, the same reasoning already used informally whenever several ordinary chronograph readings are trusted more than any single shot. User's explicit request was to import the average muzzle velocity, with the constraint (stated directly) that the parser must handle any shot count, not just the sample's 6 — the sample was treated purely as a format reference, never as a hardcoded count. Added `parseFxChronoLog(text)`: splits on lines, recognises a "Shot count: N" declaration if present (cross-checked later against how many rows were actually readable), and reads any comma-containing line shaped like a shot row — the first `<number>,FPS` pair as velocity (the sample's format repeats it a second time on the same line; only the first is used) and a `T-BC(model): value` field as that shot's BC — skipping everything else (prose, weather line, summary stats) without needing it trimmed out first. New "Import Chronograph Shot Log" advanced subsection added to Trajectory Truing, directly below the Doppler fit block: a single paste-in textarea (the whole exported log, unedited), a Parse button, a per-shot table (velocity and BC for every shot, with any shot whose BC sits more than 15% from the average flagged), and two actions — Apply Avg Velocity (writes straight into Muzzle Velocity, independent of `bcMultiplier`/Trued badge machinery, exactly like a manual edit to that field) and Apply Avg BC (sets the same `bcMultiplier` the single-point and Doppler methods both use). Apply Avg BC is deliberately gated: disabled unless every shot in the log reports the same drag model *and* that model matches the currently-selected Drag Model, since silently applying a T-BC(G1) reading while G7 is selected would mix two different drag models with no obvious symptom until the trajectory looked implausible — the button relabels itself to say which model to switch to when gated off. `clearTruingState()`/`resetTruing()` extended to clear the new fields (pasted log, parsed result, table, status), matching the existing pattern for the single-point and Doppler fields. New `test_fx_chrono_import.js` (41 checks) covers: parsing the real uploaded sample exactly (6 shots, avg velocity 760.83 matching the device's own printed "Avg: 761 FPS," avg BC 0.0245); arbitrary shot counts from 1 to 11 (proving nothing is hardcoded to the sample's 6); ignoring prose/notes lines; recognising a different drag model (G7); the full `parseFxChronoLogUI()` handler end-to-end (table rendering, button labelling, declared-vs-parsed count mismatch warning, mixed-drag-model warning); drag-model gating on `applyFxChronoBc()` including a defense-in-depth check that calling it directly (not just via a disabled button) still refuses when the models don't match; `applyFxChronoVelocity()` with metric conversion; and Reset clearing every new field. Full existing regression suite (44 of 45 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and `node --check` clean on all four. Added a new "Import Chronograph Shot Log" guide topic (Section 7, Calibrating to Your Rifle) directly after the Doppler fit topic; added a new Section 17 to the Math Models docx (appended after the Doppler fit's Section 16, following this document's established practice of adding new sections at the end of Part 1 rather than renumbering existing ones) explaining the averaging methodology and the drag-model gating rationale — validated clean and visually confirmed by rendering to PDF.

**2026-09-09 — Added a Doppler / multi-point BC fit to the Trajectory Truing section, for shooters with a Doppler radar chronograph (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to a design discussion (comment-only) about letting users attach reference data to a load: walked through what "reference data" could mean (a bigger observation log for casual truing; a general notes/journal layer; a full custom drag-curve import), and initially recommended against a manual multi-point observation log — real field data usually has soft/guessed atmospheric conditions per point, and fitting a regression to noisy inputs produces a more precise-*looking* number without being more accurate, running against this project's own "deliberate simplicity over feature completeness" principle (Math Models docx, Part 2). User's counter-point reframed it: a Doppler radar chronograph (e.g. LabRadar) measures dense, clean velocity data from a *single* shot under a *single* set of conditions, which sidesteps exactly the data-quality objection raised — condition-mismatch and hand-read noise both disappear when every point comes from the same string. Agreed this changes the calculus, then built it. Scoped to "fit BC only" (recommended over a more ambitious custom-drag-curve import, which would need `getCd()` to accept a derived table instead of a fixed G1/G7/GA lookup — flagged as a much bigger architectural change and left for later) and to a generic paste-in parser rather than one built against a specific device's export format (no sample file was available to build against, and a wrong device-specific assumption risks silently misreading columns). New "Fit BC from Doppler Data" advanced subsection sits directly below the existing single-point True Trajectory/Verify Second Point flow: distance-unit and velocity-unit selectors, a Column Order toggle (Distance,Velocity vs Velocity,Distance — deliberately not auto-detected, since a wrong silent guess would swap two different physical quantities into the fit with no obvious symptom until the result looked implausible), a paste textarea tolerant of comma/tab/space delimiters and a header row (skipped and counted, not treated as an error), and a "Fit BC from Data" button. Unlike the existing single-point solve (one observation, one unknown, always solvable exactly via bisection), this is a genuine regression: minimizes the sum of squared *velocity* residuals across every entered point — velocity, not holdover, since that's what a Doppler unit actually measures directly, and comparing it needs no zeroing/sight-in geometry at all, making it a cleaner fit target than reusing the holdover-based comparison would have been. Implemented as a coarse 60-point scan across the same [0.3, 2.5] BC-multiplier bounds the existing single-point Truing already uses, refined with a golden-section search around the best coarse sample (guards against the sum-of-squares curve failing to be strictly unimodal with noisy real data, even though every case tested was well-behaved). Added `fpsRaw` (unrounded velocity) to `computeTrajectory()`'s returned rows — same "precise raw field alongside the display-rounded one" precedent as `energyFtLbRaw`/`energyJRaw` — since the existing rounded `fps` field isn't precise enough for a regression across closely-spaced points. Deliberately sets the *same* `bcMultiplier` the single-point flow does (only one method's result is ever in effect at a time), so the Results page's Trued badge, Save-to-preset, and Reset all needed zero changes — they already operate purely on `bcMultiplier` with no awareness of which method produced it. Results are shown with a per-point residual table (observed vs. predicted velocity, RMS error) rather than just the fitted number, so a shooter can judge fit quality and spot an outlier point themselves — confirmed with synthetic-data tests that a single bad point shows a visibly larger residual than the others after fitting, without needing to be manually flagged. New `test_doppler_bc_fit.js` (29 checks) covers: parser tolerance (mixed delimiters, header rows, both column orders, both unit systems, blank/junk/negative lines); fit accuracy recovering three different known BC multipliers (+15%, -12%, exactly 1.0) from noise-free synthetic data generated by this app's own `computeTrajectory()`; robustness within 1% under ~1-2fps of realistic jitter; outlier behavior (a single 60fps-off point shifts the fit moderately, as expected of a least-squares average, while itself showing a clearly larger residual than the other points); the full UI handler end-to-end (shared status line, residual table rendering, Trued badge, Save button state); and Reset/no-Calculate/insufficient-points guards. Full existing regression suite (47 files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and the regression suite re-run clean against both propagated pairs too. Added a new "Fit BC from Doppler Data" guide topic (Section 7, Calibrating to Your Rifle) alongside the existing Truing/Verification topics; added a new Section 16 to the Math Models docx (appended after Aerodynamic Jump, following this document's established practice of adding new sections at the end of Part 1 rather than renumbering existing ones) with the regression methodology, the objective function, and the same reasoning as the code comments — validated clean and visually confirmed by rendering to PDF.

**2026-09-08 — Added Aerodynamic Jump, a wind-induced vertical shift in point of impact, as a new physical effect baked into Drop (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to the comment-only "Aero Jump on the headline tiles" discussion and its quantitative follow-up (which found it roughly comparable in size to Spin Drift once dia/len/twist are entered): user approval to build it. Distinct mechanism from both existing wind-related corrections — a bullet exiting the muzzle into a crosswind briefly yaws off-axis, and because it's spinning, gyroscopic precession converts that transient yaw into a vertical (not lateral) shift within the first few calibers of flight, well before the longer-term yaw-of-repose behaviour that drives Spin Drift takes over. Implemented Bryan Litz's published rule-of-thumb (*Modern Advancements in Long Range Shooting*): `Y (MOA/mph) = 0.01×Sg − 0.0024×L + 0.032`, where L is bullet length in calibers (lenIn/diaIn) — verified the resulting figures against this project's own earlier hand-calculated reference values for two real loads (.308 Win 150gr: Sg=2.194, L=3.987cal → 0.0444 MOA/mph; .264 CM 130gr: Sg=2.853, L=4.265cal → 0.0503 MOA/mph) before trusting the code. Direction was the single highest-risk part of this feature — getting it backwards would hand a shooter a correction that's wrong rather than merely small — so it was verified against web-sourced descriptions of Litz's own published convention before writing any code: a right-hand-twist bullet jumps *up* for a crosswind from the right and *down* for one from the left, reversed for left-hand twist. Wired via a new `crosswindFromRightMph = windMph×sin(windClock×π/6)` term (positive = wind FROM the right, the same sin() this app's Wind Clock already uses under the 2026-08-31 Kestrel/origin convention fix) multiplied by `twistDir`, producing a fixed angular correction (`aeroJumpMilsFixed`) applied to Drop with `dIn -= aeroJumpMils×range×0.036/cosA2` — the identical "constant angle, grows linearly with range" treatment and subtraction direction the Zero Offset elevation term already uses, rather than an accumulated-over-flight effect like Wind Deflection or Spin Drift. Deliberately reads only the shooter's own `windMph`/`windClock`, never a Wind at Target reading — the responsible yaw resolves within the first few calibers of the muzzle, so nothing about it depends on conditions further downrange (confirmed with a dedicated test). Gated the same way Spin Drift already is (only computed when Sg is available, i.e. diameter+length have been entered) plus one more restriction: explicitly excluded for `shape:'diabolo'`, since Litz derived and validated this formula against conventional supersonic spitzer/boat-tail projectiles, with no established validity for slow, subsonic, waisted airgun pellet shapes — the Results page shows an explicit "N/A for diabolo shape" rather than a number dressed up as physics it was never shown to support; flat-base and boat-tail airgun slugs are not excluded. Added a new three-state "Aero Jump" headline stat card (no dia/len entered; diabolo N/A; real value + a plain-language "Jump ↑ (shoots high)"/"Jump ↓ (shoots low)" direction line), tagged with the `cardTag()` helper added earlier this session — "Included in hold," since it's baked directly into the Drop figure the shooter actually dials. `params.shape` — previously only a local variable and a `stability.shape` field, never on `params` itself — had to be added to both `calculate()`'s and `buildParamsFromPreset()`'s params objects so `computeTrajectory()` could gate on it; this also meant Reticle View, HUD, and Compare Loads' wind-override paths needed no changes at all beyond that one addition, since all three already do a full `computeTrajectory()` recompute on override (not a cached-row rescale) — confirmed end-to-end with a test showing Reticle View's wind-clock override produces different elevation holds at clock=3 vs clock=9 with no code changes to `renderReticleView()` itself. New `test_aero_jump.js` (24 checks) covers: magnitude against the two hand-calculated reference loads; direction for RH/LH twist crossed with wind-from-left/right; zero at pure head/tailwind; gating off for missing Sg, missing dia/len, and diabolo shape; Wind-at-Target independence (with a sanity check proving the params genuinely differ); internal consistency between the reported inches/cm/MOA/mils fields and the actual dIn adjustment; and the Reticle View end-to-end override case. Full existing regression suite (46 files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to all three other canonical files with zero `.rej` files, byte-identical pairs confirmed, and the regression suite re-run clean against both propagated pairs too. Added a new "Aerodynamic Jump" guide topic (Section 9, Advanced Corrections) explaining the mechanism, direction convention, and diabolo exclusion in plain language, with a callout on the exclusion; added a new Section 15 to the Math Models docx (appended after Plausibility Checks, following this document's established practice of adding new sections at the end of Part 1 rather than renumbering existing ones) with the formula, derivation, and the same direction/gating reasoning as the code comments — validated clean and visually confirmed by rendering to PDF.

**2026-09-08 — Added an "Included in hold" / "Reference only" tag to every headline stat card on the Results page (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to a comment-only discussion about adding Aero Jump to the headline tiles: raised that Spin Drift and Aero Jump would sit in the same visual "bucket" as Muzzle Energy or Gyro Stability despite one being baked into what the shooter actually dials/holds and the other being purely informational, with nothing on the card itself telling the shooter which is which — a problem that already existed for the current card set, independent of whether Aero Jump gets added. User agreed and asked to code the labeling fix (Aero Jump itself remains a separate, undecided feature — not built in this change). Added a single shared `cardTag(included)` helper returning a small bordered-off line at the bottom of a card, styled via two new CSS classes (`.card-tag-included` in the existing `--green` accent, `.card-tag-ref` in `--muted`) so the wording and look can't drift apart between cards over time. Applied to every `.stat-card` in `renderResults()`'s headline grid: **Included in hold** — Drop at range, Wind at range, and Spin Drift at range (all three feed the combined dial/hold figures used by the Range Card, Reticle View, HUD, and Turret Dial); **Reference only** — Muzzle Energy, Muzzle Velocity, Velocity at range, Energy at range, Subsonic at, Air Density, Gyro Stability (Sg), Min Twist (Sg 1.5), and the conditional BC Truing card (the truing multiplier is applied upstream to the physics model rather than itself being a value the shooter dials). Confirmed via a full audit (`grep` for every `.stat-card` occurrence in the file) that this is the complete set — no separate Coriolis headline card exists (Coriolis is a table column only), and the Zero Offset/Windage-Offset/baked-truing badges in the results header are `<span>` elements, not `.stat-card`s, so they were out of scope for this specific request. Pure markup/CSS addition, no calculation logic touched; full existing regression suite (45 of 46 test files, excluding the one pre-existing, unrelated `test_chart_energy_smoothing.js` failure already documented below) re-run clean on `CervidBallistics.html`, then propagated via diff/patch to `Personal/index.html`, `CervidBallistics_GeneralRelease.html`, and `index.html` — all four applied with zero `.rej` files, and the regression suite re-run clean against both the GeneralRelease and Personal copies too. Propagated to all four files (not preset-scoped) since this is a general UI clarity fix unrelated to personal preset data. No guide/docx changes made — neither document currently describes the stat-cards' individual meanings in a way this would contradict.

**2026-08-31 — Rebuilt the built-in default presets from the user's own exported preset backup (CervidBallistics.html, Personal/index.html only — CervidBallistics_GeneralRelease.html/index.html deliberately untouched).** User uploaded `cervid_presets_2026-08-31.json` (the app's own Export Presets output) and asked to make its contents the shipped defaults. That export's `custom` entries each optionally carry a `_restoreBuiltinIdx` pointing at the original built-in they supersede, and its `hidden` sets record which built-in indices the user had already superseded on their own device — together these fully describe the user's live personalized preset list as a diff against the old built-in arrays, so the fix was to fold that diff directly into `PRESETS.air`/`PRESETS.cf` rather than just appending the export's raw entries alongside the untouched originals (which would have produced confusing near-duplicates). Of the original 9 air and 5 cf built-ins, all but two (`ZAN .25 45gr`, `.308 Win 150gr` — kept exactly as they were, since the export carried no amended replacement for either) were superseded by an edited version: most just gained an `interval` and/or an extended `max` range, but several carried genuine field-corrected data the user had clearly trued against real-world results — `JSB Exact Monster .22 25.4gr`'s BC corrected from 0.084 to 0.059, `FX .25 34gr`'s BC corrected from an implausible 0.330 down to 0.103 (with velocity 855→887), and `H&N Heavy Slug .25 38gr`'s BC/velocity/scope-height corrected from 0.094/923/3.0 to 0.112/937/3.25 — plus a few gained a switched drag model (G1→GA or G1→G7+BC7) and/or a saved Zero Offset. Six brand-new loads were promoted from the export straight into the defaults: `FX .177 10.3gr`, `Jugg .22 25.39`, `JSB Jumbo Beast.22`, `JSB KO Slug 25.39gr`, `FX Hybrid Slug`, and `.22 Frangible 40.1gr` (air), plus `.243 Win Norma` (cf) — bringing the shipped totals to 15 air and 6 cf presets. `CervidBallistics_GeneralRelease.html`/`index.html` were deliberately left alone: that build ships with empty `PRESETS.air`/`cf`/`rf` arrays on purpose (a clean public build with no personal load data baked in — see the guide's own "General Release" callouts), and this update is inherently personal preset data, not a general-purpose feature. Also bumped `CUSTOM_KEY`/`HIDDEN_KEY`/`ORDER_KEY` from `_v1` to `_v2` (in the two files that got the new array) — `HIDDEN_KEY` stores superseded built-ins by array *index*, and reordering/expanding `PRESETS` out from under an existing browser's saved v1 hidden-set would have silently hidden the wrong (unrelated) new entries; `CUSTOM_KEY` was bumped too since several of this exact export's own custom entries are now baked-in built-ins, and leaving the old v1 custom array in place would show near-duplicates of the same load. The `_v1`→`_v2` bump is a clean-slate migration (old data simply becomes unreadable, per the existing `loadCustom`/`loadHidden` `||{}` fallback pattern) rather than an attempted index remap, since remapping would have required trusting stale, possibly-inconsistent state across however many devices/browsers already have v1 data. Full existing regression suite re-run clean on both the CervidBallistics.html/Personal pair (excluding the one pre-existing, unrelated failure already documented below) and independently on the General-Release pair (confirming its untouched, empty preset arrays still work correctly). No guide/docx changes needed — neither hardcodes specific built-in preset names, counts, or figures.

**2026-08-31 — BEHAVIOUR CHANGE: fixed the Wind Clock convention to consistently mean "the clock position the wind is blowing FROM," matching a Kestrel or any other wind meter (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Prompted by the user asking how a real Kestrel wind meter reads direction (answer: the "origin" method — you orient the instrument into the wind and read where it's coming from) and then asking to switch the app to match. Investigation found this wasn't just a preference choice: the app's two wind-clock terms in `computeTrajectory()` already disagreed with each other. The head/tailwind term (`rangeWindFps`, the cos() component) was already self-consistent with the "origin" convention — a positive value there was documented and behaving as a headwind at 12 o'clock, i.e. wind FROM the target's direction. But the crosswind term (`windFps`, the sin() component) used a bare `+sin()` with no negation, which actually implemented the opposite "destination" convention — entering "3" pushed the bullet right, as if the clock meant the direction the wind blows TOWARD. Confirmed independently two more ways: the guide's own pre-existing "Wind Clock = 3 means a full-value wind from your right" text already described the origin convention (so the *documentation* was correct and the *code* was backwards, not the other way round), and the live-weather auto-fill feature (`fetchLiveWeather`) was always deriving the Wind Clock field from Open-Meteo's `wind_direction_10m` — an explicitly "degrees true, where wind comes FROM" meteorological bearing — feeding an origin-convention number into a destination-interpreting formula, so its auto-filled crosswind push has quietly been mirrored (backwards) since that feature was built. Fixed with a single, surgical change: negated the crosswind term only (`windFps`/`windFpsTarget`, both the shooter and Wind-at-Target readings) — the head/tailwind term needed no change, since it was already correct. This is a genuine behaviour change, not just a bugfix a user would be indifferent to: a Wind Clock value entered under the old convention now produces a crosswind push on the *opposite* side of the clock face than before. Updated everything downstream that assumed the old convention: the on-field hints under Wind Clock and Wind at Target Clock ("3=full R, 9=full L" → "3=from R (push L), 9=from L (push R)"), the Wind Clock help-tip text, and the Reticle View wind indicator's PUSH/R-L derivation — its arrow still points at the literal entered clock (which now correctly reads as "wind FROM," i.e. a small weathervane/Kestrel-style display), but the derived push-side letter is now the deliberate negation of the arrow's own angle (`pushWdx = -wdx`) rather than the same sign, since the push is now the opposite side from what the arrow points at. Four existing tests hardcoded the old R/L sign and were updated to the new (correct) expected values: `test_range_card_wind_per_mph.js` (loosened its regexes to allow a leading minus sign, since that test is about label *format* not direction, plus fixed a magnitude comparison to use `Math.abs()` instead of a raw signed comparison), `test_reticle_wind_indicator.js`, `test_reticle_wind_override.js`, and `smoke_reticle_wind.js` (all three swapped their expected R/L letters at clock=3/9). All other existing wind-related tests (`test_wind_at_target.js`, `test_headwind_tailwind.js`, `test_compare_wind_override.js`, `test_wind_target_maxyd_fix.js`, both Compare wind smoke tests) passed unchanged, since they test relative/magnitude behaviour rather than an absolute direction tied to a specific clock number. Full regression suite re-run clean on both file pairs (excluding the two pre-existing, unrelated failures already documented below). Updated the guide's "Wind Speed & Wind Clock," "Reticle View," "Overriding the wind in Reticle View," and "Overriding the wind for a comparison" topics, adding a callout explicitly flagging the correction and its practical effect; updated the Math Models docx's Section 5 (Wind Deflection) — added the origin-convention framing and the negative sign to the crosswind formula, fixed the dial-mapping paragraph's stale "3=full R, 9=full L" phrase, and added a dated "Wind Clock convention fix" paragraph explaining the inconsistency and the fix in the same terms as the code comments; regenerated the docx's companion PDF.

**2026-08-31 — Fixed a silent Wind at Target/maxYd mismatch in HUD and Compare Loads' live-matched slot, and added a visible cue in Reticle View when Wind at Target is active (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Investigated at the user's prompt while discussing whether Wind at Target should extend to HUD and Reticle View. Found a real correctness bug, not just a UX gap: HUD's `renderHud()` and Compare Loads' `runCompare()` live-matched slot both build their working params by cloning `lastResults.params` wholesale (`Object.assign`) and then overriding `maxYd` for their own display purposes — HUD to whatever specific distance is currently ranged, Compare to the shared comparison distance. Since `windTargetMph`/`windTargetClock` rode along unchanged in that clone, but the blend fraction (`frac(x) = x/maxYd`) was still computed against the *overridden* maxYd, either view would silently reinterpret "the target" as whatever short (or long) distance it happened to be showing right now, rather than the range the shooter actually meant when they entered the second wind reading — applying most of the differential wind far too early, or too late, without any indication anything was off. Fixed by adding a new `windTargetRefYd` field to `computeTrajectory()`'s params: `frac(x)`'s denominator now prefers `windTargetRefYd` when present, falling back to `maxYd` when it's not (so the primary Inputs-tab `calculate()` path, Reticle View, and non-live Compare Loads slots — none of which override `maxYd` in a way that changes what "the target" means — are entirely unaffected). Both HUD's `renderHud()` and Compare's live-slot construction in `runCompare()` now explicitly set `windTargetRefYd: lastResults.params.maxYd` alongside their existing `maxYd` override, pinning the blend to the original table's own max range regardless of what shorter or longer distance is currently being displayed. Reticle View needed no such fix — its `effParams` never overrides `maxYd` at all, so it was already correct — but it also had no visible indication that its windage figure includes a second, unadjustable wind reading; added a small note (`reticleWindTargetNote`) below its wind override row, shown whenever `windTargetMph` is active, stating the override only adjusts the near/shooter-side wind. New `test_wind_target_maxyd_fix.js` covers: a direct `computeTrajectory()`-level demonstration of the bug (a short `maxYd` override without `windTargetRefYd` silently inflates the blend fraction from ~0.2 to ~1.0 at a 200yd check point against a 1000yd table) and its fix; the same scenario exercised through HUD's actual `showHud()`/`renderHud()` code path; the same scenario exercised through Compare Loads' actual `runCompare()` live-slot path; and the Reticle View note showing/hiding correctly. Full existing regression suite re-run clean on both file pairs. Updated the guide's "Wind at Target" and "Overriding the wind in Reticle View" topics to describe the fix and the new note, and added a `windTargetRefYd` paragraph to the Math Models docx's Wind Deflection section (5) explaining the bug and the fix in the same terms as the code comments.

**2026-08-31 — Added a Reset link to the Wind at Target fields (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the Wind at Target feature immediately below: user request to spare having to manually clear both fields back to blank. Added a small `.link-btn` "Reset" link inline in the first field's label — same placement/style precedent as the existing Reticle View, HUD, and Turret Dial wind/velocity Reset links — wired to a new `resetWindTarget()` that clears `fWindTarget`/`fWindTargetDir` and recalculates if a result is already showing (matching `resetReticleWind()`'s own recalculate-after-reset behaviour). Extended `test_wind_at_target.js` with two checks: the reset clears both fields and forces `lastResults.params.windTargetMph` back to `undefined`, and the reset doesn't throw when called with no prior calculation on screen (`lastResults` still `null`). Full existing regression suite re-run clean on both file pairs. No guide/docx changes needed — this is a convenience control on an already-documented field, not a new capability.

**2026-08-31 — Added "Wind at Target," an optional second wind reading blended across the flight, alongside the existing single Wind Speed/Wind Clock fields (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User request, following a comment-only design discussion: the app previously assumed one uniform wind for the whole flight; this adds an optional "Wind at Target" speed+clock pair (`fWindTarget`/`fWindTargetDir`) for when conditions genuinely differ at the target — calm at the muzzle but gusting further out, say. Left blank (the default), `windTargetMph`/`windTargetClock` are `undefined` and the model behaves exactly as before. When set, `computeTrajectory()` resolves both the shooter's and the target's readings into their own crosswind and head/tailwind Cartesian components (the existing `windFps`/`rangeWindFps` calculations, computed a second time from the target reading) and blends each pair by straight-line downrange fraction (`x(yd)/maxYd`, clamped to [0,1]) rather than interpolating the raw speed-and-clock values directly — averaging clock positions can be physically meaningless (a 3 o'clock and a 9 o'clock wind don't blend to "no wind"), whereas blending the already-resolved sideways and along-the-shot components always behaves sensibly. The blended head/tailwind component (`headwindFpsAt(x)`) is re-evaluated at the bullet's current downrange position on every integration step by reassigning the closed-over `rangeWindFps` variable `derivX()` already reads, including inside the two zero-solve loops, so a zero solved at a shorter range than the table's max correctly sees only a partial blend toward the target reading. The crosswind side required a more structural change: the pre-existing drift formula (`drift = windFps*(t_actual - x/mvFps)*12`, the classic "lag rule") only works as a single closed-form multiplication because the wind speed is constant and factors straight out of the subtraction — once it can vary with position that factoring breaks, so drift is now accumulated incrementally inside the main flight loop instead (`driftAccum += crosswindFpsAt(x)*dLag*12`, where `dLag` is each step's exact elapsed time beyond a no-drag reference), which telescopes back to the exact original formula whenever the crosswind is constant (target wind unset or equal to the shooter's own reading) and is the physically correct generalization when it varies. Confirmed against the live engine, and worth noting explicitly since it runs counter to a naive assumption stated earlier in the design discussion: wind blowing during the *back half* of the flight contributes *more* total drift than the same wind confined to the front half, because the bullet decelerates over the flight and so spends more time (not just more distance) exposed to whatever crosswind is present later on — matching the long-range-shooting principle that a wind read near the target carries more weight than the same wind read near the firing line, not less. `fWindTarget` participates in the metric/imperial unit toggle exactly like `fWind` (added to `BOUND_FACTORS`/`BASE_BOUNDS`, its own `lblWindTargetUnit` span rather than replacing the whole label to avoid wiping its help-tip, the same "Fix Spot Range hint text being wiped by unit toggle" bug class fixed earlier in this project). Deliberately **not** wired into preset save/load or `clearLoadFields()`: investigation confirmed the existing `fWind`/`fWindDir` fields are themselves session-level "current conditions," not saved-load properties (absent from `buildPresetEntryFromForm()`, `loadPreset()`, and `clearLoadFields()` entirely) — Wind at Target follows that same established precedent rather than the Zero Offset precedent originally assumed when scoping this feature. The Results header's "Wind at `<range>`" stat card now shows both readings ("5mph @3 → 12mph @9 (target)") whenever the second reading is active, rather than only the blended drift figure, so there's always a visible confirmation both readings were picked up. New `test_wind_at_target.js` covers: `windTargetMph` staying `undefined` when the field is blank; target wind equal to shooter wind reproducing the uniform-wind baseline to within floating-point summation tolerance; a calm-to-windy ramp producing smoothly growing drift bounded above by the fully-uniform case; the reverse windy-to-calm taper producing measurably less drift at the target range than the ramp (the dwell-time result above); a pure 12 o'clock (head/tailwind-only) target reading producing no spurious crosswind drift; the zero-solve loop still converging correctly with a blended headwind active; metric round-trip conversion; and confirmation that `clearLoadFields()` leaves the field untouched. Full existing regression suite (39 other test files, excluding the two pre-existing, unrelated failures already documented below) re-run clean on both file pairs. Added a new "Wind at Target" guide topic (Section 4, Environmental Conditions) including a callout on the dwell-time/"wind near the target matters more" result; extended the Math Models docx's Section 5 (Wind Deflection) with the interpolation formulas, the generalized incremental drift accumulator, and the same dwell-time finding; refreshed the guide's footer revision date.

**2026-08-30 — Fixed Compare Loads silently ignoring a saved windage offset (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to a gap flagged while building the clicks-unit feature immediately above: `buildParamsFromPreset()` — the function Compare Loads uses to resolve a saved preset's own fields into a physics params object — read a preset's elevation `zeroOffsetVal`/`zeroOffsetUnit` correctly but never read `zeroOffsetWindageVal`/`zeroOffsetWindageUnit` at all, a gap dating back to when the windage field was first added (it was wired into `calculate()` for the live Inputs-tab path but missed here). The practical effect: comparing a preset with a saved windage offset showed correct windage everywhere else (the main Results table, Reticle View, HUD) but silently dropped the offset the moment that same preset appeared inside a Compare Loads comparison. The live-matched slot in `runCompare()` (the currently-loaded preset, shown as "(live)") was never affected, since that path clones `lastResults.params` directly rather than calling `buildParamsFromPreset()`, and `lastResults.params` already carries the resolved figure from `calculate()`. Fixed by adding the identical `zeroOffsetWindageMils` resolution line already used for elevation (via the shared `zeroOffsetToMils()` helper), and threading it into the returned params object. Extended `test_zero_offset_windage.js` with a check confirming `buildParamsFromPreset()` now resolves a saved windage offset correctly, rather than adding a whole new test file for what is fundamentally the same code path as the existing elevation coverage. Full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx changes needed — this restores already-documented, intended behaviour rather than adding a new capability.

**2026-08-30 — Added "clicks" as a third entry unit to both Zero Offset fields, alongside mils and MOA (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to a comment-only discussion: asked whether clicks could join the existing mils/MOA toggle. Flagged two real design considerations before building — clicks depends on a click-size setting that can change (unlike mils/MOA, a fixed ratio to each other), and that setting's own selector previously only existed on the Results/HUD pages, invisible from the Inputs tab where Zero Offset lives — then built it addressing both. Added a third "clicks" button to each field's unit-toggle, plus a click-size `<select>` (`zeroOffsetClickSizeSel` / `zeroOffsetWindageClickSizeSel`) that appears directly under whichever field has clicks active, reusing the exact same 5-option list (1/8, 1/4, 1/2 MOA, 0.1/0.2 MRAD per click) and the single shared `clickSizeMoa` global already used by the Results table's own clicks column and the Turret Dial — not a new, field-specific click size. `setClickSize()` now also calls a new `refreshZeroOffsetClickSizeSelects()`, so changing the size from either Zero Offset field's own selector, or from the Results table's, keeps every visible selector in agreement. New shared helper `zeroOffsetToMils(raw,unit)` replaces three near-duplicate mils/MOA ternaries (both fields in `calculate()`, plus Compare Loads' `buildParamsFromPreset()`, which resolves a saved preset's own offset the same way and would otherwise have silently mis-converted a clicks-unit preset as if it were raw mils) with one function, adding the clicks branch (`raw*clickSizeMoa*(1.0472/3.6)`) in a single place. Deliberately did *not* treat clicks as a frozen third persisted unit — the raw click count and the unit string "clicks" are what get saved to a preset (via the existing `zeroOffsetVal`/`zeroOffsetUnit` fields, unchanged), and the angle is resolved fresh from whatever `clickSizeMoa` is active every time Calculate runs, exactly the same "live, never baked" principle already documented for the Results table's own `toClicks()` — explicitly not a new class of behaviour, extended into an editable field rather than only a read-only display. New `test_zero_offset_clicks.js` covers: the click-size select showing/hiding correctly with the toggle on both fields; both selects staying in sync via the shared global; the conversion math at a given click size for both elevation and windage; the deliberate live-not-baked behaviour (the same 4 clicks resolving to a different mils value after the click size changes, verified as intended rather than a bug); preset save/load round-tripping "clicks" as a unit string for both fields, including the select correctly reappearing on reload; and Compare Loads' `buildParamsFromPreset()` correctly resolving a clicks-unit saved elevation offset. Full existing regression suite (including `test_zero_offset.js` and `test_zero_offset_windage.js`) re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). Updated the guide's Zero Offset topic (Section 8) to mention clicks on both fields and added a callout explaining the click-size dropdown and its live-not-baked caveat; updated the Math Models docx's Section 13 with the same clicks conversion detail and a cross-reference to Part 3 Section 3's existing click-size discussion; refreshed the guide's footer revision date. **Noted but not fixed, out of scope for this change:** while extending `buildParamsFromPreset()`, confirmed it has never read a saved preset's `zeroOffsetWindageVal`/`zeroOffsetWindageUnit` at all (only the elevation fields) — a pre-existing gap from when the windage field was first added, meaning Compare Loads currently ignores a saved windage offset entirely. Worth a follow-up fix, flagged to the user rather than folded into this change.

**2026-08-30 — Added a second Zero Offset field for windage, alongside the existing elevation one (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User request: extend Zero Offset — previously elevation-only, a known holdover at the zero range for a specific projectile that shifts the whole elevation table by a constant angular amount without touching BC or drag — to cover windage too. Renamed the existing field's label to "Zero Offset – Elevation" and added a new, fully independent "Zero Offset – Windage" field directly below it, with its own mils/MOA unit toggle (`zeroOffsetWindageUnit`, `setZeroOffsetWindageUnit()`) so the two can be entered in different units if needed. Threaded a `zeroOffsetWindageVal`/`zeroOffsetWindageUnit` pair through every place the elevation equivalent already existed: `buildPresetEntryFromForm()` (save), `loadPreset()` (restore, with the same definite-state-on-every-load and calibre/turret-preference-based unit default as elevation), `setMode()` and `clearLoadFields()` (reset on category switch / "+ New Load"), and `calculate()` (converted to a canonical mils-equivalent, `zeroOffsetWindageMils`, the same way as elevation). Applied inside `computeTrajectory()` as `drift += offsetMils*range*0.036/cosA2` — deliberately additive with no leading minus sign, unlike elevation's `dIn -= ...`, because the downstream `driftMoa`/`driftMils` formulas already carry no negation themselves (unlike `moa`/`mils`, which do), so adding here is what makes a positive entered value read as a positive right-hand correction, matching the app's "positive = right" convention (Wind, Spin Drift, Coriolis, `fmtDir()`) rather than the elevation field's "positive = hold up" one. Also unlike elevation, windage isn't already exactly zero at the zero range by construction (real crosswind deflection there is normally small but nonzero), so this is "shift the whole windage figure by a known constant," not "make the zero row read a real value instead of always zero" the way elevation's version is. Because the term is added directly into the shared `drift` variable before `driftMoa`/`driftMils`/`driftIn`/`driftCm` are derived from it, it automatically flows into everything downstream that already reads those fields: the main results table's Wind column, CSV export, and — since `totalWindMoa`/`totalWindMils` are built from `driftMoa`/`driftMils` plus Spin Drift and horizontal Coriolis — the combined windage figure Reticle View, HUD, and the Turret Dial's Windage control already use, with no separate wiring needed in any of them. Added a second results-header badge ("Zero Offset (Wind)", using `fmtDir()` for R/L rather than the elevation badge's +/- sign) alongside the renamed "Zero Offset (Elev)" badge, both following the results table's mils/MOA display toggle exactly as the original single badge did. New `test_zero_offset_windage.js` (mirroring the structure of the original `test_zero_offset.js`) covers: a constant angular shift at every range with no wind present; the R/L direction convention via `fmtDir()` for both positive and negative entries; additivity on top of real crosswind drift rather than replacing it; the combined `totalWindMils`/`totalWindMoa` figure reflecting the offset; complete independence in both directions between the elevation and windage fields (each leaves the other's columns byte-identical when only one is set, and both apply correctly together); MOA-unit entry and its mils-equivalent conversion; negative-value preset save/load round-trip; a legacy preset with no saved windage offset resetting the field to blank rather than leaking a previous value; and reset on mode switch and "+ New Load". Also updated `test_zero_offset_badge_unit.js`'s four string assertions to expect the renamed "Zero Offset (Elev)" badge text — a deliberate, expected change from this feature, not a regression. Full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). Updated the guide's Zero Offset topic (Section 8) to describe both fields and their sign conventions, added a cross-reference from the combined-windage topic (Section 10) noting the windage offset feeds into that same combined figure, and refreshed the footer's revision date. Retitled the Math Models docx's Section 13 to "Zero Offset (Manual Holdover Correction) — Elevation and Windage," added the windage formula and a paragraph explaining the two structural differences from the elevation term (the sign, and the no-self-cancellation-at-zero point), and pluralised the surrounding prose that previously assumed a single field — verified by rendering to PDF and reading the affected pages.

**2026-08-29 — Sized the BC (G7) field's "optional" hint text to match its sibling field-adjacent hints (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User comparison: asked whether the BC (G7) hint and the Projectile Shape hint ("(approx. gyro stability correction)") were the same size — they weren't. Auditing every field-adjacent muted hint in the file (the small descriptive text sitting next to a field label, as opposed to page-level footnote paragraphs, subtitles, or buttons, which are separate categories already internally consistent) turned up exactly five: Projectile Shape, Twist Direction, Wind Clock, and Azimuth were all bumped to 12px in the 2026-08-28 legibility pass, but BC (G7)'s hint was missed and left at 10px — the actual source of both this sizing inconsistency and the line-cramping fixed in the entry below. Bumped it to 12px to match the other four. Pure CSS tweak, no logic touched; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed.

**2026-08-29 — Loosened line spacing on the BC (G7) field's "optional" hint text (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Same underlying issue as the entry immediately below, flagged separately by the user against a second instance: the muted `<span>` reading "optional — used automatically when Drag Model is G7," next to the BC (G7) label, wraps to two lines on narrower screens and had no explicit `line-height`, so it inherited the browser's tight ~1.2 default. Added `line-height:1.4` directly on that span, consistent with the value used everywhere else in the app for small wrapped hint/status text. Checked the rest of the file for the same missing-line-height pattern on genuinely wrap-prone muted text (as opposed to short, single-line labels/buttons, which don't need it) — no other untreated instances found. Pure CSS tweak, no logic touched; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed.

**2026-08-29 — Loosened line spacing on the preset-edit status text (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback with a screenshot: the two-line wrapped status message shown while editing a custom preset ("Editing your preset "..." — Save will update it. Tap "cancel" to save changes as a new preset instead.") looked cramped. The `#presetEditStatus` div had a font-size and min-height set but no explicit `line-height`, so it fell back to the browser's tight default (~1.2) for a message that regularly wraps to two lines. Added `line-height:1.4`, matching the value already used by the app's other similarly-sized status banners (`#trueStatus`, `#verifyStatus`) rather than inventing a new figure. Pure CSS tweak, no logic or markup structure touched; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed — a readability fix, not a new capability.

**2026-08-28 — Lightened the app's muted/secondary text colour app-wide (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the legibility discussion immediately below: asked what the grey text actually was and whether it could be lightened without going all the way to white. It's a single design token, `--muted:#7a7f99`, referenced 73 times throughout the stylesheet — virtually every field label, section header, stat-card unit line, hint, and badge. Measured its actual WCAG contrast before proposing a fix rather than guessing: about 4.78:1 against the page background (`--bg`, barely clears the 4.5:1 AA minimum for normal text), dropping to about 4.26:1 against card backgrounds (`--surface`, below the AA minimum) and about 3.79:1 against the lighter panel colour used for dropdowns and stepper buttons (`--surface2`, a clear fail). Proposed `#9ba0be` — same hue, moved from roughly 54% to roughly 65% lightness — which measures about 7.3:1 / 6.5:1 / 5.8:1 against the same three backgrounds respectively, clearing AA everywhere the old colour failed, while staying clearly short of `--text`'s near-white `#e8eaf0` so the visual hierarchy between primary and secondary text is preserved rather than collapsed. Previewed with a live mockup of the Results page's headline stat cards (real card background, real value colours, both grey shades side by side) before touching any file, per the user's request to see it in context first. Approved, then applied as the single `--muted` variable change described above — cascades to all 73 usages at once, including the Export/Import/Reset buttons and field hints fixed in the entry below. Pure CSS colour change, no logic touched; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed — a visual contrast fix, not a new capability.

**2026-08-28 — Improved legibility/discoverability of Export/Import presets, override Reset links, and several field-adjacent hints (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User observation, discussed comment-only first: several instances of small text throughout the interface were both hard to read and hard to find, citing Export/Import presets as the clearest example. Audit found Export presets/Import presets were 10px muted, underlined plain `<span>`s styled via the shared `.restore-link` class — visually indistinguishable from a caption despite being two of the more consequential actions in the Presets panel — and the same problem existed independently on six "Reset" override links scattered across Reticle View, HUD, Turret Dial, and Compare (11px/10px, accent-coloured plain `<a>` tags, no chrome). Separately, four field-adjacent hints carrying real functional meaning (Wind Clock's "3=full R, 9=full L", Azimuth's "0=N 90=E 180=S", Projectile Shape's "(approx. gyro stability correction)", Twist Direction's "(for spin drift)") were 10px, a pure legibility problem since they're positioned correctly, just hard to read. Two other categories raised in the same discussion — section/field labels, and preset/Compare tiles — were deliberately left alone: the labels are already positionally prominent despite their small size, and the tiles pack real information into limited space where a blind font bump risks wrapping/truncation, a genuine density-vs-legibility trade-off better solved separately (e.g. weight/contrast rather than raw size) than folded into this pass. Fixed by adding a new `.link-btn` class — a bordered, transparent-background "ghost button" at 12px, matching the visual language `resetTruing()`'s button already established elsewhere in the app rather than inventing a new style — applied to Export presets, Import presets, and all six Reset links; deliberately scoped narrowly so it didn't touch `.restore-link`'s three other uses (Sort by calibre, Restore defaults, Undo), which weren't part of what was raised or approved. The four functional hints were bumped from 10px to 12px in place, no chrome needed since they're not interactive. Pure CSS/markup change — no logic touched, full existing regression suite re-run clean on both `CervidBallistics.html` and `CervidBallistics_GeneralRelease.html` (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed — a visual/legibility fix, not a new capability.

**2026-08-28 — Added an "Installing as an App" section to the user guide and updated its revision date (cervid-guide.html in the root folder and Personal/).** User request: a new section explaining how to add the app's web page as an icon on iOS, macOS, Android, and Windows, plus refresh the guide's stale "matches the application as of August 16, 2026" footer date. iOS steps were supplied verbatim by the user. For macOS, Android, and Windows, current exact menu paths and wording were verified via web research against official vendor documentation (Apple Support's Safari guide, Google's Chrome Help, Microsoft's Edge support pages) rather than relied on from memory, since browser UI text for this kind of feature has changed across versions — macOS Safari's Share → Add to Dock flow, Android Chrome's overflow menu → Add to Home screen → Install, and Windows Edge/Chrome's install-as-app-then-pin-to-taskbar flow (the two browsers use different menu wording, both included). New Section 13, added as its own topic per platform (iOS, macOS, Android, Windows) with numbered steps, following the existing "60-second cheat sheet" pattern for step lists elsewhere in the guide; added a matching sidebar nav link. Footer date updated to August 28, 2026. Propagated to `Personal/cervid-guide.html` via diff/patch; confirmed byte-identical. Pure documentation addition — no application code changed, so no docx update or test changes were needed.

**2026-08-26 — Enlarged the holding page's guide/feedback text and amended its wording (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the entry immediately below: the new guide-pointer and feedback lines were sized down at 11px to match the copyright notice they were added alongside, but the user wanted them more readable and the wording changed to credit "the physics engine" specifically, not just "it," for creating firing solutions. Wrapped just that new content in a `<span style="font-size:14px;">` — matching the app's base body font size — rather than restructuring the paragraph, so the copyright notice underneath stays at its original 11px unchanged. Updated in both places this markup exists (the static HTML and the identical string `setMode()` rebuilds on category switch). Pure content/style tweak, no logic changed.

**2026-08-26 — Added a guide pointer and feedback email to the empty-state holding page (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User request: on the page shown before any calculation ("Select a Quick Preset or configure your load, then tap Calculate Trajectory"), add a sentence pointing to the in-app guide, then a feedback email address, each separated by the same two-line gap already used above the copyright notice, with the copyright notice itself left in place below. Implemented as two additional lines inside the existing copyright `<p>` (matching its established `<br><br>` spacing convention rather than introducing a new element), with the feedback address wrapped in a `mailto:ballistics@cervid.net` link so it's tappable rather than plain text. This empty-state markup exists in two identical places — the static HTML on page load, and an identical string `setMode()` rebuilds whenever the shooter switches category (air/rimfire/centrefire) — both were updated identically so the added text appears consistently regardless of how the holding page is reached. Pure content/markup addition, no logic changed; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). No guide/docx updates needed — this doesn't describe a calculator feature, just adds a pointer to the guide itself.

**2026-08-26 — Combined Wind Deflection, Spin Drift, and (horizontal) Coriolis into a single windage figure in Reticle View and HUD (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to a user question about whether Sg feeds elevation/windage directly: it doesn't touch elevation, but it does drive Spin Drift, and investigation turned up that Spin Drift — and separately, the horizontal component of Coriolis — were both real corrections the app already computed, but only ever displayed as their own stat cards/table columns, never folded into the actual windage figure Reticle View and HUD hand the shooter to dial or hold (both of which read only from crosswind drift). A parallel discussion clarified Coriolis specifically splits into two physically distinct pieces: the vertical Eötvös component is already baked directly into Drop inside the trajectory integrator itself (via the effective-gravity term), so it needed no change; only the horizontal component is a separate lateral figure analogous to Spin Drift. To remove that ambiguity for the shooter, per explicit direction ("add them together... Leave the column for interest"), `computeTrajectory()` now also returns `totalWindMoa`/`totalWindMils` per row — the raw (pre-rounding) sum of drift + spin drift + horizontal Coriolis, all of which already share the same sign convention (positive = drifts/pushes right), so a plain sum is correct without any special-casing. Reticle View's `windVal` and HUD's hero Windage number now read from these combined fields instead of `driftMils`/`driftMoa` alone; the main Results table's separate Wind/Spin Drift/Coriolis columns, Compare Loads' "Wind Drift" row, and HUD's own headline-table "Wind" row are all deliberately left untouched, still crosswind-only, matching the explicit instruction to keep those visible "for interest" rather than replacing them. The Range Card's existing "wind per mph" reference figure was also deliberately left alone — it's a proportional rate specific to crosswind, and spin drift/Coriolis aren't proportional to wind speed, so folding them in would make that figure wrong rather than more complete. Extended `test_hud.js` and `test_reticle_wind_override.js` (the two tests whose hardcoded expectations were built against crosswind-only drift, and legitimately needed updating for this behavior change) to assert the new totals against an independent `computeTrajectory()` call, plus explicit checks that the combined figure actually differs from crosswind drift alone and equals the three components summed — guarding against a future regression that silently reverts to crosswind-only without any test noticing. Full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). Added a new "Combined windage in Reticle View & HUD" guide topic explaining the design (with cross-references added to the existing Spin Drift and Coriolis & Eötvös topics), plus a note in each of the Reticle View and HUD topics themselves; added a new "Total windage" paragraph to the docx's Section 9 (Spin Drift), giving the formula and explicitly noting the vertical Eötvös/Drop distinction.

**2026-08-26 — Corrected the same Miller velocity-correction overstatement in the user guide's two Sg callouts (cervid-guide.html in the root folder and Personal/).** Follow-up to the docx correction immediately below: the guide's Inputs-tab Gyroscopic Stability callout and its Advanced Topics entry both repeated the same inaccurate claim that Miller's formula "has no muzzle-velocity correction term," when Miller did publish one — the docx fix's reasoning applies here unchanged (see that entry for the verification detail). Reworded both spots to state that Miller's correction exists and is capped at its value at the speed of sound, while keeping the app's existing behaviour of not applying it, now for the accurate reason: neither the base formula nor Miller's correction were validated against 700–950fps airgun pellets. The Advanced Topics heading itself ("Sg has no velocity correction term") was renamed to "Sg doesn't apply Miller's velocity correction" so the heading doesn't repeat the same overstatement it's clarifying underneath. Pure documentation wording fix — no app behaviour changed. Propagated to `Personal/cervid-guide.html` via diff/patch; confirmed byte-identical.

**2026-08-26 — Corrected the Math Models docx's statement about Miller's gyroscopic-stability velocity correction (Cervid Ballistics - Mathematical Models and Design Methodology.docx only).** User relayed a technical challenge to §10's existing sentence, which said Miller's formula "carr[ies] no muzzle-velocity correction term." That's not accurate about Miller's own work — he did publish a correction factor, `(V/2800)^(1/3)`, and explicitly capped it at its value at the speed of sound rather than letting it shrink indefinitely into the subsonic range, a distinction the original wording didn't make. Verified via web research against the Wikipedia Miller twist rule article and secondary citations of Miller's 2005 *Precision Shooting* paper before editing: confirmed the 2800 fps reference velocity, the existence and exact form of the correction factor, and the Mach-1 floor are all independently corroborated (the floor claim rests on secondary paraphrase rather than a directly-quoted primary sentence, since the original PDF didn't extract cleanly — flagged as such rather than overstated). Rewrote the paragraph to state what Miller actually provided, while keeping the app's existing decision not to apply the correction — and sharpened *why*: not because Miller never wrote a correction down, but because neither his base formula nor his correction were derived from or validated against 700–950 fps diabolo/slug-shaped airgun projectiles, so adding it would swap one unvalidated number for another rather than demonstrably improve the estimate. No app code changed — `millerSg()` already omits the velocity term entirely, consistent with both the old and new wording; this was a documentation-accuracy fix, not a behavior change. Verified the edit with the skill's `validate.py` (paragraph count and XML unchanged elsewhere) and by rendering to PDF and reading back the affected page. No changes needed to the app itself, the guide, or the changelog's other entries.

**2026-08-25 — Raised every form-control font-size to 16px, fixing iOS Safari getting stuck zoomed in after focusing a field (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User report: on iPhone, the screen zooms in when a field is tapped, and pressing Enter/Done to dismiss the keyboard doesn't zoom it back out. Diagnosed comment-only first. The viewport meta tag already had no `maximum-scale`/`user-scalable=no` (correct — locking scale is an accessibility anti-pattern), so the zoom-in trigger was purely CSS: iOS Safari auto-zooms any focused field whose computed font-size is under 16px, on the assumption it isn't legibly tappable at 1.0 scale, and a scan of the app found virtually every `<input>`/`<select>` sized well under that threshold (13px on the Inputs-tab workhorse rule, 12px on the Compare/Reticle/HUD range-wind-velocity fields and the preset-name field, 11-12px on several dropdowns and the Compare per-slot velocity input). The "doesn't zoom back out" half is a separate, long-documented WebKit quirk — the reset-on-blur is unreliable specifically when the keyboard is dismissed via the accessory "Done" button rather than a tap elsewhere on the page, which is exactly how `type="number"` fields (the vast majority in this app) get dismissed, since their keypad has no Return key. Two workaround options were considered and set aside: a `transform: scale()` trick to keep fields visually small while their computed size stays ≥16px, and a JS hack that briefly toggles the viewport meta tag's `content` on blur to force a re-snap — both fight the browser rather than removing the trigger, and the JS toggle's reliability has shifted across iOS releases. Went with the durable fix instead: raise every real form control to 16px so the zoom never triggers at all. Scoped out first to confirm it wasn't the sprawling pass it looked like — the app already funnels nearly all its fields through three shared CSS rules (`.field input,.field select`, 13→16px, covering roughly 31 elements across the whole Inputs tab; `.compare-range-label input`, 12→16px, covering the 7 Compare/Reticle/HUD range/wind/velocity fields, width widened 70→82px so a 4-digit velocity stays comfortable at the larger size; `.save-preset-row input`, 12→16px, covering the preset-name field), plus six inline-styled elements needing individual edits (five small pill-style unit/click-size `<select>`s at 12→16px, and Compare's per-slot velocity input at 11→16px, widened 46→58px to fit larger digits inside its table cell). Pure CSS — no JS, physics, or markup structure changed, and no test asserts a pixel font-size, so the full existing regression suite (34 test files, excluding the two pre-existing, unrelated failures already documented above) was re-run clean on both `CervidBallistics.html` and `CervidBallistics_GeneralRelease.html` as a sanity check rather than as a targeted test of this change. No guide or docx updates — a rendering/accessibility fix, not a new capability.

**2026-08-25 — Replaced the native number-input spin arrows on Compare's per-slot velocity field and HUD's velocity field with custom, more visible +/- stepper buttons (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User observation (with a screenshot of Compare's field): the up/down arrows on the velocity fields were subtle and hard to see. Diagnosis, confirmed by checking the code: neither field had any custom arrow control at all — both relied entirely on the browser's own default number-input spin buttons, and no CSS anywhere in the app touches `::-webkit-inner-spin-button` or similar. That understates the actual problem on this app's primary target platform: Safari, on both macOS and iOS, doesn't render any native spin arrows on a plain number input by default, so on the browsers this app is explicitly built for the control wasn't just faint, it was likely entirely absent — only Chrome (where the screenshot was presumably taken) shows anything at all. Rather than trying to style the native spinner into visibility (unreliable across browsers, and simply unavailable on Safari regardless of styling), both fields were given custom square +/- buttons reusing the exact pattern Reticle View's Turret Dial already established (`stepTurretInput()`) — plain HTML buttons with a visible border and high contrast against the dark theme, so they render identically everywhere rather than depending on native input chrome. HUD's button matches the Turret Dial's own 26px sizing, since it has the same amount of room; Compare's shrinks to 18px to fit inside its tighter 46px-wide table-header-cell input, a new `stepCompareVelocity()`/`stepHudVelocity()` pair sharing a common `velocityStepFps()` helper for the step size so the two controls can't quietly drift out of sync with each other. Both step by a coarser 10fps/5m/s per tap rather than the fields' own `step="1"`, since this control exists to nudge a velocity meaningfully (testing a chronograph reading against the book value) rather than fine-tune it one fps at a time — typing remains available for an exact figure. Compare's stepper is button-gated exactly like typing into the field already is (it updates the field and the override state without recomputing; Compare itself has to be pressed again to see the effect), while HUD's recomputes immediately, matching how every other control on that page already behaves. Deliberately scoped to velocity only for both pages: Wind already has the drag dial as its primary input on both, and HUD's Distance field is free-typed, where a coarse step button doesn't map as naturally onto "the exact distance I just ranged." Extended `test_hud.js` (to 45 checks) and `test_compare_velocity_override.js` (to 31 checks) with stepper coverage on both unit systems; full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above). Added a note to the guide's existing HUD and Compare Loads velocity-override topics. No docx changes — the per-slot velocity override's existing docx section already covers the control conceptually; this is a rendering fix to it, not a new capability.

**2026-08-25 — Added a HUD (Head Up Display) view (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User request: a new button on the Results page opening a page where the shooter types a single distance to target and gets one large-type ballistic solution — elevation and windage in mils/MOA/clicks — plus a headline stat table (Muzzle Energy, Velocity at Target, Energy at Target, Drop, Wind, Air Density). Discussed comment-only first to place it against the app's existing "single glance" views: Range Card (every distance at once, large print) and Reticle View (one distance, but graphical and pattern-specific). HUD fills a real gap between them — one distance, numeric only, biggest type in the app — closer to a rangefinder's built-in ballistic overlay than to anything already here, not a duplicate of either. Confirmed scope: full wind-dial + velocity override controls (mirroring Reticle View's own, but in their own independent hudWind*/hudVelocity* state — a third page a shooter could have open with a third hypothetical condition in mind, same reasoning that already keeps Compare's and Reticle's copies apart), an angular-only unit dropdown (mils/MOA/clicks, deliberately no linear inches/cm — a HUD reading is something you dial or hold, not a physical measurement to note down), live recompute on every field change (matching Reticle View's behaviour, not Compare's button-gated one, since this view is explicitly for a real-time "what do I dial right now" read), and a headline table whose Drop/Wind rows deliberately show the same physical-distance figures (inches/cm) the main Results page's own stat cards already use — a genuine complement to the angular hero numbers above rather than a restatement of them.

The one new technical wrinkle, not present in any prior override: the distance field is free-typed (any ranged distance), not picked from a `<select>` of already-tabulated marks the way Reticle View's range field is. Getting an exact solution at an arbitrary yardage reuses Compare's own trick of setting `maxYd` to the target distance and reading back a dense-sampled row — but with one correction Compare doesn't need: `computeTrajectory()`'s zeroing pre-pass always walks out far enough to reach the zero point regardless of `maxYd`, but the *main* trajectory loop (the one that actually produces the returned rows and the zero-referenced line-of-sight baseline) stops at `maxYd*3ft`. If a HUD target is shorter than the loaded zero — a real case: zeroed at 100yd, ranging something at 40yd — setting `maxYd` to 40 directly would mean the zero point never gets sampled, and the LOS baseline (`zLOS`) would silently fall back to an assumed height at the zero range rather than the trajectory's actual one there. Confirmed this isn't just a theoretical concern: at a level shot (angle=0) the fallback happens to coincide with the correct baseline by coincidence, but at any non-zero shooting angle the two diverge — exactly the kind of quiet, hard-to-notice disagreement the project has treated as a hard rule to avoid ever since the isLive-branch bug class first turned up in Compare's wind override. Fixed by always padding `maxYd` out to at least `zeroYd+20` (same guard already used for Compare's isLive branch, same underlying reason) before running the dense recompute, then picking the specific row matching the typed distance out of the full set — rather than assuming, as Compare correctly does for its own reference-range use case, that the last row is the one wanted.

New `test_hud.js` (41 checks): auto-suggest from `lastResults.params` rather than the live form, independence from both Compare's and Reticle's own override state, a genuine recompute verified against an independent `computeTrajectory()` call, the zero-padding fix specifically — proven with a non-zero shooting angle, since the level-shot case would have passed even with the padding removed, and an explicit check that the padded and naive (unpadded) calculations actually differ so the test can't silently stop testing anything — the headline stat table's six rows including Air Density matching the same helper the main Results page's own stat card uses, the auto-suggest/reset lifecycle for both overrides, the angular-only unit dropdown with its conditional click-size selector, and the metric/imperial toggle across all three of its own fields. Passes on all four files; full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above). Added a matching guide topic. No docx changes — consistent with every prior Reticle-adjacent feature, the docx has never covered Reticle View or its siblings.

**2026-08-21 — Added a per-slot velocity override to Compare Loads (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User request, alongside the Reticle View velocity override below: could velocity be adjusted the same way wind and distance already can be? For Compare specifically, the user deliberately chose a per-slot override over a single shared one ("a separate velocity override per selected slot, so you could nudge just one load independently") — unlike Wind/Compare-to, which apply identically to every selected load, a shooter comparing three rounds may want to test a chronograph reading against one load's book value without disturbing the other two. New `compareVelocityOverrides` state object is keyed by `compareRefKey(ref)` ("mode:type:idx"), the slot's ref identity rather than its column position, so an override stays attached to the load it was set on even if another slot is toggled off and on around it and the columns reshuffle. The control itself lives in each column's own `<th>` in the results table (a small velocity input plus unit label, with a Reset link that appears only once that column has an override) rather than in the shared controls above the picker, since it's genuinely per-column state. `buildParamsFromPreset()` gained a 4th parameter, `forcedMvFps`, changing `mvFps:p.mv` to `mvFps:forcedMvFps||p.mv` — mirroring exactly how `forcedMaxYd` already overrides the comparison distance. The live-matched slot in `runCompare()` bypasses that function (it clones `lastResults.params` directly), so it needed the same re-injection treatment already applied there for the wind override: `mvFps:compareVelocityOverrides[compareRefKey(x.ref)]||lastResults.params.mvFps` — without this, the currently-loaded preset's slot would silently ignore its own override while every other slot correctly picked it up, the same isLive-branch bug class caught during the wind-override work. The input itself is button-gated like Range/Wind (typing a value only takes effect on the next Compare press, storing into the override object without recomputing), but the per-slot Reset link takes effect immediately, deleting that slot's entry and re-running the comparison right away — inconsistent immediacy would otherwise leave "Reset" looking like it did nothing until Compare was pressed again. A deselected slot's override is discarded in `toggleCompareSelect()`'s removal branch (tied to that specific load, not to "whichever load ends up in this column next"), and the whole `compareVelocityOverrides` object is cleared when the selection empties out entirely, alongside the existing Range/Wind latch resets. New `test_compare_velocity_override.js` (27 checks: independence between slots, both the non-live and live-matched param-building paths, reset-on-deselect and full-clear-on-empty-selection, the immediate-effect Reset link versus the button-gated input, and the per-column `<th>` markup itself) passes on all four files; full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above; also fixed one stale pre-existing assertion in `test_compare_loads.js` that predated the wind-override feature and no longer matched buildParamsFromPreset's architecture — it now drives `updateCompareWindSuggestion()` explicitly, the same sync point the app itself uses). Added a matching guide topic and a new docx section (Part 3, Section 6, "Per-Slot Velocity Override" — existing Sections 6–7 renumbered to 7–8 to make room).

**2026-08-21 — Added a velocity override to Reticle View (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User request: could velocity be adjusted alongside range and wind on both the Reticle and Compare pages? For Reticle View, agreed as a straightforward analogue of the existing wind override — a "Velocity" field sits between the Wind row and the Reticle Pattern selector, following the exact same auto-suggest/customize/reset pattern (`reticleVelocityCustomized`/`fReticleVelocity`, `readReticleVelocityFps()`/`writeReticleVelocityFps()`/`updateReticleVelocitySuggestion()`) already established for the wind override on this same page. `renderReticleView()`'s existing full-recompute — already required for wind, since even the headwind/tailwind component of any wind vector affects drop and time-of-flight, not just the crosswind component reflected in drift — now also folds in the velocity override: `effParams` gains `mvFps:reticleVelocityFps`, where `reticleVelocityFps=readReticleVelocityFps()||lastResults.params.mvFps` (an invalid/blank field falls back to the slot's own velocity rather than a nonsensical 0fps, unlike wind's 0-means-no-wind convention). Velocity has no separate visual indicator in the reticle SVG the way wind has its arrow, so `buildReticleSvg()`'s call site is unaffected — only the underlying hold-point math changes. Auto-suggests from `lastResults.params.mvFps` (not the live, possibly-since-edited Inputs-tab fMV field) for the same reason the wind override does, and resets on the next Calculate(), the same natural boundary. New `test_reticle_velocity_override.js` (16 checks: auto-suggest sourced from lastResults not the live form, a genuine recompute verified against an independent computeTrajectory() call under the overridden mvFps, immediate recompute on field input, Reset behavior, the blank-falls-back-to-lastResults-mv case, and the metric/imperial fps↔m/s toggle) passes on all four files; full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above). Added a matching guide topic. No docx changes — consistent with every prior Reticle-only feature, the docx has never covered Reticle View at all.

**2026-08-17 — Omitted the Reticle View wind arrow's "PUSH" caption for a pure head/tailwind (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User observation, agreed after discussion: at 12 or 6 o'clock the arrow still points straight up or down, but "PUSH" was still drawn next to it even though there's no sideways push at those positions to describe — the trailing R/L letter on the speed line was already correctly omitted there for exactly this reason, so the caption text disagreed with its own neighbouring label. Fixed by gating the "PUSH" text on the same `Math.abs(wdx)>0.05` threshold the R/L letter already uses, so both now agree on whether there's a meaningful crosswind component to report. When omitted, the speed label moves up into the caption's own vertical slot rather than leaving a blank gap above it. Extended `test_reticle_wind_indicator.js` with checks that "PUSH" is present at every clock position except 12 and 6, and reworked the existing direction-aware clearance loop (12 positions) to check whichever text is now topmost — "PUSH" where present, the speed label alone at 12/6 — clears the arrowhead by the same 5px margin as before (53/53 passing on all four files); full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above).

**2026-08-17 — Added a wind speed+direction override to Reticle View (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User request: could the same 72px drag-anywhere wind dial just added to Compare Loads be applied to Reticle View too, between the Range and Reticle Pattern fields? Design was discussed comment-only first, since the two pages are architecturally different in a way that mattered: Compare reruns computeTrajectory() from scratch on demand, but Reticle View normally just reads one already-computed row out of the frozen lastResults.results, so a dial that only changed a display label wouldn't do anything real — it had to actually feed back into the physics. A cheap linear rescale of the cached drift figure was considered and rejected: the Wind Clock field's own help-tip already documents that the headwind/tailwind component of any wind vector (not just the crosswind component reflected in drift) has a small effect on drop and time-of-flight too, larger for airgun pellets specifically, so a rescale would silently disagree with what Compare or the Results table would show for the same override wind — the same class of quiet inconsistency the isLive-branch fix (immediately prior entry) had just caught elsewhere. renderReticleView() now clones lastResults.params, overrides windMph/windClock from the page's own controls (readReticleWindMph()/reticleWindClockVal, independent state from Compare's own compareWind* variables so the two pages can't leak into each other), and runs a fresh computeTrajectory() before pulling the row for the selected range — reproducing lastResults.results exactly when the override hasn't been touched, so no special-casing was needed for the common case. The existing read-only wind arrow drawn inside the reticle SVG is now sourced from the same override values rather than lastResults.params directly, so it can never show a different "current wind" than the input dial above it. Because this page (unlike Compare) has no separate "Apply" button and everything else on it already re-renders live, the dial's own ticks/hand redraw on every pointermove (cheap, pure CSS) but the expensive trajectory recompute is deferred to pointerup/pointercancel, avoiding hammering computeTrajectory() dozens of times a second during a fast drag; the speed field's oninput recomputes immediately since typing a number is low-frequency, matching how the Turret Dial's own inputs already behave. Auto-suggest sources from lastResults.params (not the live, possibly-since-edited Inputs-tab fields) since everything else on this page is already frozen from that same source of truth, and the override latch resets automatically on the next Calculate() — a fresh calculation is this feature's natural "start over" boundary, mirroring the role clearing Compare's selection plays for its own latch. A Reset link reuses the exact placement and styling of the Turret Dial's own Reset link immediately below it. New test_reticle_wind_override.js (20 checks: auto-suggest sourced from lastResults not the live form, a genuine recompute verified against an independent computeTrajectory() call rather than just "some different number," the wind arrow tracking the override, immediate recompute on speed-field input, Reset behavior, and the metric/imperial toggle) passes on all four files; full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above). Added a matching guide topic. No docx changes — the docx has never covered Reticle View at all (zero mentions, consistent with every prior Reticle feature), so nothing to update there either.

**2026-08-17 — Added a wind speed+direction override to Compare Loads (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** User request: could the wind used for a comparison be adjusted the same way the comparison distance already can be? Design was discussed and mocked up (comment-only) before coding: two fields (speed + direction) rather than one, following the exact same auto-suggest/customize/reset pattern already established by "Compare to" (compareDistanceCustomized/fCompareRange) — both wind controls default to the live Inputs-tab Wind Speed/Wind Clock fields and keep re-mirroring them until touched, at which point a single shared latch (compareWindCustomized) freezes both together, resetting only when the selection clears to zero. Direction uses a 72px drag-anywhere clock-face dial rather than twelve discrete tap targets, after a real-estate mockup showed 12 buttons on any inline-sized dial would each cover only ~11px of circumference — well under a reliable mobile touch target — versus a whole-ring drag/tap surface (clockHourFromAngle(), via atan2, snapping to the nearest hour) that doesn't depend on tap precision; a follow-up request ("agreed, 72px drag-anywhere ring") confirmed the direction before implementation. Wiring the override into buildParamsFromPreset() (replacing its previous live-fWind/fWindDir reads with readCompareWindMph()/compareWindClockVal) covers every non-live compared slot, but the live-matched slot in runCompare() bypasses that function entirely — it clones lastResults.params directly — so it needed windMph/windClock re-injected into that clone explicitly too, alongside the existing maxYd override; without this the currently-loaded preset (the one most likely to already be on screen) would have silently ignored the override while every other slot correctly picked it up, a bug caught during testing rather than left in. New test_compare_wind_override.js (31 checks: dial geometry, unit conversion, auto-suggest/latch/reset lifecycle, both the non-live and live-matched param-building paths, and the metric/imperial toggle) passes on all four files; full existing regression suite re-run clean on all four (excluding the two pre-existing, unrelated failures already documented above). Added a matching guide topic ("Overriding the wind for a comparison") and a new docx section (Part 3, Section 5, "Wind Speed and Direction Override" — existing Sections 5–6 renumbered to 6–7 to make room), covering the auto-suggest/latch pattern, the touch-target rationale for the drag-ring, and the live-slot correctness fix.

**2026-08-17 — Increased the Reticle View arrow-to-"PUSH"-caption clearance by 5px (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback: after the direction-aware fix above, the arrow was reported as sitting close to touching the text. The per-direction margin (`pushY=maxArrowY+6`) was increased to `maxArrowY+11`, keeping the speed label's fixed 13px offset below the "PUSH" caption unchanged. Pure layout tweak, no guide/docx updates needed. Re-ran `test_reticle_wind_indicator.js` (39/39 passing on all four files, including the 12-clock-position clearance check from the previous entry).

**2026-08-17 — Made the Reticle View "PUSH" caption's vertical position direction-aware, fixing an overlap with the arrowhead on down-pointing wind (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The three successive 5px gap reductions above used a single fixed `hubY+armLen`-based offset for the "PUSH"/speed-label text regardless of arrow direction. That offset was originally sized for the worst case (a straight-down arrow, where the tip reaches the full `armLen` below the hub), so as it was tightened it left an oversized gap for horizontal and upward arrows — until the third reduction, which finally checked as safe for those directions but placed the text on top of the arrowhead for a straight-down (6 o'clock) wind: confirmed by rendering that case and finding the shaft (y=44 to 72) running straight through the text's x-position and the "PUSH" baseline (y=66) landing inside the arrowhead polygon's y-range (64 to 72). Fixed by computing the text's anchor per-direction rather than from a single fixed formula: `maxArrowY=Math.max(tipY,leftY,rightY,hubY+2.5)` finds the lowest point actually reached by the shaft tip, either arrowhead base corner, or the hub circle for that specific clock direction, and the "PUSH" text is anchored 6px below that (`pushY=maxArrowY+6`), with the speed label a further fixed 13px below (unchanged from before). This gives near-horizontal and upward arrows the tight ~6px gap that was wanted, while a straight-down arrow's text automatically drops far enough to clear its own head, and every direction in between scales smoothly. Pure layout fix, no guide/docx updates needed. Extended `test_reticle_wind_indicator.js` with a check across all 12 clock positions verifying the "PUSH" text clears the arrowhead's lowest point by at least 5px (39/39 passing on all four files); full existing regression suite re-run clean on all four files (excluding the two pre-existing, unrelated failures already documented above).

**2026-08-17 — Tightened the gap between the Reticle View wind arrow and its "PUSH" caption a third time (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** A further 5px reduction, from `armLen-1`/`armLen+12` to `armLen-6`/`armLen+7`, again preserving the 13px PUSH-to-speed-label spacing. Pure layout tweak, no guide/docx updates needed. Re-ran `test_reticle_wind_indicator.js` (27/27 passing on all four files).

**2026-08-17 — Tightened the gap between the Reticle View wind arrow and its "PUSH" caption a second time (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Further to the previous 5px reduction, the offset was reduced by another 5px, from `armLen+4`/`armLen+17` to `armLen-1`/`armLen+12`, again preserving the 13px spacing between the "PUSH" caption and the speed label beneath it. Pure layout tweak, no guide/docx updates needed. Re-ran `test_reticle_wind_indicator.js` (27/27 passing on all four files).

**2026-08-17 — Tightened the gap between the Reticle View wind arrow and its "PUSH" caption (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback: the arrow and the "PUSH" text sat too far apart. Both text lines are positioned relative to `armLen` rather than the arrow's actual tip (which varies with direction), so the fix reduced the shared offset from `armLen+9`/`armLen+22` to `armLen+4`/`armLen+17` — a 5px tighter gap under the arrow, with the 13px spacing between the "PUSH" caption and the speed label underneath it left unchanged. Pure layout tweak, no guide/docx updates needed. Re-ran `test_reticle_wind_indicator.js` (27/27 passing on all four files) — none of its checks assert absolute text position, so no test changes were required.

**2026-08-17 — Replaced the Reticle View wind indicator's chevron arrowhead with a filled triangle and lengthened/thinned the shaft (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback: the arrow was too thick and would look better longer, and the arrowhead's lines didn't align. Diagnosis: the head was drawn as two separate open `<line>` chevron strokes meeting the shaft at a point; with the default flat "butt" line cap, three line segments converging at nearly the same point at a stroke width close to the head's own length never line up cleanly — each segment's blunt edge shows through at a slightly different angle, reading as a jagged or misaligned tip rather than a sharp point. This was a rendering artifact of the construction, not a geometry error — the underlying direction math was already correct. Fix, per the user's explicit choice of "filled-triangle head plus the longer/thinner shaft": the two chevron strokes were replaced with a single filled `<polygon>` (tip plus two base corners computed from a perpendicular unit vector, headLen=8, headWidth=3.2) — a filled shape has no seam to misalign, the same construction most icon arrows use. The shaft grew from 20px/2.6px-stroke to 28px/1.6px-stroke and gained a rounded line cap, for a longer, more refined pointer rather than a short, thick stub; the hub circle's radius was reduced from 3 to 2.5 to match. The "PUSH" caption and speed-label text positions are expressed relative to `armLen`, so they shifted down automatically with no separate formula change. Purely a rendering change — no wind-arrow direction, correction math, or on-screen text changed, so no guide/docx updates were needed (consistent with the earlier reticle dot-sizing fix, which also needed none). Extended `test_reticle_wind_indicator.js` with five new checks (single filled polygon present, exactly one grey shaft line remaining with no leftover chevron strokes, rounded line cap present, 1.6px stroke width, ~28px shaft length) — all 27 checks pass on all four files, and the full existing regression suite (excluding the two pre-existing, unrelated failures already documented above) ran clean on all four files.

**2026-08-17 — Disambiguated the Reticle View wind arrow with a "PUSH" caption and a crosswind-side R/L letter (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User question, raised as discussion before any code: could the wind arrow be misread, since a bare directional arrow is genuinely ambiguous between "wind is coming from here" (the meteorological/weathervane convention) and "wind is blowing toward here" (a common instinctive reading), with different people defaulting to opposite interpretations? The arrow's own direction was already internally consistent — it's drawn to agree with the app's own effect, matching "3=full R" on the Wind Clock field with a rightward-pointing arrow at clock=3 — but nothing on screen stated which of the two conventions it represented, leaving room for a reader to import the wrong one. Resolved by relabelling the indicator's meaning rather than changing the arrow's geometry: a small "PUSH" caption (8px, bold, letter-spaced, drawn above the speed line) reframes it explicitly as showing which way the correction pushes point of impact — sidestepping the source-vs-destination question entirely, since it was never claiming to be either. The speed label also gained a trailing crosswind-side letter (e.g. "10mph R"), giving the same directional information a second, non-graphical way that doesn't require judging the arrow's exact angle; derived from the sign of the same `wdx` value already driving the arrow's horizontal component (positive → R, negative → L, matching "positive drift = right" used throughout the app), and correctly omitted — no letter, not even a stray space — for a pure head/tailwind at 12 or 6 o'clock, where there's no crosswind component to report a side for. A separate proposal (moving the whole indicator to the opposite corner depending on correction direction) was discussed but not implemented: it would only capture the coarse crosswind sign and flatten the finer angle information the arrow already carries, and would need the top-left corner's collision-safety guarantee (verified clear of all three patterns' marks) independently re-verified for a second corner, so the cheaper R/L-letter approach was chosen instead. Extended `test_reticle_wind_indicator.js` to assert the "PUSH" caption is present whenever the arrow is drawn, that the speed label carries the correct R/L suffix at clock 3/9 and no suffix at all at clock 12/6 (pure head/tailwind), and that both unit systems still append the suffix correctly — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Extended the guide's Reticle View topic to explain the "PUSH" wording and the R/L letter; rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`.

**2026-08-17 — Enlarged the Reticle View wind indicator (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Immediate follow-up to the wind indicator added earlier the same day — user feedback that the arrow and speed label were too small and easy to miss at a glance. Scaled every dimension of the indicator up: `armLen` (arrow shaft length) 13→20, `headLen` (arrowhead) 5→7, stroke width 1.8→2.6, hub radius 2→3, font size 9→12 with `font-weight="600"` added (previously unweighted). The hub anchor moved from (38,38) to (44,44) and the text's vertical offset grew to match the longer arm, so the whole cluster stays proportionally spaced rather than the larger arrow crowding into a still-small label. Colour (`#7a7f99`, matching the reticle's other muted tick labels) is unchanged — this was purely a size pass, not a re-think of the design. Updated `test_reticle_wind_indicator.js`'s hub-anchor assertion to match the new (44,44) position; the direction and unit-label assertions needed no changes since they check relative movement and text content, not absolute pixel values. Run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Purely a sizing change — no guide/docx updates needed.

**2026-08-17 — Added a wind direction/speed indicator to Reticle View (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** User suggestion: could the wind conditions be shown as part of the reticle rendering itself, not just implicit in where the aim-point marker sits? The data was already available at render time (`lastResults.params.windMph`/`.windClock`, populated by `calculate()`), so this was purely a drawing addition. `buildReticleSvg()` gained three new parameters (`windMph`, `windClock`, `met`) and now draws a small compass-style arrow in the reticle's top-left corner — chosen because it sits well outside `frameR` on all three patterns, so it can't collide with the Christmas Tree's diagonal number labels or the MOA Hash's major-tick numbers regardless of range/values. The arrow's rotation uses the exact same "3=full R, 9=full L" analog-clock convention the Wind Clock input field already states in its own on-screen hint (12=up, each hour +30° clockwise) — deliberately matching that existing mental model rather than inventing a second one, so a user who already knows how to read that field reads the arrow the same way. The speed label follows the metric/imperial toggle the same way the Range Card's wind reference does (`met?(windMph*1.60934).toFixed(1)+'km/h':windMph+'mph'`, mirroring the existing `windSpeedDisp` pattern used in the results stat card). At 0mph the arrow is replaced with a plain "Calm" label rather than drawing a directionless arrow. Drawn in the same muted grey (`#7a7f99`) used for tick labels elsewhere on the reticle, deliberately not the orange used for the aim-point marker, so it reads as background context rather than a second target — this is a static readout of the condition the drawn hold already assumes, not an interactive control. `windMph` is an optional parameter; when omitted entirely (`buildReticleSvg()` called without wind context) the whole indicator is skipped rather than rendering a broken zero-state. Verified with a new dedicated test (`test_reticle_wind_indicator.js`) that calls `buildReticleSvg()` directly with known clock values and asserts the arrow's endpoint moves in the correct screen direction for 3/6/9/12 o'clock (right/down/left/up respectively, matching the field's stated convention), that the speed label reads correctly in both unit systems, that 0mph shows "Calm" with no arrow drawn, that omitting `windMph` entirely skips the indicator without throwing, and that all three patterns render the indicator at the same anchor point without error — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Extended the guide's Reticle View topic to describe the indicator and its conventions; rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`. No docx change — the Math Models document doesn't currently describe Reticle View in any detail, so there was nothing to reconcile.

**2026-08-17 — Refined Reticle View dot sizing on the Mil-Dot and Christmas Tree patterns (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User feedback: the dots on the Mil-Dot and Christmas Tree reticle patterns looked too large. Root cause was `buildReticleSvg()` using fixed pixel radii (3.5 for Mil-Dot's main-axis dots, 2.2 for the Christmas Tree's wind-hold dots) regardless of how far apart the marks they sit between actually are on screen. This was worst on the Christmas Tree, whose wind-hold dot columns sit only 1 mil apart (`pxPerUnit` ≈ 9.8px at that pattern's span of 12) — a fixed 2.2px radius (4.4px diameter) against that spacing worked out to roughly 45% of the gap between adjacent dots, well past the ~20% ratio real mil-dot reticle specs (e.g. USMC mildot) typically use, so the dots visually crowded into each other on the tree's wider rows. Replaced both fixed radii with a single derived value, `dotR = Math.max(1.2, pxPerUnit*0.1)`, computed once per pattern and reused everywhere a mark-dot circle is drawn: this keeps every dot's diameter at a consistent ~20% of the spacing between marks regardless of a pattern's span, rather than a number hand-tuned to look right on one pattern and wrong on another. Concretely: Mil-Dot's dots shrink from a fixed r=3.5 to r≈2.35 (pxPerUnit≈23.5 at span 5); the Christmas Tree's shrink from r=2.2 to r=1.2, hitting the floor since its raw formula value (0.98) would otherwise render as nearly invisible. The 1.2 floor exists purely so a future pattern with a much larger span doesn't shrink its dots into nothing. MOA Hash (which uses tick lines, not dots) and the off-reticle aim-point marker (a fixed-size element unrelated to the mark-dot spacing) are both untouched by this change. Verified with a new dedicated test (`test_reticle_dot_size.js`) that parses the generated SVG's `<circle>` elements and asserts: Mil-Dot renders exactly 20 dots (4 marks/axis × 2 axes × 2 directions) at the expected 2.352px radius; the Christmas Tree's wind-hold dots render at the expected 1.2px floor value; MOA Hash's circle count is unchanged (frame + aim-marker only, no mark-dot circles); and the frame and aim-point marker radii are untouched — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Purely a rendering change — no holdover positioning, math, or data changed, so no guide/docx updates were needed.

**2026-08-17 — Reworked the Range Card wind reference: explicit "(per mph)"/"(per kph)" label, and metric-mode readings now denominated in kph instead of always mph (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Two follow-up considerations from the user on the wind-per-mph feature shipped earlier the same day. First: the reference figure should say "wind (mph)" or similar for clarity, rather than relying on the reader to infer the unit from a trailing fraction. On discussion, settled on something more precise than a bare "(mph)" label — a plain unit suffix would read like a wind-speed value ("wind is 0.28mph") rather than a rate, so the label now spells out "Wind (per mph)" / "Wind (per kph)" explicitly. Second: since the app's wind speed field displays in kph when the page is set to metric (though it's always stored internally as mph via `params.windMph`, per the app's canonical-imperial-internally convention), the user asked whether the Range Card reference should follow suit rather than always being denominated in mph — a fair point, since every other Range Card value (range in m vs yd, drop in cm vs in) already follows the metric/imperial toggle. `windPerMph(row, windMph, met)` gained a third `met` parameter: when true, the rate's denominator converts to kph (`windMph*1.60934`) and the returned label switches to "Wind (per kph)"; the numerator (which unit the correction itself is shown in — mils/MOA/clicks/in/cm) remains governed independently by `unitDrop`, unchanged from before. The function's return value changed from a bare unit-suffixed string (e.g. "0.28 mil/mph") to a complete, self-contained label (e.g. "Wind (per mph): 0.28 mil"), simplifying both call sites — `showRangeCard()`'s `.rc-wind` div and `buildRangeCardText()`'s plain-text line now just embed the returned string directly rather than appending a trailing " wind" themselves. Rewrote `test_range_card_wind_per_mph.js` to assert the new label format and decimal/whole-number rounding rules hold under both the mph and kph denominators, that a kph-denominated rate is correctly smaller than the equivalent mph-denominated rate for the same row (1kph being a smaller wind speed than 1mph), that `showRangeCard()` switches its rendered label between "Wind (per mph)" and "Wind (per kph)" when the metric/imperial toggle changes, and that the zero-wind `null` guard holds regardless of the `met` flag — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Updated the guide's Range Card topic and the Math Models docx paragraph to describe both the explicit label and the metric-aware kph denominator; rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`, and re-rendered the docx to PDF to verify.

**2026-08-17 — Documentation audit: fixed a stale unit list in the guide's Coriolis tip (cervid-guide.html in the root folder and Personal/).** Follow-up to the Range Card wind-per-mph entry below — cross-checked the guide and Math Models docx against the last five shipped features (wind-per-mph, Compare chart axis label, click-size-freezing fix, Compare clicks dropdown, Results table clicks support) to confirm nothing had gone stale. Four of five needed no changes (already covered, or the changelog's own "no doc changes needed" rationale still held). Found one small inconsistency: the Coriolis column's tip callout listed its supported display units as "mils, MOA, inches, or cm," omitting clicks — correct when originally written, but left stale after clicks support was added to that column. Corrected the list to include clicks, matching the equivalent (and already-correct) unit list elsewhere in the same guide section.

**2026-08-17 — Added a wind-per-mph reference figure to the Range Card (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Prompted by the user asking whether wind should be added to the Range Card at all, or whether it was too much of a variable to print reliably. Elevation holdover is deterministic for a given rifle/load/zero and safe to print as a single static number, but wind correction depends on the single wind-speed/direction input (`fWind`/`fWindDir`) entered before Calculate — one static guess standing in for real field wind, which varies continuously — so printing a single baked-in wind-corrected number risked implying a precision the input never had. After sketching a mockup of the concept (via the visualization tool, not committed to the app) and getting it approved, implemented a `windPerMph(row, windMph)` helper that divides a row's drift back down to a per-1mph rate — matching how printed dope/wind cards traditionally present this — in whichever unit the holdover value itself is already shown in (mils, MOA, clicks, in, or cm), reusing the same `unitDrop`-switching convention as `dispWind()`/`cuWind()` elsewhere in the app. Per user direction, angular units (mils, MOA) are shown as decimals, matching how drift is displayed everywhere else, while clicks are rounded to a whole number, matching how `toClicks()` rounds everywhere else in the app (clicks being inherently discrete). The reference is omitted entirely when the calculation used 0mph wind, since there is no rate to derive (`windPerMph()` returns `null`). Wired into both `showRangeCard()` (a new `.rc-wind` line stacked under the existing `.rc-val` inside a new `.rc-valwrap` flex column, with `.rc-row`'s `align-items` changed from `baseline` to `center` to accommodate the two-line value) and `buildRangeCardText()` (appended in parentheses on the same line, e.g. "500yd ◆: 3.71 mil (0.28 mil/mph wind)"), so the plain-text Share export used for the Notes/Reminders/Shortcuts → Apple Watch path picks up the addition automatically with no separate handling needed. Made both call sites defensively guard against a missing `params` object (`r.params&&r.params.windMph`) after an existing test (`test_share_range_card.js`) that fabricates a `lastResults` fixture without a `params` field was found to throw — the real app always populates `params` via `calculate()`, but the guard costs nothing and keeps `buildRangeCardText()` resilient to any future caller that constructs `lastResults` incompletely. Verified with a new dedicated test (`test_range_card_wind_per_mph.js`) covering: decimal output and correct division for mils/MOA/in/cm; whole-number rounding for clicks; the `null` return for 0mph wind; that `showRangeCard()` renders the new `.rc-wind`/`.rc-valwrap` markup; that `buildRangeCardText()`'s plain-text export includes the wind reference; and that `shareRangeCard()` still runs without throwing — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected). Extended the guide's Range Card topic to explain the new reference figure and its rationale, and added a new paragraph to the Math Models document describing the design reasoning and implementation; rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`, and re-rendered the docx to PDF to verify.

**2026-08-16 — Added a vertical-axis unit label to the Compare Loads trajectory chart (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The chart's left-edge numbers (drop in inches or cm) had no stated unit, unlike the single-load chart's own "Drop(in/cm)" label — a user glancing at the comparison chart could see numbers going up the axis with no way to tell what they meant. Added the identical "Drop(in)"/"Drop(cm)" label `drawCompareChart()` was missing, matching the single-load chart's existing convention exactly (same text format, same font, same muted grey). Since the chart's top margin already carries the per-load colour-swatch legend on a single row, the new axis label needed its own line above it rather than sharing the row and risking the two colliding on narrow screens (the legend has no wrap logic and can already run long with three loads). Split the previously-uniform `pad` variable into separate `padTop`/`padBottom` values — `padTop` grew from 32 to 44 to fit two stacked header lines (axis label, then legend, pushed down from y=14 to y=28), `padBottom` stayed at 32 — and threaded the split through the gridline loop and the `tx()`/`ty()` coordinate functions, which previously assumed a single symmetric `pad` for both margins. Verified with a new dedicated test (`test_compare_chart_axis_label.js`) that captures every `fillText()` call the chart makes (via a canvas context stub that records draw calls instead of a real canvas) and asserts: the axis label is drawn, reads "Drop(in)"/"Drop(cm)" correctly under each unit system, sits above the legend without overlapping it, and that the gridline tick-value numbers are still drawn correctly — a regression guard on the padTop/padBottom refactor not having silently broken the existing axis. Run against all four files (the two "General Release" files with no built-in centrefire presets by design skip the Compare-chart-specific checks, since there's nothing to compare, but the file itself is still verified to load and parse correctly), plus a full re-run of the existing suite — the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected. No guide or docx changes were needed — neither describes the chart's axis labeling in enough detail to be made inaccurate by this addition.

**2026-08-16 — Fixed clicks values not updating when the turret click-size selector is changed after Calculate/Compare has already run (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** User-reported bug, illustrated with two screenshots of the Compare Loads table showing identical clicks figures (43/42/47 clicks, 22 R/14 R/18 R) under both a 1/4 MOA/click and a 1/8 MOA/click selection — halving the click size should have roughly doubled the click counts. Root cause: `computeTrajectory()` baked `clicks`, `driftClicks`, `spinClicks`, and `coriolisClicks` as fixed strings into each row object, computed once using whichever `clickSizeMoa` was active at calculation time. `setClickSize()` only re-rendered from those same cached rows (`lastResults.results` / `lastCompareList`) without ever recomputing them, so every clicks value silently froze at the click size in effect when Calculate or Compare last ran — this affected the elevation Drop column too, not just the Wind/Spin/Coriolis columns added earlier this session, since Drop's clicks field had carried the identical latent bug since it was first introduced. Fixed by removing all four baked fields from `computeTrajectory()`'s row object entirely (kept only the underlying unit-invariant `moa` fields) and adding a shared `toClicks(moaStr)` helper that divides by the *current* `clickSizeMoa` at the moment of display, not at calculation time. `toClicks()` is now called wherever a clicks value is shown: `renderResults()`'s drop/wind/spin/coriolis cells (replacing the `windCol`/`spinCol`/`corCol` field-name-lookup pattern with `dispWind()`/`dispSpin()`/`dispCor()` getter functions, since a live computation can't be expressed as a static field name to look up), `renderCompareResults()`'s `cuDrop()`/`cuWind()` helpers, and both Range Card functions (`showRangeCard()`, `buildRangeCardText()`). Verified with a new dedicated test (`test_click_size_live_update.js`) that directly reproduces the reported scenario — calculate once, then change the click-size selector *without* recalculating, and assert the rendered table HTML actually changes — for both the main Results table and the Compare Loads table (matching the exact screenshots), plus confirms `toClicks()` recomputes correctly from the underlying `moa` fields at any click size. Updated `test_wind_spin_cor_clicks.js` and `test_compare_clicks.js` (written for the two preceding entries) to assert the *absence* of the now-removed baked fields and to verify the live `toClicks()` derivation instead, since they had been asserting the specific implementation this fix intentionally replaced. Run against all four files, plus a full re-run of the existing suite — the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected. No guide or docx changes were needed — both already described clicks as following the selector correctly, which this fix makes true rather than changing what's documented.

**2026-08-16 — Added a "clicks" option to the Compare Loads "Drop & Wind units" dropdown (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to the entry immediately below: having just added clicks to Wind/Spin/Coriolis on the single-load Results table, the user asked for the same option on the Compare Loads dropdown "for consistency" — closing a gap the Math Models document had previously described as a deliberate scope boundary (clicks was left out of the Compare dropdown because it needs a secondary click-size selector, which felt like unwarranted complexity for a summary table at the time). Compare's `cuDrop()`/`cuWind()` helper functions gained a `cu==='clicks'` branch reading the same `clicks`/`driftClicks` fields `computeTrajectory()` already produces (no new physics or fields needed — Compare draws on the identical per-load result rows the single-load table uses), and the dropdown's option list grew from `['mils','moa','in','cm']` to `['mils','moa','clicks','in','cm']`. Selecting clicks now also reveals a click-size `<select>` next to the unit dropdown, matching the single-load table's own. Rather than duplicating that selector's option-list markup a second time, extracted a shared `clickSizeOptionsHtml()` helper (previously inlined separately in each table's render function) and pointed both call sites at it, and extended `setClickSize()` to re-render whichever of the single-load table and the Compare table are currently cached (`lastResults`/`lastCompareList`), so the two click-size selectors always agree — changing the click size in either view updates both, since there's only ever one `clickSizeMoa` global rather than two independent settings that could silently disagree. Verified with a new dedicated test (`test_compare_clicks.js`) covering: the shared helper renders correct option markup with the right selection; the Compare table's Drop and Wind Drift row labels and dropdown correctly switch to "(clicks)" and back; the displayed clicks values trace back to the same `clicks`/`driftClicks` fields the single-load table uses, not an independent computation; and that `setClickSize()` correctly reflows the Compare table when it's the active view — run against all four files (the two "General Release" files with no built-in centrefire presets by design skip the comparison-table-specific checks, since there's nothing to compare, but still verify the shared helper and wiring), plus a full re-run of the existing suite — the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected. Updated the guide's Compare Loads "Drop & Wind Drift units" topic to mention clicks and the shared click-size selector, and replaced the Math Models document's paragraph explaining the old omission with one describing the new shared-selector design, rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`, and re-rendered the docx to PDF to verify.

**2026-08-16 — Added clicks support to the Wind Drift, Spin Drift, and Coriolis columns on the main Results table (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to two "comment don't code" discussions: adding clicks to the Compare Loads unit dropdown, and noticing the main Results page's Wind column showed inches, not clicks, when the elevation Drop column was displaying clicks — both surfaced the same gap, that no clicks equivalent existed anywhere for the app's three lateral/angular correction columns (only elevation Drop had one, via `clickSizeMoa`). The user approved converting all three. `computeTrajectory()` now returns `driftClicks`, `spinClicks`, and `coriolisClicks` alongside the existing `...Moa`/`...Mils`/`...In`/`...Cm` fields, computed identically to Drop's existing clicks field (`(moa/clickSizeMoa).toFixed(0)`), so the same click-size selector (1/8, 1/4, 1/2 MOA, or 0.1/0.2 MRAD per click) now governs elevation, wind, spin, and Coriolis consistently. The `windCol`/`spinCol`/`corCol` column-selection ternaries and their `windHdr`/`spinHdr`/`corHdr` header ternaries gained a `unitDrop==='clicks'` branch pointing at the new fields, matching how `driftMoa`/`driftMils`/etc. were already selected. Also fixed a latent bug in `fmtDir()` (the shared "X R"/"X L" formatter for all three lateral columns): its negative branch hardcoded `.toFixed(2)`, which was a silent no-op for every existing unit (mils/MOA/in/cm all already arrive pre-formatted to 2dp) but would have rendered whole-number click counts wrong — e.g. "-8" showing as "8.00 L" instead of "8 L". `fmtDir()` now derives its decimal count from the caller's own string instead of hardcoding it, fixing this for clicks while remaining unchanged for every pre-existing unit. Verified with a new dedicated test (`test_wind_spin_cor_clicks.js`) covering: the three new click fields appear and match `moa/clickSizeMoa` exactly; the column/header ternaries correctly resolve to the clicks fields and "(clicks)" headers when `unitDrop==='clicks'`, and are unchanged for mils/MOA; `fmtDir()`'s regression guard (existing 2dp units still round-trip identically) and its fix (whole-number clicks render without forced decimals, in both directions); and that changing the shared click-size selector recomputes all three consistently — run against all four files (using a real built-in preset via `loadPreset()` on the two files that carry one, and equivalent manually-populated fields on the two "General Release" files that ship with an empty built-in centrefire library by design), plus a full re-run of the existing suite — the same two pre-existing, unrelated failures already documented in earlier entries were confirmed unaffected. The Compare Loads dropdown remains unchanged — that was only discussed, not approved for implementation.

**2026-08-15 — Extended the .308 Win 150gr velocity correction to CervidBallistics.html, matching Personal/index.html.** Follow-up to the entry below: the user asked for the 2843fps → 2650fps correction to apply to all versions, not just Personal. `index.html` and `CervidBallistics_GeneralRelease.html` ship with an empty built-in centrefire preset library (`cf:[]`) by design — the public "General Release" builds have no built-in preset library at all — so there is no `.308 Win` entry in either to change; only `CervidBallistics.html` and `Personal/index.html` carry the built-in centrefire presets, and both now read 2650fps identically.

**2026-08-15 — Corrected the built-in .308 Win 150gr preset's muzzle velocity from 2843fps to 2650fps (Personal/index.html only).** A personal-data correction, deliberately applied only to the Personal build rather than all four canonical files — the public-facing builds (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html) keep the previous published figure. No logic changed.

**2026-08-15 — Fixed the Results page "Zero Offset" badge staying in mils regardless of the mils/MOA display toggle (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** Follow-up to the angularUnit fix above: a screenshot showed a preset with its own MOA turret preference (the Results table's mils/MOA toggle correctly showing MOA active) still displaying "Zero Offset +0.44 mil" in the header badge — the underlying number was a correct mils-equivalent conversion, but the unit itself never followed the page's own display toggle the way every other angular column (Drift, Spin, Coriolis) already does. Root cause: the badge read `params.zeroOffsetMils` directly and hardcoded the "mil" label, with no reference to `unitDrop` at all. Fixed by converting the stored mils value to MOA (`×3.6/1.0472`) whenever `unitDrop==='moa'`, and labelling the badge to match, falling back to mils for the non-angular display modes (clicks/in/cm) since a constant angular offset has no meaningful one-line linear equivalent to show there. Verified with a new dedicated test (`test_zero_offset_badge_unit.js`) reproducing the exact reported scenario (a `.177` preset with a mils-entered offset, Results table switched to MOA) and confirming the badge converts correctly, that a negative "hold-under" offset preserves its sign through the conversion, that the badge correctly falls back to mils on clicks, and that no badge appears at all when Zero Offset is inactive — run against all four files, plus a full re-run of the existing suite (the same two pre-existing, unrelated failures already documented in earlier commits were unaffected). Extended the guide's existing Zero Offset tip callout to mention the badge now follows the toggle too; rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`.

**2026-08-15 — Replaced the calibre-based MIL/MOA default with a per-preset stored preference, fixing Zero Offset's mils-only default and its own root cause (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Investigated a report that Zero Offset always saved in mils, even when a preset's own convention was MOA. Confirmed the save/load round-trip itself was correct — whichever unit was selected persisted and restored correctly — but found the real problem one layer up: the Zero Offset toggle always reset to mils by default on every preset load, with no way to inherit a preset's own MOA convention. The first proposed fix — default Zero Offset to `unitDrop`, the existing results-table display unit, which itself defaults to MOA for `.177` calibres and mils for everything else — was flagged and rejected on review: calibre says nothing about which turret is actually mounted on a rifle, and a .308 with an MOA-turret scope or a .177 with a mil-dot scope are both real configurations that heuristic gets wrong. Implemented the corrected fix instead: each preset now carries its own `angularUnit` field (mils or MOA), captured automatically the first time it's saved with the results table showing mils or MOA — independent of calibre — and preferred over the old calibre guess on every later load of that preset. Saving while the results table is showing a non-angular mode (clicks, inches, cm) neither sets nor overwrites this preference, so briefly checking click values before hitting Save can never silently erase a preference set earlier; `buildPresetEntryFromForm()` now takes an optional `priorEntry` parameter to support this carry-forward, threaded through all three save call sites (`saveCurrentAsPreset()`, and both branches of `saveTruingToPreset()`) in all four files — including the two files (`index.html`, `CervidBallistics_GeneralRelease.html`) that additionally carry Single Load Mode's own replace-in-place logic, which needed the same threading through its distinct code path. A preset that has never been saved on mils or MOA falls back to the original calibre-based guess exactly as before, so every preset saved prior to this change behaves identically — no migration needed. Zero Offset's own default now follows this resolved unit instead of a hardcoded mils. Verified with a new dedicated test (`test_angular_unit_preference.js`) covering: a non-.177 preset with an MOA preference persisting and restoring correctly (the core reported concern); the inverse case, a `.177` preset with a mils preference overriding the old forced-MOA default; Zero Offset's toggle following the resolved unit when no offset is saved yet; legacy presets with no `angularUnit` field falling back to the old calibre guess unchanged; saving on a non-angular display mode preserving rather than clobbering a prior preference; and a genuinely brand-new preset saved on a non-angular mode ending up with no stored preference at all. Run against all four files (the Single Load Mode default in two of them required explicitly disabling it in the test to make "brand-new preset" comparable across files), plus a full re-run of the existing suite — the same two pre-existing, unrelated failures already documented in the Share button entries were confirmed unaffected. Added a callout to the guide's Zero Offset topic explaining the new per-rifle preference, and a new paragraph to the Math Models document's Section 13 (Zero Offset) covering the original calibre heuristic, why it was wrong for this purpose, and the corrected per-preset design — verified by rendering to PDF.

**2026-08-15 — Removed the Range Card's "Known limitation" callout about the WebKit share-sheet quirk from the User Guide (cervid-guide.html in the root folder and Personal/).** The callout was added defensively alongside the title-drop fix, before that fix had been confirmed live. Since the fix (dropping the `title` field from `navigator.share()`) resolves the reported Notes behaviour, the user asked for the now-unnecessary warning to be removed rather than carried indefinitely. Deleted the callout from the guide's Range Card topic in Section 10 (Field Tools), rebuilt, and redeployed to both the root folder and `Personal/`; the app-side fix and the Math Models document's historical account of the bug and its resolution are untouched, since those remain accurate records of what happened rather than live warnings to the end user.

**2026-08-15 — Documented the WebKit share-sheet quirk and its fix in the User Guide and the Math Models & Design Methodology document (cervid-guide.html in the root folder and Personal/; Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to the title-drop fix below. Added a "Known limitation" callout to the guide's Range Card topic (Section 10, Field Tools) explaining that Notes or another share target may occasionally show a link back to the app instead of the range text — a Safari quirk outside the app's control — and that choosing "Copy" from the share sheet reliably gets the actual text onto the clipboard regardless. Rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`. Extended the Math Models document's existing Apple Watch paragraph (Part 2, Section 1) with the concrete follow-up: what the bug looked like, why it happens (Safari's share sheet, invoked from a regular browser tab, offers the current page as a shareable item alongside the supplied text, and Notes' share extension preferred that page-link item when both title and text were present with no url), and the mitigation (dropping the title field). Verified by rendering the updated document to PDF and reading the new sentences in place.

**2026-08-15 — Fixed the Share button dropping Range Card text in favour of a page-link card on macOS Notes (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up bug report: sharing to Notes produced an empty note body with only a rich link card ("Cervid Ballistics" / mybc.cervid.net, with the app's thumbnail) instead of the range data. Root cause is a documented, long-standing WebKit quirk rather than an app bug — when `navigator.share()` is called from a regular Safari tab (not an installed/Home-Screen web app), Safari's share sheet also offers the current page itself (URL, title, Open Graph thumbnail) as a shareable item alongside whatever `text`/`title` fields the page supplies, and different target apps pick unpredictably between them; Notes' share extension in particular favours the page-link item over plain text when both `title` and `text` are present with no `url`. Confirmed via multiple independent reports (Apple Developer Forums, a WebKit bug referenced from a 2020 write-up, and Apple Community threads) describing the same title+text-without-url combination as one of the most unreliable. Fix: dropped the `title` field from the `navigator.share()` call in `shareRangeCard()`, passing only `text` — the combination most consistently reported to avoid the substitution. Updated the dedicated headless test to assert `title` is absent from the captured `navigator.share()` payload (guarding against it being reintroduced), re-ran it plus the full regression suite against all four files — all clean. This is a mitigation, not a guaranteed fix, since the underlying behaviour lives in WebKit's share-sheet implementation rather than anything the app controls; if Notes (or another target) still substitutes a link card, the clipboard-copy fallback path remains available as a fully reliable alternative.

**2026-08-15 — Added a "Share" button to the Range Card, as a practical bridge to Apple Watch (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** The user asked whether the app's Range Card could integrate with an Apple Watch. Direct integration isn't possible within this project's single-file, no-server, offline-first architecture — watchOS has no general web browser, so there's no way for a watch to load the app directly, and building a companion watch app or backend sync service would mean abandoning the "open a file in Safari, nothing else required" model that's been the project's core constraint from the start. Proposed and built the practical alternative instead: a Share button next to Print, using the standard Web Share API (`navigator.share()`) to hand the Range Card's data to the native iOS/macOS share sheet as plain text, which can then be routed to Notes, Reminders, Messages, or a personal Shortcut — several of which sync to and display on an Apple Watch automatically, without the app needing to know anything about watchOS at all. Added `buildRangeCardText()`, which formats the currently-displayed range/holdover rows (respecting whichever unit is selected — mils, MOA, in, cm, or clicks — and the current imperial/metric range units) into a compact plain-text list with ★/◆ markers for the zero and spot ranges, matching what's shown on screen; and `shareRangeCard()`, wired to the new button, which calls `navigator.share()` where available and falls back to `navigator.clipboard.writeText()` (with a brief "Copied!" confirmation on the button itself) on browsers without share-sheet support, or a plain alert as a last resort. Replaced the previous full-width Print button with a two-button flex row (Print + Share) so neither loses prominence. Verified with a new headless test covering text construction (calibre/name fallback, zero/spot markers, unit-label correctness in both imperial and metric modes, empty-state no-op when no results exist yet) and all three `shareRangeCard()` code paths (Web Share API, clipboard fallback, alert fallback), run against all four canonical files, plus a full re-run of the existing regression suite to confirm nothing else was affected — two unrelated, pre-existing test failures (`test_chart_energy_smoothing.js`, `test_export_amended_builtins.js`) were confirmed to already fail identically against the pre-change files, ruling them out as regressions from this change. Updated the guide's Range Card topic in Section 10 (Field Tools) to describe the new Share button and the Notes/Reminders-sync-to-Watch workflow, rebuilt, and redeployed to both the root folder and `Personal/`. Also added a paragraph to the Math Models & Design Methodology document, in Part 2 Section 1 ("Single-File, Zero-Dependency Architecture"), documenting the Apple Watch question as a concrete example of that architectural constraint being tested and resolved without compromise — direct integration was ruled out (watchOS has no browser), so the Share button became the way to honour the request while staying single-file and server-free. Verified by rendering the updated document to PDF and confirming the new paragraph reads correctly in place.

**2026-08-14 — Added a headwind/tailwind velocity effect to the trajectory model, on top of the existing crosswind deflection (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/, Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Follow-up to the "why isn't headwind modelled" discussion: asked whether it should be, given the initial user base skews air rifle/rimfire and pellets are known to be especially wind-sensitive. Quantified it first, against the real engine, before writing any code — a light, low-BC air rifle pellet (JSB Exact .177 8.44gr, BC 0.019) at a typical 50yd Field Target distance showed roughly 0.07-0.09mil of extra drop from a 15-20mph headwind, proportionally *larger* in angular terms than the same wind speed's effect on the app's own .243 Win 100gr centrefire preset at 500yd — because wind speed is a much bigger fraction of a pellet's much lower muzzle velocity. A heavier, higher-BC pellet (JSB Exact Monster .22 25.4gr, BC 0.084) showed an effect roughly ten times smaller under the same conditions. That result changed the call from "marginal, skip it" to "worth building." Implemented physically: drag is now computed from the bullet's velocity relative to the air, not the ground. Added `rangeWindFps` — the same Wind Clock field's along-shot component, using `cos()` where the existing crosswind term uses `sin()`, so it's zero at the pure-crosswind positions (3/9 o'clock) and maximal at 12 o'clock (headwind, positive) and 6 o'clock (tailwind, negative). Inside the RK4 integrator, `derivX()` now computes the deceleration's magnitude and direction from the relative airspeed (`vx+rangeWindFps`, `vy`) rather than ground-frame `(vx,vy)` — including the Mach number fed into the drag-table lookup — while ground-frame position and every reported figure (velocity, energy, Mach, subsonic flag) are left on true ground speed exactly as before, since that's what a chronograph or the target itself actually measures. Because `derivX()` is a single function reused by all three RK4 loops in `computeTrajectory()` (the zero-elevation search, the barrel-pitch bisection, and the main output loop), this one change applies consistently everywhere a trajectory gets computed, including inside Trajectory Truing. Verified the implementation against the standalone diagnostic script used to quantify the effect beforehand — the live engine's headwind/tailwind numbers matched it exactly — then added a dedicated headless test confirming zero wind is a complete no-op, pure crosswind (3/9 o'clock) is byte-for-byte unchanged from the pre-existing model (a regression guard on 5+ years of established crosswind behaviour), headwind increases drop/decreases velocity/increases time of flight, tailwind does the exact opposite, and the two are roughly symmetric in magnitude — plus a full re-run of the existing Compare Loads, Zero Offset, Coriolis, and mobile-rotation suites across all four files to confirm nothing else regressed. Updated the Wind Clock help-tip in the app (previously stated head/tailwind wasn't modelled — now explains both effects and their relative scale), rewrote the guide's Wind Speed & Wind Clock topic and retitled/rewrote its former "only crosswind is modelled" limitations entry to "Head/tailwind now affects drop, not just crosswind" with the quantified numbers, and added a new subsection to the Math Models document's existing "5. Wind Deflection" section covering the `rangeWindFps` formula, the relative-airspeed integration detail, and the same magnitude findings — verified by rendering the updated document to PDF and reading the relevant page.

**2026-08-14 — Corrected the Scope Height guidance figure in the User Guide, and added a real explanation of why headwind/tailwind isn't modelled, in both the app and the guide (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** Two edits. First, Section 3's "core numbers" topic said Scope Height is typically 1.5–2.5in for a rifle; corrected to 2–3in, the more accurate range. Second, the user pointed out that the app never actually explains *why* a head or tailwind's effect on velocity isn't separately simulated — only that it isn't. That line already existed almost verbatim as a one-sentence fact in the guide's Tips & Known Limitations section, stating the what with no why. Added a help-tip (the same `?`-icon pattern already used throughout the Firearm & Load panel) to the Wind Clock field in the app itself, and expanded the guide's limitations topic with the actual reasoning: typical wind speeds are a small fraction of typical muzzle velocities, so a headwind or tailwind changes the bullet's airspeed — and therefore its drag and time of flight — by only a percent or two, a second-order effect dwarfed by the crosswind's direct sideways push and by other uncertainties already in the model (BC tolerance, chronograph error); modelling it properly would also require tracking wind as a full vector relative to the shot rather than a simple clock-position split, real added complexity for a correction too small to matter at practical distances. Checked the Math Models & Design Methodology document for the same gap — it already documents the wind-clock convention in its own words and doesn't contain the brief unexplained line the guide had, so it didn't need a change. Verified with a syntax check (`new Function()` over the inline scripts) confirming the new help-tip is present and doesn't break anything, plus a full re-run of the Compare Loads, Zero Offset, Coriolis, and mobile-rotation test suites across all four app files, and a brace-balance/content-presence check on the rebuilt guide.

**2026-08-13 — Fixed the same iOS Safari text-inflation-on-rotation bug on the User Guide (cervid-guide.html in the root folder and Personal/).** Follow-up to the Compare page fix above — the user reported the same scaling behaviour on the Guide when rotating an iPhone. Same root cause (iOS Safari's automatic text-size inflation on standalone paragraph-like blocks after a layout change), but far more exposed here: the app's case was one isolated warning banner, whereas the guide is built almost entirely out of exactly the shape of content that heuristic targets — every `.topic` card's `<p>` text and every `.callout` box throughout all twelve sections. Checked for a second contributing cause too, since the Compare page's version of this bug was compounded by an unconstrained-width table; the guide has no tables actually rendered in its current content (the `table.ref-table` styling exists in the stylesheet but isn't used by any section yet), so this was a clean, single-cause fix. Added the same `-webkit-text-size-adjust:100%;text-size-adjust:100%;` opt-out to the guide's `html` rule in its build template (`guide_template.html`), rebuilt, and redeployed to both the root folder and `Personal/`. Verified the rebuilt file's braces are balanced and both the new rule and the previously-added `closeGuide()` function are intact.

**2026-08-13 — Fixed the Compare Loads warning text still enlarging on iPhone rotation after the chart-and-table fix below (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Immediate follow-up: the chart fix resolved the chart itself, but the user reported the yellow warning banner still grew larger than the rest of the page text specifically when rotating. This turned out to be an unrelated bug from the table/chart issue — iOS Safari's automatic text-size inflation ("font boosting"), a WebKit heuristic that enlarges standalone, paragraph-like blocks of text after a layout change (like a rotation) unless the page explicitly opts out. The warning banner — a lone `<div>` with its own small `font-size:11px` and prose-style content, sitting by itself rather than embedded in a table cell or button — is exactly the shape of element that heuristic targets; nothing elsewhere on the page has that shape, which is why only the warning text was affected. Fixed by adding `-webkit-text-size-adjust:100%;text-size-adjust:100%;` to the page's `html` rule, the standard opt-out for this behaviour — it doesn't change how anything renders under normal conditions, it just stops Safari from silently rescaling text after a rotation. Verified with a full syntax check (`new Function()` over the inline scripts plus a style-block brace-balance check) and a complete re-run of the Compare Loads, Zero Offset, Coriolis, and mobile-rotation test suites across all four files — all passed clean, since this is a pure CSS addition with no logic touched.

**2026-08-13 — Fixed the Compare Loads chart and warning text appearing to "scale" incorrectly on iPhone when rotating between portrait and landscape (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user suspected the results table at the bottom of the Compare page — reported wider than the rest of the page content on iPhone specifically, not iPad or desktop — and investigation confirmed two compounding bugs, both isolated to Compare. First: the main single-load Results table is already wrapped in `<div class="tbl-wrap">` (`overflow-x:auto`), so if it's ever too wide it scrolls locally and the page itself is unaffected — but the Compare table never got the same treatment, and its `.metric-label` column is `white-space:nowrap`, so labels like "Wind Drift (MOA) at 1000yd" (lengthened by the recent unit-dropdown feature) plus 2–3 load columns can exceed an iPhone's ~375–430px portrait width without ever being contained, letting the overflow escape into the page — which is almost certainly what confuses iOS Safari's viewport/zoom recalculation on rotation (a known Safari quirk when page content exceeds the viewport with nothing to clip it), dragging the whole visible page, including the warning banner text, along with it. Second, and independently: `drawCompareChart()` sets the canvas's pixel-buffer width from `canvas.offsetWidth` exactly once, at render time, with no `resize`/`orientationchange` listener to redraw it — so rotating the phone changes the canvas's CSS box size while the underlying bitmap stays fixed, and the browser stretches the stale bitmap to fit, visibly warping the chart on every rotation regardless of the table issue. (The main single-load chart has the identical code pattern but was never observed to have this problem, supporting the table overflow as the real trigger rather than a canvas-specific difference — its own table's containment means its page never enters the corrupted state to begin with.) Fixed both: wrapped the Compare table in the same `.tbl-wrap` class already used elsewhere, and added `redrawCompareChartOnResize()` — wired to both `resize` and `orientationchange` (the latter with the same 200ms settle delay already used for `setAppHeight()`, since iOS reports the wrong dimensions in the instant right after rotation) — which redraws the chart from the already-cached `lastCompareList`/`lastCompareRefYd` (the same cache the unit dropdown uses) rather than recomputing anything, and is a no-op whenever the Compare overlay isn't open. Verified with a new headless test confirming the table markup is correctly wrapped, that both listeners are registered, that a resize while the overlay is closed touches nothing, and that a simulated rotation (via a canvas offsetWidth change) correctly updates the canvas's pixel width without altering the comparison selection — plus a full re-run of the existing Compare Loads, Zero Offset, and Coriolis test suites across all four files to confirm nothing else regressed.

**2026-08-13 — Swapped header button order (Compare now before Guide) and added a Close button to the User Guide for mobile users (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html, cervid-guide.html in the root folder and Personal/).** Two small usability requests. First, reordered the header so "⚖️ Compare" sits before "📖 Guide" in all four app files — purely a markup reorder, no logic touched. Second, added a "✕ Close" button to the guide's own header, next to the existing Print button: the guide opens via a link with `target="_blank"`, and on desktop that's a normal new tab with its own close control, but on mobile the user pointed out that returning to the app relies on a swipe-back gesture many people won't know about, and some mobile browsers actually ignore `target="_blank"` and navigate in the same tab anyway, which makes the missing on-screen way back more noticeable. Since the guide is shared across all four app variants, the button can't just link to one hardcoded "back" destination — implemented `closeGuide()` with a three-step fallback: first `window.history.back()` if the guide replaced the current tab (true whenever a browser ignored `target="_blank"`, which covers the swipe-back scenario reported); if that tab has no prior history (a genuine new tab), fall back to `document.referrer` — reliably populated with whichever of the four app files actually linked here, since the link uses `rel="noopener"` only, not `noreferrer`; and only as a last resort attempt `window.close()`. The button is already covered by the existing print stylesheet's blanket `.header-actions{display:none}` rule, so it doesn't appear in printed/PDF copies. Verified the JS change with a brace-balance and function-presence check on the rebuilt guide, and confirmed the button-order change with a full syntax check (`new Function()` over all inline scripts) plus a re-run of the existing Compare Loads, Zero Offset, Coriolis, and unit-dropdown test suites against the edited file — all passed clean, since reordering two sibling elements touches no logic or IDs any test depends on.

**2026-08-13 — Replaced the User Guide header banner with the app's own full-artwork crop, ending the clipping for good (cervid-guide.html in the root folder and Personal/).** Second follow-up on the same clipping report — my previous re-crop (wide 1.97:1 strip with headroom above the peak) still clipped, and the user pointed at "other instances of the graphic" to compare against. Template-matched `cervid-icon-square.jpg` (the small icon already used in the main app's own header, confirmed clipping-free) back against the original artwork and found it uses a crop box of (80,80)–(1175,1175) out of the 1254×1254 source — essentially the *entire* piece of art with just the outer picture-frame border removed, not a tight strip around any one element. Every previous banner attempt, including today's earlier one, had instead tried to slice a wide horizontal band out of a composition that isn't laid out to survive that: the mountain range, full crosshair reticle, and bullet all occupy nearly the full height of the square canvas, so any band narrower than the whole thing was always going to cut something. Replaced the banner with that same near-full-image crop the icon already uses (490KB PNG region → 500×500 JPEG, 31KB), and reduced the header's display size from a 420×130 wide slot down to a 220×160 square slot to match the image's now-square aspect ratio, per the user's explicit "if it needs to be reduced in size, so be it." `object-fit:contain` still governs final placement, so nothing is cropped at any viewport width. Verified the same way as before — rendering a pixel-accurate mockup of the real CSS constraints — confirming the full mountain range, both crosshair arms, and the complete bullet all render with clear margin on every edge. Rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`, committed locally.

**2026-08-13 — Fixed the actual cause of the User Guide header banner clipping: the source image itself was pre-cropped too tight, not the CSS (cervid-guide.html in the root folder and Personal/).** Immediate follow-up to the CSS fix below — the user confirmed the mountain tops were still being cut off after that fix shipped. Investigation found the CSS (`object-fit:contain`, which cannot crop by definition) was correct and fully deployed; the real problem was one layer downstream. `cervid-banner-crop.jpg`, the image actually embedded in the guide, was a leftover asset from an earlier, since-abandoned wide-banner header design (see the 103–108 header-consolidation entries below) — it had already been cropped down to a 2.43:1 strip in a way that sliced the tallest mountain peak's summit off at the top edge, so no amount of CSS could recover detail that wasn't in the file. Went back to the original uncropped square artwork, `cervid-banner.png` (1254×1254), located the actual picture-frame border programmatically to crop it out cleanly, and cut a fresh wide crop with generous headroom above the peak (aspect ratio 1.97:1) instead of reusing the old strip. Downscaled the new crop to 840×426 (2× the header's display size, for retina sharpness without bloating the file) before re-embedding as base64, which also shrank the guide file from ~135KB to ~124KB despite the image being visually larger. Verified by rendering a pixel-accurate mockup of the actual CSS box (900px-wide dark background, image scaled to the real `max-height:130px object-fit:contain` constraint) and confirming the full mountain range and bullet render with no cropping. Rebuilt and redeployed `cervid-guide.html` to both the root folder and `Personal/`, committed locally.

**2026-08-13 — Fixed the User Guide header banner being heavily cropped, and updated both the User Guide and the Math Models & Design Methodology document to cover the new Compare Loads unit dropdown (cervid-guide.html in the root folder and Personal/; Cervid Ballistics - Mathematical Models and Design Methodology.docx).** Two follow-ups after shipping the unit-dropdown feature below: the guide's own banner was cropped, and neither reference document mentioned the new dropdown yet. The banner bug: `.gheader .banner-wrap` forced the 2.43:1 banner image to `width:100%` inside a `max-height:220px` container with `overflow:hidden`, so at any reasonably wide viewport the scaled image was far taller than 220px and had its top and bottom sliced off. Fixed by switching to `object-fit:contain` inside a flex-centred, unconstrained-width wrapper capped at `max-height:130px` — `contain` cannot crop by definition, so the full banner is always visible, just smaller. Added a new "Drop & Wind Drift units" topic to the guide's Compare Loads section (tagged Intermediate) describing the dropdown and its live-relabelling behaviour. Rebuilt the single guide file from its template/section/image sources and redeployed identical copies to both the root folder and `Personal/`, re-checking anchor integrity, CSS brace/paren balance, and embedded-image validity as before. On the Math Models document, added a new Part 3, Section 3, "Display Unit Selection (Drop & Wind Drift)", covering the `compareUnitDrop` state variable, the re-render-from-cache mechanism (`runCompare()` caching `lastCompareList`/`lastCompareRefYd`, `setCompareDropUnit()` reading from it), why every unit variant is already available on each result row with no recomputation needed, and the deliberate decision to leave "clicks" out of this dropdown. Inserting the new section renumbered the three sections after it (Live vs. Saved State Resolution, Centrefire-Scoped Subsonic-Crossing Warning, and Testing Methodology, now Sections 4–6) and required updating one internal cross-reference in Section 2 that pointed to the old Section 3 number. Verified by rendering both the new section and the following renumbered sections to image and reading them directly, confirming the cross-reference updated correctly and the section numbering runs cleanly through to the end of Part 3.

**2026-08-13 — Added a unit dropdown (mils/MOA/inches/cm) to the Compare Loads results table, with the Drop and Wind Drift row labels now stating the unit explicitly (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked whether the Compare table's Drop and Wind Drift rows could show what unit they were expressed in, and whether a dropdown could let the shooter pick the readout they find most useful — previously these two rows were hardcoded to whichever of inches/cm matched the app's Imperial/Metric toggle, with no unit shown in the label at all and no way to see them in mils or MOA the way the single-load Results table already allows. Added a new `compareUnitDrop` state variable (defaulting to `'mils'`, matching the single-load table's own default) and a `<select>` dropdown rendered directly above the comparison table, offering mils/MOA/inches/cm — deliberately independent of the single-load Results table's own unit toggle, since a shooter comparing loads may reasonably want a different readout there than on the main page. The Drop and Wind Drift row labels now read e.g. "Drop (mil) at 500yd" and "Wind Drift (mil) at 500yd", updating live as the dropdown changes. Rather than re-running the comparison, `setCompareDropUnit()` re-renders from a newly cached `lastCompareList`/`lastCompareRefYd` (set at the end of `runCompare()`) — every unit variant (`dropIn`, `dropCm`, `moa`, `mils`, `driftIn`, `driftCm`, `driftMoa`, `driftMils`) is already present on each result row from the original calculation, so switching units is a pure, instant re-render with no recomputation, consistent with how the single-load table's own unit buttons already work. `clicks` was deliberately left out of this dropdown (unlike the single-load table's five-way toggle) since it requires a secondary click-size selector that would add real complexity to this compact summary table for a unit the user didn't ask for. Verified with a new headless test confirming the default is mils with the label stating so, that each of the four dropdown options produces both the correct label text and an independently-recomputed matching value in the table, that the `<select>` element itself reflects the current selection after a switch, and that switching units never touches the comparison selection or distance — plus a full re-run of the existing Compare Loads test suite and wider regression suite to confirm nothing else regressed, across all four files.

**2026-08-13 — Added a linked, single-file User Guide covering the app for basic, intermediate, and advanced shooters (new file `cervid-guide.html`, added to both the root folder and `Personal/`; header link added to CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked for an aesthetically pleasing guide for three audiences and to suggest design options first. Proposed and confirmed three choices before building: format (single-file HTML rather than PDF or Word, matching the app's own "single file, works offline" philosophy, with a `@media print` stylesheet built in so a paper copy is still one click away via the browser's own Print dialog), structure (one feature-by-feature walkthrough rather than three separate documents, with each topic tagged Basic/Intermediate/Advanced via a colour-coded badge so a reader can self-filter without the content being duplicated three times), and visual style (dark and app-matched, reusing the calculator's own CSS palette, banner, and icon rather than a generic light "manual" look). Built as a single self-contained HTML file — the banner and app icon are embedded as base64 data URIs rather than linked as separate image files, so the guide stays exactly as portable as the calculator itself. Content was written against the actual current interface (field labels, button text, and section names pulled directly from the live HTML/JS rather than from memory) and covers all twelve feature areas end to end: getting started, entering a load, drag model choice, environmental conditions, reading results, presets, Trajectory Truing and Verification, Zero Offset, Coriolis/spin drift, Range Card/Reticle View/turret dial, Compare Loads, and a closing tips-and-known-limitations section. A caught-and-fixed bug during build: the sidebar navigation's CSS and JS both targeted `.sidebar nav a`, but the sidebar `<nav>` element *is* `.sidebar` itself rather than containing a nested `<nav>`, so none of the intended link styling or the mobile "close menu on click" behaviour would actually have applied — fixed by correcting the selectors to `.sidebar a` in both the stylesheet and the click handler. Verified with automated structural checks in place of a real browser (unavailable in this environment): every sidebar link's `#anchor` target confirmed to exist, CSS brace/paren balance and full `var(--x)` custom-property resolution checked programmatically, both embedded images confirmed to decode to valid JPEG headers, and a full HTML parse pass with no fatal errors — plus a manual line-by-line read-through of the assembled file (which is how the sidebar selector bug above was actually caught). A "📖 Guide" button was added to the header of all four synced app files, opening the guide in a new tab alongside the existing "⚖️ Compare" button.

**2026-08-13 — Restored the Compare Loads subsonic-crossing warning, scoped to centrefire only (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Immediate follow-up to removing the warning outright below: after discussion, agreed subsonic centrefire loads are usually a deliberate choice (e.g. a purpose-built subsonic .300 BLK load for a suppressed rifle) and worth flagging, whereas the original blanket removal was really only solving a noise problem specific to air and rimfire, where subsonic flight is the ordinary case rather than the exception. Re-added `wentSubsonic` to `runCompare()`, but gated it with `x.ref.mode==='cf'` so it's only computed/true for centrefire selections — air and rimfire loads never trigger it regardless of their own actual velocity. This needed `mode` threading through cleanly since the per-load object built in `runCompare()` didn't previously need to know which mode a given Compare slot belonged to; `x.ref.mode` was already available (each selection carries its own mode, since Compare allows mixing modes in one comparison) so no new state was needed, just reading what was already there. Updated the existing headless test (`test_compare_editable_distance.js`'s Test 5) back to asserting the warning fires exactly when a centrefire load actually goes subsonic within the compare distance, and added a new Test 5b seeding an air-mode pellet load that genuinely goes subsonic to confirm the mode gate actually suppresses the warning there, not just that it happens not to fire. Full regression suite re-run clean across all four files.

**2026-08-13 — Removed the subsonic-crossing warning from Compare Loads (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked for the warning that a compared load "goes subsonic before [distance]" to be dropped, alongside a claim that subsonic velocity is essentially universal for airgun pellets/slugs and generally the case for rimfire. That claim doesn't hold as a blanket rule — magnum springer and PCP pellet loads routinely clear the sound barrier, and high-velocity rimfire (standard-velocity .22LR is subsonic, but high-velocity .22LR, .17 HMR, and .17 WSM are all comfortably supersonic) — but the UX case for removing the warning stands on its own regardless: for the loads where it's genuinely normal, the warning was firing on a large share of Compare results and adding noise rather than signal. Removed `wentSubsonic` entirely from `runCompare()` (no longer computed via `results.some(r=>r.isSubsonic)` or attached to the returned per-load object) and removed the corresponding line from `renderCompareResults()`'s warning banner. The extrapolation warning (a load shown beyond its own saved or currently-configured max range) is untouched and still fires independently. This only affects the Compare Loads overlay — the single-load Results table's per-row subsonic marker is a separate, unrelated piece of UI and was left exactly as is. Updated the existing headless test (`test_compare_editable_distance.js`'s Test 5) to assert the warning text is now absent even for a load that genuinely does go subsonic within the compare distance, rather than asserting it fires — plus a full re-run of the existing Compare Loads test suite and the wider regression suite to confirm nothing else regressed, across all four files.

**2026-08-13 — Compare Loads now uses live, not-yet-saved amendments for the load currently open on the Inputs tab, instead of always re-reading the saved preset (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked whether the comparison could compute a preset's live/amended values — e.g. after Trajectory Truing, or a quick weight or BC tweak that hasn't been saved back — rather than always falling back to whatever's stored, provided it still falls back correctly when there's nothing amended. Added `isLiveMatchForRef()`, which checks whether a selected Compare slot's preset is the exact same one currently loaded on the Inputs tab — matched by mode, type, and name against `loadedPresetRef`, the same identity-matching pattern the baked-truing badge already relies on, plus requiring `lastResults` to exist (nothing calculated yet means there's no live outputs page to defer to). When it matches, `runCompare()` clones `lastResults.params` — literally the same params object driving the currently-displayed Results page — instead of calling `buildParamsFromPreset()`, so a live BC from truing, an edited Zero Offset, a changed weight, or anything else not yet saved is picked up automatically; it falls back to the stored preset the instant that's not true (different preset selected, nothing loaded, or the mode was switched, which already clears both `loadedPresetRef` and `lastResults`). No new "is this amended" detection logic was needed: reading live state whenever the identity matches produces identical results to the saved preset when nothing has actually changed, so the fallback is automatic rather than a separate code path. A live-matched load is clearly marked "(live)" in both the headline table's column header and the chart legend, and the extrapolation warning's wording adjusts to say "currently configured max range" rather than "saved max range" for that case, since there's no saved value to refer to. This is a pure read — the live single-load state and the saved preset are never written to by Compare. Verified with a new headless test: confirms no live match exists before anything is loaded on Inputs, confirms loading a preset makes it a live match, confirms an unamended-but-loaded preset still produces identical figures to the saved version, confirms a genuine unsaved amendment (muzzle velocity bumped from 2938 to 3200fps without saving) is reflected in the Compare table rather than the stale saved figure, confirms the saved preset itself is never touched by any of this, confirms an unrelated selected load that was never loaded on Inputs is correctly not treated as live, and confirms a mode switch correctly clears the live-match state — plus a full re-run of the existing Compare Loads test suite to confirm nothing else regressed, across all four files.

**2026-08-13 — Made the Compare Loads comparison distance editable, with soft warnings instead of a hard cap (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to Compare Loads: asked whether the shared distance driving the comparison chart and table could be user-editable rather than fixed to the auto-computed shortest-shared-max. Added a "Compare to" field next to the Compare button, defaulting to that same auto-suggested distance and re-suggesting it as the selection changes — but only until the shooter types their own value, at which point `compareDistanceCustomized` latches and the typed value is left alone across further selection tweaks, only resetting back to auto-suggest once the selection is cleared to zero (a genuinely fresh comparison). Kept consistent with the rest of Compare Loads' explicit-button interaction model per instruction: editing the field never recomputes anything on its own, only the next tap of Compare. Deliberately did not cap the distance — the physics engine will extrapolate to any typed value — but added soft, non-blocking warnings instead: one when the chosen distance exceeds a selected load's own saved max range ("figures out this far are extrapolated"), and one when a load actually crosses into the subsonic/transonic regime before reaching that distance, reusing the existing per-row `isSubsonic` flag rather than inventing new detection logic. Both warnings render in the same yellow inline-banner style already used elsewhere in the app (BC warning, plausibility warning), and are purely informational — the comparison still runs and displays regardless. Verified with a new headless test confirming the field auto-suggests correctly as loads are added/removed, that a manual edit survives further selection changes, that `runCompare()` actually uses the typed value and reflects it back into both the field and the table's row labels, that the extrapolation warning fires without blocking computation, that the subsonic warning's presence exactly matches whether a load actually goes subsonic within the chosen distance (checked against an independent recomputation, not just string-matching the warning text), and that clearing the selection to zero resets the field back to auto-suggest mode — plus a full re-run of the existing test suite (including the original Compare Loads test) to confirm nothing else regressed, across all four files.

**2026-08-13 — Added "Compare Loads": side-by-side headline stats and trajectory chart for up to three saved presets, triggered by an explicit Compare button (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** A test user suggested letting a shooter compare several rifle/load combinations at once — e.g. deciding which of three to bring to a competition. Discussed feasibility and layout first: the core physics function, `computeTrajectory()`, was already a pure function of a params object with no dependency on the live form, which is what made a comparison view tractable without touching the calculation engine at all. Implemented as a full-screen overlay (rather than a third desktop column or mobile tab, since one layout then works identically at any width) opened via a new "⚖️ Compare" button in the header. Inside, a mode-grouped preset grid (Air Rifle / Rimfire / Centrefire, reusing the existing preset data and covering both built-in and custom presets) lets the shooter tap up to three tiles — each mixable across modes, since nothing about the physics requires them to match — with the selected tiles getting a numbered, coloured badge (blue/orange/violet) that carries through to a removable chip strip and the eventual chart legend, so the same colour always means the same load throughout. Selecting is non-destructive and explicit: nothing recomputes until the shooter taps the new "Compare" button, and opening the whole overlay never touches the live form fields the shooter has open on the Inputs tab. New `buildParamsFromPreset()` builds a params object directly from a stored preset's own fields (weight, velocity, BC, zero/max, dia/len/twist — already stored in canonical imperial units regardless of what unit system was active when saved) while reading environmental conditions (altitude, temperature, wind, latitude, etc.) live from the form, since presets never store those — so all compared loads are judged under today's actual conditions, not whatever was on screen when each was individually saved. `runCompare()` first finds the shortest max range among the selected presets and recomputes all three out to exactly that shared distance, so every load has a genuine data point at the comparison range and the chart's lines all end together rather than at mismatched lengths. Results render as a compact headline table (muzzle velocity/energy, velocity/energy/drop/wind at the shared range, gyro stability) plus a new `drawCompareChart()` overlaying up to three coloured drop lines with a legend on one canvas — deliberately drop-only rather than trying to also fit velocity/energy on the same chart, which would have cluttered the exact comparison this feature exists to make clear. Verified with a new headless test that seeds three custom presets with different max ranges, confirms the shared-reference-range logic picks the shortest one, confirms per-load results correctly end at that shared range with velocity decreasing downrange as expected, confirms environmental fields are read live rather than baked into a preset, confirms the three-slot cap and deselect/reselect behaviour, and confirms comparing loads across different modes (air + centrefire together) computes without error — plus a full re-run of the existing test suite to confirm nothing else regressed, across all four files.

**2026-08-12 — Coriolis column now follows the selected results-table unit (mils/MOA/clicks/in/cm), matching Wind and Spin Drift (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked to double-check whether Coriolis was expressed in whichever measurement unit the results table is set to, the same way Wind and Spin Drift are. It was not: Wind and Spin Drift each carry four computed variants (in/cm/moa/mils) and the table already picked the right one via `windCol`/`spinCol` based on the unit toggle, but Coriolis only ever had inches and cm, and the table's Coriolis column was hardcoded to `met?coriolisCm:coriolisIn` regardless of the toggle — so switching the whole table to mils or MOA left Coriolis the one column still reading in raw linear inches or cm. This was purely a display gap, not a physics error: the underlying Coriolis magnitude and formula had already been independently verified earlier in this project against first-principles derivation and the user's own reported parameters. Fixed by adding `coriolisMoa`/`coriolisMils` fields to `computeTrajectory()`, computed with the identical angular-conversion formula already used for wind drift and spin drift (linear inches × incline-angle correction ÷ range-scaled MOA-or-mil constant), and adding a `corCol`/`corHdr` selector directly alongside the existing `windCol`/`spinCol` logic so the column and its header now switch together with the rest of the table. Verified with a headless test confirming the new angular fields round-trip back to the same linear inches value (within expected 2dp display rounding) and that the column/header selection correctly switches across all five unit modes, run against all four files.

**2026-08-06 — Added a persistent "BC baked from Truing" badge to the Results page, showing before/after BC values (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the Zero Offset badge: the user asked whether the Results page could also show if a truing correction had been applied to the currently loaded preset, and (once confirmed) whether it could include the before/after BC values. The existing orange "Trued ×N" badge only reflects a *live, in-session* `bcMultiplier` — it disappears the instant a trued load is saved to a preset, since saving folds the correction into the BC number itself and resets the multiplier to 1, so a freshly reloaded preset with real truing history showed no indication of it at all. The data needed to fix this already existed, just unused outside one menu action: every custom preset that's had truing baked in carries a `_preTruingBackup` marker (`{field, value}` — the BC before that first bake), originally added only to power the "Restore pre-truing BC" option in the preset overflow menu. Added a new badge, visually distinct from both the live Trued badge and the blue Zero Offset badge (muted grey, since it's historical/informational rather than an active correction), that checks whether the currently loaded preset is a custom preset carrying that marker — matched by name against `loadedPresetRef` so a moved, renamed, or deleted slot can't show a stale badge — and if so displays "BC baked from Truing: [before] → [after]", reading the after-value straight from the preset's own stored BC/BC7 field. Verified with a new headless test covering three scenarios: no badge for a load with no preset loaded; the badge appearing with the exact correct before/after BC values on a fresh reload of a preset that had truing baked in (with the live Trued badge correctly absent in that same state, since the multiplier resets to 1); and the badge correctly disappearing again after a deliberate manual BC edit and re-save, since that already clears `_preTruingBackup` under existing logic — this test also incidentally confirms that existing clear-on-manual-edit behavior still works correctly. Full existing test suite re-run to confirm nothing else regressed, across all four files.

**2026-08-06 — Added "Zero Offset": a known holdover at the zero range for a specific projectile, independent of BC Truing (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user described a real-world scenario: a rifle is zeroed for one load, the shooter switches to a different projectile without re-zeroing, and already knows from experience how much that new load holds over or under point of aim at the zero range. They asked whether the calculator could take that known figure and shift the whole table accordingly, describing it as similar to Trajectory Truing but always anchored at the zero point. After confirming the scenario and design approach in discussion first, implemented it as a genuinely distinct feature from Truing rather than a variant of it: Truing solves for a BC multiplier so the drag model matches one observed data point, which assumes the zero itself is a true 0-drop reference; Zero Offset does the opposite — it takes the zero as a starting point that's deliberately *not* a true zero, and shifts the entire table by a constant angular amount (mils or MOA) to match a directly known holdover, without touching BC or drag at all. Added a new "Zero Offset (optional)" field next to Spot Range, with its own mils/MOA unit toggle (kept independent from the results table's own unit toggle, so switching how the *results* are displayed can never silently reinterpret what was typed into this field). The correction is computed once in `calculate()` as a single canonical mils-equivalent value, then applied inside `computeTrajectory()` as `dIn -= offsetMils*range*0.036/cosA2` — a term that scales linearly with range (since mils/MOA are angular units, a constant angular holdover is a growing linear one downrange, not a fixed number of inches) and divides through cosA2 so incline angle doesn't distort the requested value. Because it modifies the shared `dIn` calculation, every downstream consumer — the results table, the chart, CSV export, the Range Card view, and the turret-strip graphics — automatically reflects it with no separate changes needed. It's saved and restored per preset alongside zero/max/interval (including correctly handling negative "hold under" values, and resetting to blank rather than leaking between presets, mode switches, or "+ New Load"), and shows a distinct blue "Zero Offset" badge in the results header whenever active, so it's never a silent, invisible correction easily confused with the orange "Trued" badge. Verified with a new headless test suite: confirms the offset reads as a constant angular shift at the zero row and every other range, confirms the linear (inches) columns scale proportionally with range rather than shifting by a flat amount, confirms energy/velocity/windage/spin-drift columns are completely untouched, confirms MOA-to-mils conversion is exact, confirms negative values and preset save/load round-trip correctly (including legacy presets with no saved offset resetting cleanly to blank), confirms mode-switch and "+ New Load" clear it, and confirms Trajectory Truing's own search correctly accounts for an active Zero Offset (since it rebuilds its trial trajectories from the same already-offset `params` object) rather than double-correcting — plus a full re-run of the entire existing test suite to confirm nothing else regressed, across all four files.

**2026-07-30 — Preset export now includes amended built-in presets' "superseded" state, not just the amended data itself (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked whether exported presets could include amended originals, not just additional entries. Investigation (backed by a headless test capturing the actual exported JSON, not just a code read) found the amended values themselves were already in the export — editing a built-in preset saves it as a custom copy under the same name, and `exportPresets()` already includes all custom presets. What was missing was the other half: editing a built-in also hides the original stock entry via a separate `HIDDEN_KEY` set so the amended copy doesn't sit duplicated next to it, and that hidden-state was never part of the exported file. Importing on another install (or after a fresh reload) brought the untouched built-in back, sitting alongside the amended copy — making the amendment look like it hadn't really replaced anything. Added the hidden-built-in index set to `exportPresets()`'s payload, and a new `mergeImportedHidden()` helper (additive-only — never un-hides an index that's already hidden locally for an unrelated reason) that `importPresetsFile()` now applies alongside the existing preset merge. Older export files simply have no `hidden` key, which the merge treats as "nothing new to hide," so importing them behaves exactly as before. Verified with a headless test that edits a built-in, captures the real exported JSON, confirms the hidden index is present, simulates importing that file onto a fresh install and confirms the amended copy is added *and* the original is hidden (no duplicate), and confirms the merge never un-hides an unrelated, already-hidden entry — plus a full re-run of the existing preset/chart test suite across all four files.

**2026-07-30 — Gave the built-in centrefire presets their own Max range (CervidBallistics.html, Personal/index.html).** Follow-up to the interval-propagation fix above: flagged that all five built-in centrefire presets had no Max of their own and so silently shared the app's single 500yd centrefire default. Per the user's direction, added an explicit `max:500` to `.243 Win 100gr` and `max:1000` to all three 6.5 Creedmoor entries (`.264 CM 130gr`, `.264 CM 129gr`, `.264 CM ABLR 129gr`) — the longer figure reflecting 6.5 Creedmoor's common use as an ELR-capable cartridge. `.308 Win 150gr` was left on the shared default, since it wasn't specified. Only applied to CervidBallistics.html and Personal/index.html, since CervidBallistics_GeneralRelease.html and index.html ship with an intentionally empty centrefire preset list. Verified with a syntax check and the full existing preset/chart test suite across all four files.

**2026-07-30 — Fixed Interval appearing to "propagate" between presets (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Immediate follow-up to adding interval persistence below: the user reported that after that change, editing zero/max/interval on one preset seemed to bleed into every other preset. A new headless test seeding two distinct custom presets and editing only one of them confirmed zero and max were never actually shared — each preset's own value round-tripped correctly through storage. Interval was the real culprit: the previous fix deliberately left `fInterval` untouched when loading a preset that had no saved interval of its own (to avoid disturbing legacy presets), but since virtually every existing preset still has no saved interval until re-saved, loading any of them just left whatever interval was last typed in sitting in the field — which looks exactly like "propagation" even though nothing was actually written to another preset's stored data. Fixed by having `loadPreset()` always land the Interval field on a definite state for the preset being loaded: its own saved value if present, or a blank "auto" field if not — mirroring how a preset with no saved Max already falls back to an explicit mode default rather than silently keeping the previous preset's number. Verified with an updated headless test asserting a legacy (no-interval) preset resets the field to blank rather than keeping a leftover value, plus the cross-preset contamination test and the full existing suite, across all four files. Separately, centrefire's five built-in presets currently have no explicit Max of their own at all (only Zero) and so all silently share the app's single centrefire default of 500yd — flagged for the user as a data-content gap distinct from the interval bug, pending a decision on whether to add distinct per-load Max values.

**2026-07-30 — Presets now save and restore their Interval setting, matching Zero and Max (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The user asked whether an edited preset's zero range, max range, and interval could all be relied on to save rather than reverting. Investigation (backed by a new headless save/reload test, not just a code read) confirmed zero and max were already round-tripping correctly through `buildPresetEntryFromForm()` → `localStorage` → `loadPreset()` with no bug. Interval, however, had never been part of the saved preset schema at all — it was always read fresh from the standalone `fInterval` field at calculation time, entirely decoupled from any preset, so a value typed in for one load wouldn't travel with that preset the way zero/max did. Added `interval` to `buildPresetEntryFromForm()` (only stamped onto the entry when the field holds a real value greater than 0, so a preset left on "auto" doesn't get pinned to a stray 0) and added the matching restore step to `loadPreset()`, with the same imperial/metric conversion zero and max already use. Verified with a new headless test covering the full save → storage → reload round-trip for zero/max/interval together, and a full re-run of the existing preset/chart test suite to confirm nothing else regressed, across all four files. (The initial legacy-preset handling in this entry was superseded by the fix directly above.)

**2026-07-21 — Fixed edited presets silently reverting on reload — Save button could scroll out of view (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** A user on Mac Safari reported that editing a custom preset (e.g. changing a field value) appeared to work, but reverted to the old data after restarting the app — and separately, that the on-screen instruction "Save will update it. Tap 'cancel' to save changes as a new preset instead." made no sense because no Save or cancel control was visible. Diagnosis: these were the same bug. The sidebar scrolls independently from the rest of the page, and the Save/Update button plus the "cancel" link are a fixed row directly below the preset grid — with enough custom presets to make that grid several rows tall, entering edit mode could leave the Save button scrolled just out of view, with no cue that it existed. The edit only ever lived in the on-screen form and that session's calculation; since Save was never (visibly) clicked, nothing was written to `localStorage`, so a reload correctly reloaded the last version that *was* actually saved — indistinguishable from "my edit disappeared" even though nothing was silently lost. Added a `scrollSaveRowIntoView()` helper (`#presetSaveBtn.scrollIntoView({block:'nearest',behavior:'smooth'})`) and called it from both places edit mode gets armed: the plain tap-to-edit on a custom preset, and the built-in preset's "Edit" row in the overflow menu — so the control you need to click is always brought on-screen the moment it becomes relevant, regardless of how long the preset list has grown. Verified with a new headless test asserting `scrollIntoView` is called exactly once from each entry point (with `block:'nearest'` so it doesn't over-scroll), plus a full re-run of the existing preset/touch/mobile-tab/overflow-menu test suite to confirm nothing else regressed, across all four files.

**2026-07-21 — Dropped the copyright notice down two more lines (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Quick follow-up: added two extra line breaks ahead of "© Copyright 2026" in the empty-state placeholder, pushing the notice further down from the main caption above it. Updated in both places the placeholder is built across all four files; verified with a syntax check and a direct search confirming the extra spacing landed in both occurrences of each file.

**2026-07-21 — Added a copyright line to the empty-state placeholder (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Added a small, two-line copyright notice ("© Copyright 2026" / "Simon J. Gibson, CBE") directly below the "Select a Quick Preset or configure your load…" caption on the Results page's empty state, set at 11px so it reads as a subtle footnote rather than competing with the main caption. Updated in both places the placeholder is built (initial markup and the JS mode-switch reset string) across all four files; verified with a syntax check and a direct search confirming exactly two occurrences landed in each file.

**2026-07-21 — Trimmed the empty-state logo back 10% and reworded its caption (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Quick follow-up to the doubling above: brought `cervid-icon-square.jpg` down from 192×192 to 173×173px (a 10% reduction), and changed the caption from "Configure your load and tap **Calculate Trajectory**" to "Select a Quick Preset or configure your load, then tap **Calculate Trajectory**" — since a preset can also get someone straight to a result, not just manual field entry, and "Quick Preset" was clarified as the exact wording to avoid any ambiguity with other preset-related UI. Updated in both places the placeholder is built (the page's initial markup and the JS string that rebuilds it on mode switch) across all four files; verified with a syntax check and a direct search confirming no stale 192px sizing or old wording was left behind anywhere.

**2026-07-21 — Doubled the empty-state logo icon on the Results page (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The "Configure your load and tap Calculate Trajectory" placeholder shown before a calculation has run has a generous amount of empty space around it, and the user pointed out the logo sitting there was a free opportunity for brand visibility. Bumped the `cervid-icon-square.jpg` image from 96×96 to 192×192px in both places it's set inline (the page's initial markup, and the JS string that rebuilds the same placeholder whenever the mode is switched with no results yet) — kept as a simple 2× scale-up of the existing square, rounded-corner treatment rather than a redesign. Verified all four files stay syntactically valid and that no stale 96px references were left behind.

**2026-07-21 — Fixed the green energy line rendering as a broken/stepped dashed line (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to adding the right-hand energy axis below: the user reported the green line itself now showed visible "steps," reminiscent of the drop line's own smoothing bug from earlier in the project. Investigated the data itself first — computeTrajectory()'s dense per-yard pass (added for that earlier drop-line fix) already feeds identical, fully monotonic, smooth data to both lines, confirmed with a headless test that directly checked for flat/repeated consecutive values in realistic airgun and long-range centrefire scenarios and found none, even before this fix. So the underlying numbers were never the problem. The real cause was rendering, not data: with a dense per-yard point set, a long-range chart (e.g. 1000yd) packs far more vertices into the plotted line than there are horizontal pixels (sub-pixel spacing between points) — invisible for the drop line's solid stroke, but the energy line is deliberately dashed (`[3,3]`) as part of the earlier colour-only-signalling accessibility fix, and a dashed stroke has to decide, pixel by pixel, where each on/off segment falls; with vertices closer together than a device pixel, that interacts badly with the dash pattern and renders as an uneven, broken line instead of a smooth dashed one. Fixed by thinning the energy line's own path to roughly one point per horizontal pixel before stroking it (always keeping the true final point so the line still reaches the actual last value) and adding round line joins/caps for extra smoothness — the drop line, the axis math, and the underlying trajectory data are all untouched; a short-range chart with already-few points is left exactly as it was. Verified with a new headless test asserting the energy line's drawn point count actually drops for a long-range/high-density scenario, that the downsampled line still ends exactly at the true final data point, that round joins/caps are applied, and that a short-range dataset (already under one point per pixel) isn't touched at all — plus a full re-run of the existing chart-axis and preset test suites to confirm nothing else regressed, across all four files.

**2026-07-21 — Added a right-hand axis showing energy values on the trajectory chart (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The chart already had drop-in-inches/cm numbers running down the left edge, tied to the blue drop line, but the green energy line had no equivalent numeric scale — just a color-coded legend label at the top. Added a mirrored column of numbers down the right edge, in the same green as the energy line, at the same 5 horizontal gridline heights the drop axis already uses. Since the energy line is deliberately drawn at 80% of the plot's height (so it has headroom and doesn't visually collide with the header labels), the right-axis numbers are computed by inverting that same scale — i.e. each number is exactly the energy value that would put the green line at that pixel height, so the two stay visually consistent. This required widening the right-hand margin of the plot area from 32px to 40px (the left margin is unchanged) to leave room for energy figures, which run to 3-4 digits; the "Energy(ft-lb)"/"Energy(J)" header label (now including its unit, matching the "Drop(in)"/"Drop(cm)" label on the left) was also switched to right-align against the canvas edge rather than a fixed offset, so it can't get clipped regardless of how long the unit suffix is. Verified with a new headless harness that renders the chart against a fake canvas recording every text draw call, across both imperial and metric units and both a wide (600px) and a narrow (340px) canvas, confirming exactly 5 right-axis labels appear, their values match the inverse-scale formula, none are positioned close enough to the edge to clip, and the existing left-axis drop labels are unmoved — plus a full re-run of the existing preset-menu/touch/mobile-tab test suite to confirm nothing else regressed, across all four files.

**2026-07-21 — Replaced the per-preset edit/delete/restore buttons with a single "⋯" overflow menu (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Prompted by a design question: would a pull-down menu in the preset section work better and save space? A true native `<select>` isn't a good fit here (iOS Safari takes it over as a full-screen system picker rather than a lightweight popover), so the real question was whether moving actions into a custom popover was worth trading an extra tap for less visual clutter. Tapping a custom preset already loads it straight into edit mode (since the edit-in-place fix earlier in this changelog), which made the standalone pencil-edit button redundant for custom presets — so rather than fold every action into a menu (which would have cost the one-tap edit its whole reason for existing), only the less-frequent actions were consolidated: Delete and Restore for your own presets, and Edit and Hide for built-ins, each now living behind one small "⋯" toggle per tile instead of two or three separate icons. This also let the tile shrink back down: since there's only one corner button now, `.preset-btn` moved from a tall 30px top padding (reserved for a row of icons) to a slim 34px *right* padding (reserved for just the one), pulling tile height back close to its pre-icon-era size — the actual "save space" win. A new document-level click-away listener closes an open menu when the user taps elsewhere, following the same pattern already used for the help-tip popover; every row/toggle click inside the menu calls `stopPropagation()`, so the two listeners never fight each other. The existing touch-drag guard (`closest('button')!==btn`) needed no changes, since it already excludes any nested `<button>` other than the tile itself, generically. Verified with the full existing test suite (syntax checks, edit-in-place, touch-reorder, and mobile-tab harnesses) plus a new harness that opens/closes the menu via its toggle, confirms the correct rows appear for custom vs. built-in presets, confirms Delete actually removes a preset and closes the menu, and confirms a simulated click elsewhere closes an open menu — across all four files. (The General Release/`index.html` deployment variant ships with no built-in presets by design, so the built-in-menu-rows check is skipped there rather than failing.)

**2026-07-21 — Fixed editing a preset silently jumping to the Results tab on iPhone (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** After the touch-target fix above, the pencil-edit button turned out to actually be working correctly all along — `loadPreset()` was populating the fields and arming edit mode exactly as intended — but `loadPreset()` unconditionally calls `calculate()`, which on narrow screens (`window.innerWidth<=900`) auto-switches the mobile view to the Results tab. That's the right behavior for an ordinary tap-to-load, but wrong for editing: the name field and Save/Update button the edit flow just armed live on the Inputs tab, so the user got bounced straight past them to a Results screen with no visible sign anything had happened — indistinguishable from the button doing nothing. Added `if(window.innerWidth<=900)setMobileTab('inputs');` right after `loadPreset()` in all three edit-entry points (the plain tap on a custom preset, and both pencil-edit handlers), so entering edit mode now keeps — or returns to — the Inputs tab where the edit controls are actually visible. Verified with a new headless test that simulates a mobile viewport already showing Results, taps a preset, and confirms the Inputs panel becomes visible again, across all four files.

**2026-07-21 — Fixed the pencil-edit button breaking again on iPhone after the icon-only restyle (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The touch-drag guard added earlier (`if(e.target.tagName==='BUTTON'&&e.target!==btn)return;`) worked when the edit button had a visible colored box, since a tap anywhere in that box landed directly on the `<button>` element. Once it became icon-only, people naturally aim at the small visible SVG icon itself — and a tap landing on that `<svg>` or its `<path>` has a target whose `tagName` is `svg`/`path`, not `BUTTON`, so the same-tagName check silently failed to match and the drag-hijack bug came back specifically for icon-only buttons. Replaced the check with `e.target.closest('button')!==btn`, which walks up the ancestor chain from wherever the touch actually landed — through any number of SVG/path layers — to find the real button, regardless of how deep the tap landed inside its icon. The earlier verification missed this because it only ever simulated a touch whose target was the button itself, never a nested descendant; extended the harness with a new scenario that specifically simulates a touch on a fake `<svg><path>` nested inside the edit button (mimicking exactly what a real tap on the icon produces) and confirmed the drag timer no longer arms in that case, alongside all previously-passing scenarios, across all four files.

**2026-07-21 — Trimmed the preset tile's reserved top padding to shrink tiles slightly (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the icon-only restyle: with the buttons now visually light, the 36px top padding reserved for them (from when they were solid colored boxes) looked like more whitespace than necessary. Trimmed to 30px, pulling the name/weight/velocity text up and shrinking each tile a little, while still leaving a small buffer past the buttons' actual 32px tap footprint (4px top offset + 28px height) — their hit area is still full-size even though it's invisible at rest, so text sitting flush against that edge could still occasionally steal a tap meant for a button. Re-verified with the full existing test suite across all four files.

**2026-07-21 — Restyled the preset edit/delete/restore buttons to icon-only (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** The 28px enlargement above fixed iPhone tap reliability but left three solid colored boxes sitting on every preset tile, which read as visually heavy. Presented the tradeoff and went with the user's preferred option: keep the same 28px tap area (still needed for reliable iPhone hits) but drop the persistent background/border chrome entirely — each button is now just a small icon (pencil 13px, delete/restore glyphs 13-14px) with a faint background tint that only appears on `:active` (press) as feedback, so the tiles read as clean at rest while the actions stay exactly as reliable to tap as before. The reserved 36px top padding on `.preset-btn` was left unchanged, since the actual invisible hit-box is still the full 28px regardless of how little of it is visibly styled — shrinking that space back down would let the buttons' now-invisible edges silently steal taps intended for the tile itself. Re-verified with the full existing test suite (syntax checks, edit-in-place, and touch-reorder harnesses) across all four files.

**2026-07-21 — Fixed the enlarged preset buttons overlapping the tile's own text (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Immediate follow-up to the 28px button enlargement below: only the buttons themselves grew, so the tile's normal-flow content (the bold preset name plus weight/velocity line) still started at the original 7px padding and ended up covered. Rather than shrink the buttons back down, `.preset-btn`'s top padding was increased to 36px (just past the button row's 4px+28px extent), pushing the text down to sit cleanly below the buttons instead; side and bottom padding are unchanged. Re-verified with the full existing test suite (syntax checks, edit-in-place, and touch-reorder harnesses) across all four files.

**2026-07-21 — Enlarged the preset edit/delete/restore buttons and replaced the pencil emoji with an SVG icon (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Two related reports: the pencil-edit icon rendered as a color emoji (yellow) on iOS but a hard-to-see plain glyph (tinted blue by the button's own CSS color) on the Mac, since emoji color presentation isn't consistent across platforms — the exact same category of problem the app's main icon hit long ago, fixed the same way: `✏` is now a small inline SVG pencil drawn with `stroke="currentColor"`, so it renders identically (and legibly) everywhere. Separately, the pencil-edit button worked on iPad but did nothing on iPhone — traced to `.preset-edit`/`.preset-del`/`.preset-restore` being only 18x18 CSS px with ~2px between them, well under Apple's own 44pt-minimum touch-target guidance; an imprecise tap (more likely one-handed on a smaller iPhone than a steadier iPad) could land just outside the button and hit the tile underneath instead, which — since the tile handles drag-to-reorder — could swallow the tap as the start of a drag rather than a click on the button. All three buttons are now 28x28 with a 4px gap, a middle ground that's meaningfully easier to hit without visually dominating a small preset tile. Verified with `node --check`-equivalent syntax checks plus a full re-run of the existing edit-in-place and touch-reorder harnesses (which look up buttons by their `preset-edit` class name) across all four files to confirm the enlarged/re-iconed buttons didn't break anything already tested.

**2026-07-21 — Added a status line clarifying custom-vs-built-in preset editing state (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up to the edit-in-place fix: the user asked why the name field sometimes pre-fills after selecting a preset and sometimes shows the "Name this load…" placeholder — this was already correct-by-design (pre-filled = one of your own presets, ready to update in place; blank = a built-in, browse-only until the pencil icon is used) but wasn't visually obvious. Added a small `#presetEditStatus` line under the save row that now spells this out explicitly: `Editing your preset "X" — Save will update it` for your own presets (from either the plain tap or the pencil icon), `Built-in preset — tap ✏ to edit it, or type a name and Save to create your own copy` when browsing a built-in, and `Editing built-in "X" — Save will create your own copy and hide the original stock preset` once the pencil icon is actually used on one. Cleared automatically by "cancel" and by "+ New Load". While wiring this in, also fixed a small pre-existing gap in `clearLoadFields()` ("+ New Load"): it cleared the name field but never reset `editingCustomIdx`/`editingBuiltinIdx` or the Save button's label back to "+ Save", so starting a fresh load right after editing a preset could leave the button stuck reading "✓ Update" — it now fully resets alongside the new status line. Verified with the same headless harness (syntax checks plus re-running the existing edit-in-place and touch-reorder test suites) across all four files to confirm nothing in `cancelPresetEdit()`/`clearLoadFields()` regressed.

**2026-07-21 — Fixed the pencil-edit button doing nothing on iOS (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Follow-up bug from the touch-reordering fix above: since the touch handlers are attached to the whole preset tile, a tap on the small nested pencil/delete/restore buttons still bubbles up to them — and a careful tap on a tiny target (exactly the scenario the user hit) can easily linger past the 350ms long-press threshold, which then armed drag mode on the *entire tile* and swallowed the click those buttons needed. Added a guard at the top of the touchstart handler that skips arming the drag timer whenever the touch actually started on a nested `<button>` other than the tile itself, so those icons get an ordinary, immediate tap again regardless of how long the touch lingers. Verified with an extended version of the same headless harness: a simulated touch with its target set to the nested edit button no longer arms the timer, while a touch on the tile body still does, confirmed identically across all four files.

**2026-07-21 — Preset editing now overwrites in place, and manual preset reordering works on iOS (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Two fixes to the preset grid in `renderPresets()`:

Edit-in-place: previously, loading a custom preset by tapping it (as opposed to the small pencil icon) left the app in "new preset" mode, so tweaking a value — e.g. after Trajectory Truing — and hitting Save created a duplicate rather than updating the preset you'd loaded. The plain-click handler now sets `editingCustomIdx` and pre-fills the name field the same way the pencil icon already did, so an ordinary load-tweak-save cycle amends that same preset. Hit "cancel" first (already-existing UI) to detach and save your changes as a genuinely new preset instead. Built-in presets are deliberately left out of this — they still require the pencil icon before editing, since overwriting one also hides the original stock entry, a bigger and less-reversible step than updating your own preset.

Touch reordering: manual drag-to-reorder was built entirely on the native HTML5 drag-and-drop API (`draggable`, `dragstart`/`dragover`/`drop`), which iOS Safari has never reliably fired for ordinary elements like these preset buttons — it works there mainly for links, images, and text selections — so dragging with a finger silently did nothing on iPhone/iPad even though it worked fine with a mouse on the Mac. Added a parallel touch implementation using a ~350ms press-and-hold to enter drag mode (the same disambiguation pattern iOS's own home-screen icon rearrangement uses), so a quick tap or an ordinary scroll that starts on a preset button isn't hijacked. Once in drag mode, `document.elementFromPoint` finds whichever button is currently under the finger (layout-agnostic, works regardless of how the grid wraps) and reuses the exact same `getOrderedPresets()`/`persistOrder()` splice logic the native drop handler already used, so both entry points commit through one code path.

Verified both changes with a headless Node harness that loads each file's actual `<script>` block into a `vm` context with a minimal fake DOM/localStorage (no jsdom available in this sandbox — network access to npm is blocked) and drives the real functions directly: simulated a load-tweak-save cycle confirming no duplicate is created and the update lands; confirmed cancel+rename still produces a genuine second preset; simulated a short/small touch gesture confirming it does *not* reorder; simulated a full press-and-hold-then-drag gesture confirming it does, and that the resulting order is persisted correctly. All four production files passed identically.

**2026-07-14 — Added an iOS "Add to Home Screen" icon using the header photo (CervidBallistics.html, CervidBallistics_GeneralRelease.html, index.html, Personal/index.html).** Previously there was no `apple-touch-icon` link tag anywhere, so iOS Safari fell back to auto-generating a plain icon from the first letter of `apple-mobile-web-app-title` ("Cervid" → a "C"). Added `<link rel="apple-touch-icon" href="cervid-icon-square.jpg">` (plus a matching `<link rel="icon">` for browser tabs/bookmarks) to each file's `<head>`, reusing the same photo already used for the header thumbnail and empty-state image — no new asset needed. iOS applies its own corner-rounding mask automatically, so the square source file works as-is.

Noted in passing: `CervidBallistics_SimpleMode.html` was found missing from the project folder (deleted outside of any edit made here, still shown as an uncommitted deletion in git). Asked the user whether to restore it before adding the icon; they chose to leave it deleted, so it was excluded from this change.

**2026-07-14 — Replaced the empty-state SVG glyph with the header photo, then enlarged it 50% (all three files + two deployment copies).** The blank "Configure your load and tap Calculate Trajectory" placeholder shown before a first calculation used the small vector bullet/crosshair glyph (`bulletIcon(48)`, plus an identical hardcoded copy in the initial page markup for the pre-JS state). Both spots now use `cervid-icon-square.jpg` instead — a rounded `<img>`, initially 64px to match the header thumbnail, then sized up to 96px (border-radius scaled 10px→12px to match) per follow-up request since this spot is a standalone illustration rather than a small inline badge. No new asset needed since the file is already shipped alongside the HTML for the header; `bulletIcon()` itself is untouched and still used elsewhere (Trued badge, Reticle View button, BC Truing card), as is the header thumbnail's own `.headerImg` CSS class (still 64px, unaffected by this change).

**2026-07-14 — Adopted the Side Image Header design into all three production apps, retiring the banner-behind-title header; removed the `/SideImageHeader/` GitHub Pages test folder.** After the comparison variant went live for mobile testing (see the two entries below), the side-thumbnail design was preferred and folded directly into `CervidBallistics.html`, `CervidBallistics_SimpleMode.html`, and `CervidBallistics_GeneralRelease.html` (plus their two deployment copies, `index.html` and `Personal/index.html`) — replacing the full-width `cervid-banner-crop.jpg` background header with the small square `cervid-icon-square.jpg` thumbnail beside the title/subtitle. Same swap as the comparison variant: header CSS changed to a flex row with a 64px (48px on narrow screens) rounded thumbnail, and the landscape-specific crop media query was removed entirely since the thumbnail sizes itself naturally with the header's ordinary padding — no `background-size:cover` math needed.

Updated `install.sh` and `install_general_release.sh` to copy `cervid-icon-square.jpg` (renamed `SOURCE_ICON`, was `SOURCE_BANNER`) alongside the HTML instead of the old banner crop, and `github_push.sh`'s deployment-copy sync to refresh `Personal/cervid-icon-square.jpg` from the root copy on every push. `cervid-banner-crop.jpg`, `Personal/cervid-banner-crop.jpg`, and `cervid-banner.png` (the original source illustration) are no longer referenced by any production file — left in place as unused/orphaned assets rather than deleted, since removal wasn't requested. The `SideImageHeader/` GitHub Pages folder (`index.html` + `cervid-icon-square.jpg`) was deleted outright, along with its sync block in `github_push.sh`, since the design no longer needs a separate live A/B path now that it *is* the production header. `CervidBallistics_SideImageHeader.html` and its dedicated install scripts (`install_side_image_header.sh`, `add_to_dock_side_image_header.sh`) are now fully redundant with the main app but were left untouched — not part of this request.

**2026-07-14 — Deployed the Side Image Header comparison variant to GitHub Pages, at `/SideImageHeader/`, for mobile testing.** New `SideImageHeader/` folder in the repo, with `index.html` (a deployment copy of `CervidBallistics_SideImageHeader.html`) and its companion `cervid-icon-square.jpg`, following the same pattern as the existing root and `Personal/` deployment copies. `github_push.sh`'s Step 1 sync now also refreshes this pair on every push, so — same as the other two live copies — an edit to the source file can't go stale on the live site just because it wasn't manually re-copied first. Verified the deployed copy's HTML and image are byte-identical to their sources via checksum before pushing. Once pushed, reachable at `mybc.cervid.net/SideImageHeader/`. Still deliberately left out of `update_all.sh` and `add_to_dock_all.sh` (the local-install scripts) — this only wires up the live-preview path, not the "official third app" status.

**2026-07-14 — Created a "Side Image Header" comparison variant with its own install script and Dock launcher.** Requested design comparison: instead of the full-width banner-behind-title header used by `CervidBallistics.html`, a small square photo sits beside the title/subtitle. `CervidBallistics_SideImageHeader.html` is a full copy of the main app with only the header CSS/markup changed — the `cervidMetalGrad` gradient defs and all the small inline `bulletIcon()` glyphs elsewhere in the app (section headers, badges, buttons) are untouched, since this comparison is scoped to the header banner only. Its companion image, `cervid-icon-square.jpg` (320x320, cropped to remove the white/black frame border visible in the original artwork), is referenced by relative path the same way `cervid-banner-crop.jpg` is by the main app.

Because the side-thumbnail sizes itself naturally with the header's ordinary padding (no `background-size:cover` math needed), this variant doesn't need the landscape-specific height/crop-position media query the banner version required — that rule was removed rather than carried over, with a comment explaining why.

Set up as a fully separate, installable app rather than just a loose file: `install_side_image_header.sh` (installs to `~/Documents/Cervid Ballistics (Side Image Header)/`, copying the companion image alongside per the established pattern, and creates `~/Applications/Cervid Ballistics (Side Image Header).app`) and `add_to_dock_side_image_header.sh`, both modelled directly on the General Release variant's scripts. Deliberately left out of `update_all.sh`, `add_to_dock_all.sh`, and `github_push.sh` — this is a comparison build, not a third production app, so it stays install-it-yourself rather than getting swept up in the "update everything" / "push everything live" flows until a direction is chosen.

**2026-07-14 — Fixed the mobile-landscape banner crop cutting into the top of the bullet (all five HTML files).** The first landscape fix (below) shrank the header to 56px and pushed the focus point to 62%, and the user reported this now clipped the bullet itself rather than just the mountains above it. Root cause, confirmed by overlaying a pixel ruler on `cervid-banner-crop.jpg`: the bullet's true vertical span in the 451px-tall source is y=170-340 (37-75%), well below where the pushed-too-far 62% anchor put the (also too-short) visible window — at that combination the window's own top edge landed inside the bullet rather than above it. Recalculated properly this time: a more moderate height cut (92px to 74px, versus the original's overcorrected 56px) paired with a small nudge of the anchor (45% to 47%, versus the overcorrected 62%) keeps the visible window within the bullet's own band across the realistic range of phone landscape widths (roughly 650-950px), so the crop comes out of the mountain/sky above the bullet instead. Also caught and fixed a copy-paste slip where the equivalent edit to `index.html` had only updated its explanatory comment and left the old broken CSS rule in place underneath it — a reminder to verify the actual rule text per file, not just assume a matching comment means the code changed too.

**2026-07-14 — Shortened the header banner on mobile landscape, and brightened the subtitle text (all five HTML files).** Two quick follow-up fixes after the banner shipped: the "Air Rifle, Rimfire & Centrefire" subtitle used `var(--muted)` (a grey), which read poorly against the busy banner image despite the text-shadow, so its color moved to `var(--text)` (the app's near-white) — a one-line change since the subtitle is its own `<span>`, independent of the banner background. Separately, on phones held in landscape the header ate a noticeable share of the already-short viewport height, so a new `@media(max-height:500px) and (orientation:landscape)` rule reduced the header's `min-height` and vertical padding and shifted the banner's `background-position` further down the source image, intended to keep the bullet fully in frame while cropping the mountains above it — see the correction above, this first pass overcorrected and clipped the bullet instead.

**2026-07-14 — Added a full-illustration header banner behind the app title (all three files, install scripts, and both deployment copies).** Follow-on from the icon redesign below: the user's original illustrated artwork (mountains, trajectory arc, dashed reticle lines, chrome-shaded bullet) has real detail that only reads at a decent size, so rather than force it into a ~20px inline icon, it's now used once, large, as a background behind the header title — the simplified `bulletIcon()` SVG glyph from the entry below stays as-is for all the small repeated inline spots. Presented the sizing/cropping tradeoff to the user directly (mobile header space is scarce) and went with their preferred option: a short wide crop centred on the bullet and crosshair, used as a `background-image` behind the title text with a dark gradient overlay (`rgba(10,12,18,.45)` to `.72`) and `text-shadow` on the title/subtitle so they stay legible regardless of what's under them.

Implemented as a companion JPEG (`cervid-banner-crop.jpg`, cropped from the original square artwork to trim its white frame and an unneeded sky/dashed-crosshair margin, then re-encoded at quality 85 — 1.6MB down to ~60KB) rather than inlining it as base64. Inlining was tried first to preserve the project's normal single-file-per-app convention, but the base64 payload alone tokenizes at roughly 1:1 (not the ~4:1 ratio typical of prose), making it impractically expensive to shuttle through the available editing tools across three source files. Using a small external asset is the pragmatic tradeoff: it keeps every HTML file's own size unchanged and lets the banner be cached separately by the browser, at the cost of needing the image copied alongside the HTML wherever the HTML itself gets copied — so `install.sh`, `install_general_release.sh`, and `github_push.sh`'s deployment-copy sync step were all updated to carry `cervid-banner-crop.jpg` along automatically, the same way they already keep the HTML copies themselves in sync, rather than leaving that as a manual step someone could forget.

Verified via syntax checks on all five HTML files (three source, `index.html`, `Personal/index.html`), a grep confirming exactly one `cervid-banner-crop.jpg` reference per file, and a rendered preview of the actual header markup/CSS to check crop framing and text contrast before shipping.

**2026-07-14 — Replaced the target-and-dart emoji icon with a custom bullet + scope-crosshair SVG, used in the header and throughout the results page (all three files, re-synced deployment copies).** Requested change: the target/dart emoji (&#127919;) is a generic, widely-used social-media icon with no connection to the app, so it doesn't read as a distinctive mark. Iterated the replacement design live with the user across several rounds — flat outline, then a shaded/gradient version for depth, an angled orientation, a "punchier" highlight variant (rejected, reverted), before landing on the final: a horizontal bullet with a soft metallic gradient (dark &rarr; bright &rarr; mid &rarr; dark) and two dark cannelure rings, crossed by a scope-style crosshair (not a bullseye/target-ring) with tick marks, no mountains or landscape elements to keep it legible at small sizes.

Implemented as inline SVG rather than a raster image or extra font/icon dependency, keeping the single-file architecture intact. A single hidden `<svg><defs>` block holding the shared `cervidMetalGrad` linear gradient was added once, right after `<body>` opens, so every icon instance can reference `url(#cervidMetalGrad)` without redefining gradient stops per instance. The three icon occurrences that live in static HTML (header title, "Trajectory Truing" section heading, initial empty-state icon) got the SVG markup inlined directly; the four occurrences built dynamically in JS (empty-state re-render on mode switch, "Trued" badge, "Reticle View" button, "BC Truing" stat card) now call a new `bulletIcon(px)` helper that returns the same markup at whatever pixel size the context needs (48px for the large empty-state icon, ~20px for the header, 11-12px for inline badges/labels/buttons).

Verified via a Node syntax check on each file's extracted script, plus structural checks confirming zero remaining `&#127919;` references, exactly one `cervidMetalGrad` gradient definition, and five `bulletIcon(` occurrences (one function definition plus four call sites) in each of the three source files. Re-synced `index.html`/`Personal/index.html` from the updated `CervidBallistics_GeneralRelease.html`/`CervidBallistics.html`.

**2026-07-12 — Fixed the turret strip constantly showing MAX for realistic values (all three files, re-synced deployment copies).** Reported bug (screenshot): a modest 1.10 mil elevation dial already pegged the strip at its MAX edge. Root cause: the strip's visible range had been derived directly from click size (10 clicks &times; 0.1 mil = only &plusmn;1 mil total shown), when click size and total dial travel are unrelated in practice — a 0.1mil-click turret can easily need 10+ mils of real travel, so almost any realistic correction overflowed a range that small.

Decoupled the two: click size still governs the +/- stepper increment and the click-count readout, but the strip's visible range now auto-picks the smallest "nice" value from a candidate list (1/2/5/10/20/40 mils, or 5/10/20/50/100/200 MOA) that comfortably fits the current value with headroom, independently per axis (elevation and windage often differ a lot in magnitude). Added a small "scale &plusmn;X" note next to each strip's value/click-count readout so the current scale is never ambiguous. The MAX clamp still exists as a backstop for genuinely extreme values beyond the largest candidate, but no longer fires for ordinary use.

Verified via a 13-check Node/vm harness: the reported 1.10 mil case confirmed to no longer trigger MAX (picks a &plusmn;2 mil scale); a range of small-to-extreme values confirmed to pick sensible candidate ranges on both sides of each 90%-headroom boundary; elevation and windage confirmed to scale independently when their magnitudes differ; and the largest-candidate fallback with MAX still confirmed to fire only for a genuinely out-of-range value. Cross-file structural checks confirmed identical across all three files, with no leftover references to the old click-based ranging.

**2026-07-12 — Added a graphical turret-position readout (elevation and windage strips, plus click-stepper buttons) to the Reticle View's turret dial, alongside the existing numeric fields (all three files, plus re-synced `index.html`/`Personal/index.html`).** Requested addition, prompted by screenshots of three other ballistics apps (Element, an Applied-Ballistics-style app, and a Strelok-style app) that each show turret position graphically rather than as plain numbers. Discussed the approach before building: a fully drag-to-set graphical dial was considered and deliberately set aside for now, since precisely snapping a touch/drag gesture to an exact click increment is easy to get wrong on mobile for a number that's meant to represent something dialed by hand on a real turret — going with a readout-only strip first, with the plan to try full drag interaction (or revert) if the readout version doesn't feel right in use.

Two new horizontal ruler-strip SVGs render under the existing Elevation/Windage inputs — one per axis, labelled U/D and R/L respectively, with tick marks spaced to a "click" size and a marker showing the current dialed position (turning orange with a "MAX" label if the value runs off the visible range rather than silently clipping). Click size follows the unit already in use: MOA reuses the app's existing configurable `clickSizeMoa` (since real MOA turrets commonly vary between 1/4, 1/2, and 1 MOA per click), while mil patterns use a fixed 0.1 mil default (mil turrets are near-universally 0.1 mil/click in practice, so there's no equivalent selector for it elsewhere in the app). New +/- stepper buttons flank each numeric input, sized to exactly one click in whichever unit is currently active, and route through the same `onTurretInputChange()` path a manual edit already used — so typing, stepping, and the strip's readout can never disagree about what a "click" is. Deliberately left out any modeling of turret revolutions/rev-counters (common on real elevation turrets for large corrections), consistent with the app's existing stance of staying generic rather than replicating specific manufacturer hardware.

Verified via a 14-check Node/vm harness extracted from all three files: zero value centers the marker exactly; values at the strip's +/-range land exactly on its edges; off-range values clamp and show the MAX label; click-count math checked in both directions (a mil turret value displayed under a MOA pattern, and vice versa) against hand-calculated expected click counts; both strips' direction labels (U/D, R/L) confirmed present. Cross-file structural checks (duplicate ids, function/call-site counts) confirmed identical across all three files. Also re-copied the updated `CervidBallistics_GeneralRelease.html`/`CervidBallistics.html` into the site's actual deployment files (`index.html`/`Personal/index.html`) as part of this change, per the sync step added to `github_push.sh` after the GA drag model update below went out without it.

**2026-07-12 — Added GA, a diabolo-pellet-specific drag model, as a third Drag Model option alongside G1/G7 (all three files).** Followed on from an earlier wind-calculation investigation (see the wind-clock entries below): a centrefire preset checked against Applied Ballistics matched to within 0.01 MOA, but an air rifle preset showed a real ~7% gap against three independent airgun calculators, narrowing the cause down to G1 being a poor aerodynamic fit for diabolo pellets specifically rather than any bug in this app's math. Researched what the airgun ballistics community actually does about this: ChairGun's developers hit the same problem years ago and derived a named replacement drag table, "GA" ("airgun"), now also supported by Strelok Pro and widely treated as the de facto standard for diabolo pellets. The Cd-vs-Mach table now embedded in this app is the one shared by users extracting it from those apps' own data exports — a real, named, widely-used community model, but not an officially published spec sheet from Hawke/ChairGun, so it's documented in-code as the best available reference rather than an authoritative source.

Implementation reuses the existing table-driven architecture (`getCd(mach, tbl)` already interpolates any Mach→Cd table) rather than adding new machinery: `tbl` selection became a three-way ternary, and GA reuses the same primary BC field G1 already uses (both are "the BC number for whichever model is selected," unlike G7 which has its own optional field) — the only UI wrinkle was that the BC field's label was hardcoded to always say "(G1)," so it's now synced dynamically (`syncBcPrimaryLabel()`) to read "(GA)" when that model is active, called from every place the drag model can change (dropdown, preset load, mode switch, "+ New Load", and page-load preference restore).

Air mode's default now nudges toward GA instead of G1 for any load started from scratch, since that's the better fit for what most air rifle presets actually are; rimfire and centrefire still default to G1. Deliberately did *not* retag the eight existing built-in air rifle presets from G1 to GA, even though they're all diabolo pellets: their published BC figures were entered as G1-referenced numbers, and GA's Cd curve differs enough from G1's (lower through the subsonic band, sharply higher through transonic) that pairing an unchanged G1 BC with the GA curve would silently introduce a new, unverified error while chasing the original one — the same "don't curve-fit to a single data point" reasoning already applied during the wind investigation. GA is available for anyone who has a genuinely GA-referenced or pellet-specific BC (e.g. from ChairGun's own calculator, a manufacturer's GA-quoted figure, or future multi-point chronograph fitting), without disturbing the presets' existing, verified G1 pairing.

Verified via a Node/vm harness extracting the updated engine from all three files: syntax-checked; confirmed no duplicate ids; confirmed the dropdown carries exactly the three expected options in order; confirmed the BC label id and all six `syncBcPrimaryLabel()` call sites are present; and ran the JSB Exact .177 8.44gr preset through both G1 and GA at identical conditions, confirming GA produces sane, meaningfully different (not NaN, not wildly implausible) drop/drift/velocity figures consistent with the known shape of the GA curve — lower drag through the pellet's normal subsonic operating range, a much steeper transonic rise above it.

**2026-07-12 — Fixed input boxes misaligning across a row whenever one field's label wraps to two lines and its neighbour's doesn't (all three files, extends the Twist fix below).** Follow-up report (screenshot): the Interval field's input sat noticeably higher than Zero Range/Max Range in the same row — a different case of the same underlying problem as the Twist fix, but this time caused by label text length rather than HTML structure. "Zero Range (yd)" and "Max Range (yd)" wrap to two lines at some widths while the shorter "Interval (yd)" stays on one, and since each field's own content just flows top-down independently, that one-line difference in label height was carrying straight through to a one-line offset between the input boxes.

Fixed generally rather than just for this one row: `.row2`/`.row3` now default to `align-items:end` (CSS Grid bottom-alignment), which cancels out any label-height difference within a row automatically, since the only thing that varies between fields sharing a row is the label — everything below it (gap, then input) is identical, so bottom-aligning brings every input back level regardless of how many lines any one label wraps to. This covers every row of this kind in the form, not only the one that happened to get reported, and needs no per-field tuning as label text or screen width changes.

Two rows were excluded from this default and pinned back to `align-items:start` (the previous, correct behaviour) because bottom-aligning them would introduce a new misalignment rather than fix one: Wind Speed/Wind Clock and Latitude/Azimuth each have one field with a trailing `<small>` hint after its input that its row-mate doesn't have, so those two fields are already different heights by design (not by accident), and top-alignment is what keeps their inputs level.

Verified via a syntax check across all three files and a direct count confirming both the default bottom-alignment and the two explicit exceptions are present and identical in all three.

**2026-07-12 — Fixed the Twist 1:X input field being misaligned/pushed down relative to Diameter and Length in the same row (all three files).** Reported bug (screenshot): the Twist field's help-tip "?" icon appeared on its own line between the label and the input box, instead of sitting inline next to the label text like every other help-tip in the app — pushing that field's input down out of alignment with its two row-mates. Root cause: the Twist field's `<span class="help-tip">` was placed as a sibling immediately after the closing `</label>` tag rather than nested inside it; since `.field label{display:block}` and the help-tip span has no width:100% of its own, it fell onto its own line. Every other help-tip in the app (BC(G1), BC(G7), Drag Model, Azimuth, etc.) already nests the span inside the label correctly — this was the one field that didn't.

Fixed by moving the span inside the `<label>` tag, matching the established correct pattern elsewhere. Verified via a syntax check across all three files plus a direct grep confirming zero remaining instances of the buggy `</label><span class="help-tip">` sibling pattern anywhere in any file (ruling out the same mistake existing elsewhere unnoticed).

**2026-07-12 — Added a turret dial to Reticle View, showing a combined "dial + hold" solution across all three reticle patterns (all three files).** Requested addition: a way to enter how much has already been dialed on the scope's elevation and windage turrets, and see the *remaining* hold needed on the reticle for the rest — the way long-range shooters actually work when the full correction is more than they want to dial, or when they've dialed a round number and want to hold the remainder.

Two new inputs (Elevation, Windage) sit under the Reticle Pattern picker, each labelled with the sign convention that matches how turrets are actually marked — elevation positive = dialed up, windage positive = dialed right — rather than reusing the table's internal sign conventions (which are opposite-feeling for windage, as documented in the Reticle View entry below). Canonical storage is always in mils regardless of which pattern is selected, so a dial amount entered while viewing a mil pattern still displays correctly (converted via the standard 1 mrad = 3.43775 MOA factor) if the user then switches to the MOA Hash pattern — the physical dial amount doesn't silently change or reset when switching reticle illustrations.

The two axes needed different sign math, worked out from concrete examples rather than assumed symmetric: elevation's raw table value is already a *correction* ("positive = dial up"), so a matching turret dial subtracts directly from it. Windage's raw table value is a *drift effect* ("positive = drifts right"), and dialing right is the same kind of rightward push on point of aim as the wind itself — so it adds to the raw drift rather than subtracting, and the existing hold-position math (which already flips a positive combined value into a "hold left" instruction) handles the rest unchanged. Checked against worked examples before trusting it: dialing the exact amount needed reduces the remaining reticle hold to zero on both axes; over-dialing flips the remaining hold's direction, as it should.

The reticle graphic and hold-text below the inputs now reflect the *remaining* hold only, with a new "Turret: ..." line above it summarising what's been dialed when the turret fields are non-zero; leaving both at 0 (the default every time the view is opened — the turret dial does not persist between sessions, only while the modal stays open) reproduces the exact old behaviour with no turret line at all. A small "Reset" link clears both fields back to 0 without closing the view.

Verified via a 12-check Node/vm harness across all three files: fully-compensating and over-compensating elevation dials produce the correct remaining value and direction flip; a fully-compensating windage dial correctly zeroes the remaining hold; a value entered in one pattern's unit round-trips correctly through a pattern switch to the other unit and back; and the Reset control correctly zeroes both the canonical values and the displayed hold text. Syntax-checked across all three files.

**2026-07-12 — Fixed the Christmas Tree reticle's mil-depth numbers being covered by its own wind-hold dots, iterating through three positioning schemes before landing on a straight diagonal (all three files).** Reported bug: the numbers marking each 1-mil depth sat directly on top of the widening rows of wind-hold dots. First fix computed each rung's own label position dynamically, just past that rung's own outermost dot — cleared the dots, but looked like a staggered staircase rather than an intentional layout. Changed to a single fixed x for every depth (set by the widest rung's clearance) for a clean straight vertical column — this cleared the dots too, but on request was replaced again with a straight diagonal instead, closer to how real tree reticles typically number their elevation scale (running alongside the tree's own outward taper rather than off to one fixed side).

The diagonal uses the *continuous* half-width value (`n*0.5`, the same figure already used to size each rung's dot spread) rather than the *floored* dot count used for the actual dots. Since the continuous value is always greater than or equal to the floored one at every depth, offsetting the label by that continuous value plus a small fixed margin guarantees it clears the real dots at every single row by construction — not just empirically verified at a few sample points. It also happens to be exactly proportional to depth, so the numbers fall on a perfectly straight line with no separate slope-fitting needed; the line naturally starts near the centerline at the shallow end (row 1, no dots yet) and reaches the same position the old vertical column used at the deepest row (row 12), so the two approaches agree exactly at the point where clearance is tightest.

Verified via a Node harness checking two things directly rather than assuming them from the formula: that consecutive labels' x-positions increase by an exactly constant amount (confirming a straight line, not a curve or stagger), and that no label's bounding box overlaps any dot's bounding circle at any of the 12 depths — both held with zero failures across all three files.

**2026-07-12 — Added a "Christmas Tree" reticle pattern to Reticle View, alongside Mil-Dot and MOA Hash (all three files).** Requested addition: a third reticle option "akin to the Element Optics Theos scope." Researched Element's actual Theos reticles (APR-2D and MPR-1D, both FFP) rather than guessing — real long-range "tree" reticles use a numbered vertical mil scale for elevation with a spread of wind-hold dots that widens the further down (deeper) the mark sits, so a shooter can read both elevation and wind holds off the same reticle without touching a turret. As with the first two patterns, this is deliberately a generic style "in the same broad family," not a reproduction of Element's (or anyone else's) exact proprietary geometry — the `RETICLE_PATTERNS` table entry and its code comment say so explicitly.

Implementation differs from the two existing patterns in one structural way: Mil-Dot and MOA Hash both mirror symmetrically above and below center, but a Christmas Tree reticle doesn't bother with an upward half — holdover is only ever needed for a dropping bullet, never a rising one — so the new branch in `buildReticleSvg()` only draws downward from center. Each 1-mil depth gets a small tick and a number on the centerline, plus a row of wind-hold dots whose half-width grows with depth (`floor(n*0.5)` dots per side), which is what produces the recognisable widening "tree" shape. `span:12` (vs Mil-Dot's 5 and MOA Hash's 20) was chosen to roughly match how far real tree reticles' printed elevation scale actually reaches. The existing marker/off-reticle-clamping math needed no changes — it was already written to be pattern-agnostic (just reads whatever `span`/`pxPerUnit` the selected pattern reports).

Verified via a Node harness (extracted `RETICLE_PATTERNS`/`buildReticleSvg` from all three files): pattern registered with the right unit/span; SVG renders correctly for both a normal in-range hold and an extreme out-of-range one (correctly flagged "OFF RETICLE"); all 12 depth labels confirmed to sit below center, never above; dot count at each depth confirmed to match the expected widening pattern (72 dots total across the full 12-mil span); and Mil-Dot/MOA Hash confirmed unaffected (regression check). Syntax-checked across all three files.

**2026-07-12 — Added a "Reticle View" showing holdover on a reticle graphic instead of just numbers (all three files).** Requested feature: show where to actually hold on the reticle itself — both elevation and windage together, as a marked point — rather than only giving the shooter a numeric mils/MOA value to translate mentally or dial on a turret.

New "🎯 Reticle View" button sits next to Range Card/Export CSV and opens a modal with a range picker (populated from the current calculation's rows, defaulting to the furthest one), a reticle-pattern picker, and a generated SVG reticle with an orange ring marking the calculated hold point. Two generic patterns shipped to start — Mil-Dot (mil) and a simple MOA Hash/witness-mark duplex (MOA), later joined by a Christmas Tree pattern (see changelog entry above) — deliberately not reproductions of any specific manufacturer's reticle (Vortex, Nightforce, Horus, etc. all treat their exact reticle geometry as proprietary); all are broad, long-standing industry-generic styles instead. More patterns can be added later by extending the `RETICLE_PATTERNS` table.

Two design calls worth recording. First, elevation and windage use opposite-feeling sign logic that's easy to get backwards: the app's existing "positive = dial up" convention for elevation means a positive value shows as a mark BELOW center on the reticle (holding a below-center mark on target is what raises the actual point of aim, without touching a turret) — while for windage, the table's existing "positive = drifts right" convention means a positive value shows as a mark to the LEFT of center (holding left compensates for a rightward push). Both are documented in code comments at the point of use, since getting either backwards would put the shooter's hold exactly wrong rather than just imprecise. Second, reticle subtensions are only physically accurate at one magnification unless the scope is first-focal-plane — rather than adding a required "what magnification are you at" input, the view just states that assumption in a note under the reticle rather than forcing another field into the flow.

If a calculated hold exceeds what the reticle pattern actually shows (5 mils / 20 MOA from center — roughly matching how far real holdover reticles typically reach), the marker clamps to the edge of the visible reticle and labels itself "OFF RETICLE", while the numeric hold text below always shows the true value regardless — so a holdover the reticle can't display still isn't hidden from the shooter, it's just flagged as needing to be partly dialed.

Verified via a 20-check Node/vm harness across all three files: elevation/windage sign math confirmed to place the marker on the correct side of center for both positive and negative values; off-reticle values confirmed to clamp to the frame edge and show the warning label; on-center (zero range) confirmed to read "On center" rather than a hold value; the range picker confirmed to populate from the live calculation and default to the furthest row; and both reticle patterns (Mil-Dot's dots, MOA Hash's tick marks with major-tick labels) confirmed to render their expected SVG elements. Actual visual rendering/layout isn't something the harness can check — that part relies on on-device confirmation, same limitation as the mobile header-wrap fix above.

**2026-07-12 — Fixed the mode toggle clipping next to the title on narrow phones (all three files).** Reported bug: with three mode buttons (Air Rifle/Rimfire/Centrefire) now sharing the header row with the app title, the rightmost button (Centrefire) was getting clipped on iPhone-width screens — the header simply ran out of horizontal space. Considered switching to a `<select>` dropdown instead, but decided against it: mode is switched often enough mid-session to deserve a single-tap, all-options-visible control, and a dropdown would've been the only place in the app using that pattern for what's otherwise treated as primary state (Imperial/Metric and the drop-unit buttons both use the same segmented-button look).

Fixed instead by letting the mode toggle wrap onto its own full-width row below the title, scoped to the existing `@media(max-width:900px)` mobile breakpoint only — desktop is untouched, still one row. On mobile, the three buttons now stretch evenly (`flex:1` each) to fill that row rather than clustering on the left. Since the header is one row taller on mobile once this happens, and the app's layout math (`.layout`'s `height: calc(...)`) previously subtracted a hardcoded `57px` guess for the header's height, that constant was replaced with a `--header-height` CSS variable measured live via `header.offsetHeight` — mirroring the existing `--app-height`/`window.innerHeight` pattern already used for mobile browser toolbar quirks — so the layout sizes correctly whether the header is one row (desktop) or two (mobile), rather than relying on a second hardcoded guess for the taller case.

Verified via a syntax check across all three files plus an isolated unit test of the new `setAppHeight()` logic (stubbed a header with `offsetHeight:96`, confirmed `--header-height` and `--app-height` are both set correctly) — real CSS layout/wrapping itself isn't something the Node/vm harness can render, so that part relies on manual on-device confirmation.

**2026-07-11 — Added a third "Rimfire" mode alongside Air Rifle and Centrefire (all three files).** Requested addition: the mode toggle only offered Air Rifle and Centrefire, with nowhere for .22LR/.17HMR/etc loads to live except awkwardly under one of the other two. Added a third `btnRF`/`'rf'` mode, positioned between Air Rifle and Centrefire in the toggle (low-to-high power order) and given its own yellow `.tag-rf` results tag to keep it visually distinct from the existing green (Air Rifle) and blue (Centrefire) tags.

Rimfire starts with zero built-in presets in all three files — no fabricated .22LR/.17HMR figures were invented to fill the list, consistent with the app's existing "never guess at plausible-looking data" stance (the same reasoning already applied to General Release's empty `PRESETS.air`/`PRESETS.cf`). Users can add their own via the existing "+ Save" flow. Rimfire's default Zero/Max range (50yd/150yd) and plausibility bounds (700-2500fps muzzle velocity, 15-60gr weight) were newly chosen for this change, sitting between Air Rifle's and Centrefire's existing ranges.

Adding a third mode touched more than the toggle button: `currentMode` was already used as an object key throughout persistence (`loadCustom`/`loadHidden`/`saveHidden`/`loadOrder`), so every one of those was extended to a three-way `{air,cf,rf}` shape. The trickiest part was migration safety — existing users' `localStorage` predates Rimfire entirely, so `{air:[...],cf:[...]}` has no `rf` key at all. `loadCustom()`/`loadHidden()` were rewritten to rebuild their return shape key-by-key (`d.rf||[]` / `new Set(r.rf)`) rather than falling back to a default only when the *entire* stored value is missing, so a legacy user's first rimfire save doesn't crash on `undefined.push(...)`. `checkPlausibility()`'s two-mode ternary chain was refactored into a `PLAUSIBILITY_BOUNDS` lookup table (and `setMode()`/`loadPreset()`'s Zero/Max defaults into a shared `MODE_DEFAULT_RANGE` table) — both were already at the point where a third ternary branch would have been unreadable, and the table form means a future fourth mode is a one-line addition rather than another branch everywhere. `mergeImportedPresets()` (preset export/import) now merges all three sections and tolerates older export files that predate the `rf` key. The muzzle-energy-bar visual (an Air Rifle-specific legal-limit-style indicator) was deliberately left gated to `currentMode==='air'` only — it doesn't apply to rimfire's much higher energy figures, so Rimfire correctly falls in with Centrefire there, unchanged.

Verified via a 69-check Node/vm harness across all three files (29 on the main app, 20 each on the two variants): mode-switch to Rimfire activates the right button and blanks the form with the correct Zero/Max defaults; `PRESETS.rf` confirmed empty in all three files; plausibility bounds confirmed correct for Rimfire while Air Rifle/Centrefire bounds are unchanged; a simulated legacy `localStorage` payload (air/cf only, no `rf` key) confirmed to load without throwing, and confirmed capable of having a first Rimfire custom preset saved and reloaded correctly; `mergeImportedPresets()` confirmed to handle a legacy (no-`rf`) import file without throwing; and a saved `mode:'rf'` preference confirmed to restore correctly on reload.

**2026-07-06 — Fixed a visibly kinked/non-smooth trajectory chart for short-zero loads (all three files).** Reported bug: the trajectory chart looked jagged rather than a smooth curve for the first built-in preset in the Simple Mode and General Release variants. Root cause: `computeTrajectory()` only records a data point at the table's configured interval (10yd by default), and the chart was drawing that exact same sparse point set. For a load with a short zero range (30yd, true of the first air rifle preset), the trajectory's entire rise-above-the-sightline-then-fall-back-through-zero arc happens within the first two or three interval marks — connecting so few points with straight lines makes an otherwise-smooth arc look distinctly polygonal. Loads with a 50yd+ zero spread that same arc over more sample points and look smoother, which is why only some presets showed it; it isn't specific to "the first" preset, just to short-zero ones, and the first built-in happens to be one.

Considered auto-loading the first preset vs. a purely visual fix, and went with the latter since the underlying data was always correct — only the chart's sampling density was the problem. `computeTrajectory(p, dense)` now takes an optional second parameter: when true, it records a point at every single yard (the physics loop's native resolution) instead of only at the table's interval marks. `calculate()` now makes a second call, `computeTrajectory(params, true)`, storing the result as `lastResults.chartResults`, and `drawChart()` now draws that dense array instead of the sparse one. The table, CSV export, Range Card, and Trajectory Truing all continue reading `lastResults.results` exactly as before — nothing about their row count or content changed. The extra call has no meaningful performance cost: the RK4 physics stepping already runs every yard internally regardless of which points get recorded, so this only changes which points get kept, not how much computation happens.

Verified via a 33-check Node/vm harness across all three files: `chartResults` confirmed present with roughly one point per yard of max range (vs. the sparse table's one point per 10yd), confirmed to include in-between yards (5, 25) that the table's interval would skip, confirmed strictly increasing with no duplicate/out-of-order yards; the table (`lastResults.results`), CSV export, and Range Card all confirmed to still use exactly the sparse interval-based row count, unaffected by the denser chart data sitting alongside it.

**2026-07-06 — Switching Air Rifle ↔ Centrefire now resets the load form instead of carrying over stale values (all three files).** Reported bug: switching modes left the previous load's Weight/BC/Muzzle Velocity/Scope Height/Diameter/Length/Twist/Calibre/Drag model/Shape sitting in the fields under the new mode's label — e.g. an air pellet's 15gr/0.019 BC could still be showing after switching to Centrefire, where those numbers are meaningless. Only Zero Range and Max Range were ever reset (to mode-appropriate defaults), and any previously-calculated trajectory stayed on screen unchanged, no longer describing either the old or the new load.

Considered two fixes: reset to a blank form, or auto-load the first available preset for the new mode. Went with blank, since it's consistent with what the mode toggle already does to Zero/Max (start fresh for this mode) and it never substitutes a load the user didn't explicitly choose — auto-picking "the first preset" would have been an guess dressed up as a real load. `setMode()` in all three files now also blanks Calibre/Weight/BC(G1)/BC(G7)/Muzzle Velocity/Scope Height/Diameter/Length/Twist, resets Drag model to G1 and Shape to flat-base, clears `lastResults`, and restores the main panel to its original "Configure your load and tap Calculate Trajectory" empty state — mirroring exactly what the existing "+ New Load" button (`clearLoadFields()`) already does, so the behavior isn't new to the app, just newly triggered by the mode toggle too. Zero/Max keep their existing separate mode-default logic, untouched by this change. Saved presets themselves are completely unaffected either way — this only touches the live form.

Verified via a 66-check Node/vm harness across all three files: switching modes blanks every listed field, resets Drag/Shape to their defaults, sets Zero/Max to the new mode's defaults (confirmed in both directions, air→cf and back), clears `lastResults`, and restores the empty-state markup exactly; a follow-up check confirmed Single Load Mode's per-section preset tracking (CervidBallistics_SimpleMode.html) is untouched by a mode switch — a preset saved for Air Rifle stays saved and reappears correctly on switching back, while the live form in between shows nothing for the other section until something is saved there too.

**2026-07-06 — Renamed the "Simple Mode" feature to "Single Load Mode" (CervidBallistics_SimpleMode.html and CervidBallistics_GeneralRelease.html).** "Simple" didn't say what the toggle actually does; "Single Load Mode" names the behavior directly — one saved load shown per section instead of a full list. Updated the visible button label ("Single Load Mode: On/Off"), the on-screen note text under the preset grid, the save-confirmation dialog wording, and all the surrounding code comments in both files. Left the internal identifiers unchanged (the `simpleMode` variable, `SIMPLE_KEY`/`ballistics_simpleMode_v1` localStorage key, `.simple-mode-btn` CSS class, and the `simpleModeLink`/`toggleSimpleMode`/`updateSimpleModeUI` names) — renaming the localStorage key specifically would have reset the toggle's remembered state for anyone who'd already installed one of these files, and renaming the internal plumbing offers no user-facing benefit. `CervidBallistics.html` doesn't have this feature at all, so it wasn't touched.

**2026-07-06 — Created CervidBallistics_GeneralRelease.html, a distributable variant with no personal data baked in.** Follow-up to a review of the Single Load Mode variant: that file still shipped the author's full pellet/cartridge library, just hidden behind the Single Load Mode toggle, and its default field values (40gr, BC 0.045, 900fps, 0.2175in diameter, 0.72in length, 1:16in twist) were quietly airgun-specific. This new file, forked from `CervidBallistics_SimpleMode.html`, addresses both for a copy meant to go to other users, while leaving `CervidBallistics.html` and `CervidBallistics_SimpleMode.html` themselves untouched:

*Built-ins genuinely removed, not just hidden.* `PRESETS` is now `{air:[],cf:[]}` — no reference loads ship in the file at all. A recipient who switches Single Load Mode off sees only what they themselves have saved; there's nothing personal to find.

*Single Load Mode defaults on.* With no built-in library to browse, showing every UI affordance for managing dozens of presets on day one made little sense. `simpleMode` now initializes to `true` when `localStorage` has never held a value for it, while still respecting an explicit prior toggle-off (`v===null?true:v==='1'`) — so once a user has chosen a state, that choice sticks across reloads exactly as it already did in the Single Load Mode variant.

*Airgun-specific default values blanked.* Removed the hardcoded `value=` attributes from Weight, BC(G1), Muzzle Velocity, Diameter, Length, and Twist, replacing each with a neutral placeholder (e.g. "e.g. 150", "e.g. 0.400"). Scope Height's `1.5in` default was deliberately left in place — a judgment call, since 1.5in is a genuinely mode-agnostic scope-mounting figure rather than an airgun-specific ballistic value, not something explicitly on the original suggestion list. Zero Range (30yd) and Max Range (100yd) were also left with their original defaults, since they weren't part of the original six flagged fields, though they arguably lean airgun-typical too — worth a look if this gets more use.

*Required-fields guard added in `calculate()`.* Blanking the defaults alone wasn't sufficient: `calculate()` and `effectiveBc()` both contain silent numeric fallbacks (`||40`, `||0.045`, `||0`) that would otherwise still produce a plausible-looking trajectory from meaningless defaults if a field was left empty. `calculate()` now checks Weight, BC(G1), Muzzle Velocity, Zero Range, and Max Range up front and, if any are missing, shows a friendly "Enter your load details first" message naming exactly which fields are still needed, instead of computing anything.

*"Restore defaults" reworded to "Clear all."* With no built-in library left to restore to, the old wording ("Restore defaults?") no longer made sense — reworded the link and its confirm dialog to describe what the action actually does now: clearing the user's own saved loads for that section. Flagged as a second judgment call beyond the literal suggestion list, since it's a UI-wording change rather than one of the five reviewed recommendations.

Verified via a 31-check Node/vm harness: `PRESETS` confirmed empty in both modes; Single Load Mode confirmed defaulting to on for a fresh install while still honoring an explicit prior off-toggle; `calculate()` confirmed blocking with the correct named-fields message for a fully blank load and for a partial fill (single missing field), and confirmed succeeding normally once all five required fields are filled in; the six blanked fields confirmed free of their old hardcoded values and carrying placeholders; Scope Height confirmed still defaulting to 1.5in; and the "Clear all" wording confirmed present in both the HTML link and the confirm-dialog string.

**2026-07-05 — Single Load Mode note now also counts hidden built-ins (CervidBallistics_SimpleMode.html only).** The "N other saved loads are hidden" note only ever counted custom presets — it never mentioned that Single Load Mode also always hides the built-in reference presets, regardless of custom count. That made the numbers look wrong: e.g. "3 other saved loads hidden" but toggling off revealed far more than 4 entries, because most of what reappeared was the built-in library, not saved loads.

`renderPresets()` now also computes the visible (non-hidden-via-✕) built-in count for the current section and appends it to the note, e.g. "3 other saved loads and 5 built-in presets are hidden until you turn Single Load Mode off." Singular/plural wording adjusts correctly whether it's reporting saved loads alone, built-ins alone, or both together.

Verified via the Node/vm harness across four scenarios (no customs, exactly one, several extra, and with one built-in separately hidden via the ✕ icon) — the note's built-in count always matches what actually reappears when Single Load Mode is toggled off, cross-checked directly against `getOrderedPresets()`.

**2026-07-05 — Regenerated CervidBallistics_SimpleMode.html from the current base.** The Single Load Mode variant had been forked before a long run of later fixes and features (tooltip click-popovers, export/import, undo-delete, mode/unit persistence, plausibility warnings, Range Card, colour-only accessibility fixes, the Azimuth tooltip, and the metric interval drift fix) — so it was drifting further out of date every time. `CervidBallistics.html` itself was left untouched throughout.

Rebuilt by copying the current `CervidBallistics.html` fresh and reapplying the Single Load Mode diff on top: the `.simple-mode-btn` pill toggle and its CSS, the `simpleModeLink`/`restoreDefaultsLink`/`simpleModeNote` markup in the Quick Presets header, `simpleMode` state plus `toggleSimpleMode()`/`updateSimpleModeUI()`, the `getOrderedPresets()` single-preset filter (preferring the currently loaded custom preset, else the most recent), the note logic in `renderPresets()` (with the unique-name de-dup fix), and the single-slot replace-with-confirm logic in both `saveCurrentAsPreset()` and `saveTruingToPreset()` (including its stale-tracked-reference and built-in-sourced fallback branches).

Verified via the Node/vm harness across two layers: the Single Load Mode behaviors themselves (built-ins hidden when on, single-slot save-with-confirm, declining leaves data untouched, full list restored on toggle-off) all still work correctly on the new base; and the newer base-file features (plausibility warnings, the metric interval fix, Range Card, help tooltips, mode/unit persistence, preset export/import) all continue to function correctly inside this variant too.

**2026-07-05 — Fixed metric interval drift in the trajectory table.** Reported bug: with a 10m interval set in metric mode, ranges displayed cleanly as 10, 20, ... 80m, then jumped to 91, 101, 111, 121. Root cause: the whole physics engine (`computeTrajectory()`) only has 1-yard resolution internally (`stepFt=3`), and the row-selection logic always rounded the requested interval to the nearest whole *yard* before stepping — so a "10m" interval became 11yd (`10/0.9144=10.94`, rounds to 11), and each successive 11yd step accumulates its own conversion-rounding error in metres. By the 9th step (99yd = 90.53m), the accumulated error crossed the 0.5m rounding threshold and every mark after that was off by one meter, then increasingly more.

Fixed by generating the recorded sample points directly in the *displayed* unit instead of always in yards: `computeTrajectory()` now takes `p.metric`, `p.intervalNative`, and `p.maxNative` (the interval/max-range values exactly as entered, before conversion), builds the list of target marks by stepping through native metres (or yards, if imperial) from 0 to the max range, and converts each mark to its own independent nearest-yard equivalent — so no error ever compounds between marks. This also fixes the same latent drift for anyone in metric mode using the *default* interval (no custom value entered), which was previously always falling back to round-yard defaults (10/25/50yd) regardless of unit system.

Verified via the Node/vm harness: a 10m interval in metric mode now produces exactly 0, 10, 20 ... 500m with zero drift; the default (blank) interval in metric mode lands on clean 25m marks; and imperial mode's whole-yard stepping is unchanged.

**2026-07-05 — Added a tooltip for Azimuth.** Users kept asking what it was. Added the standard "?" help-tip (same `showHelpTip()` click-popover as the rest of the app) explaining it is the compass direction being fired toward, how it pairs with Latitude for the Coriolis calculation, and that 0/North is a safe default when unknown.

**2026-07-05 — Final three workability/UX suggestions (CervidBallistics.html).**

*Soft plausibility warnings.* New `checkPlausibility(params)` runs on every `calculate()` and flags likely typos without ever blocking or altering the calculation: BC outside roughly 0.01-1.0, muzzle velocity outside a per-mode range (150-1700 fps for Air Rifle, 500-5000 fps for Centrefire — wide enough to avoid false positives on legitimate slow/fast loads, tight enough to catch an fps/m-s mixup or a stray digit), and bullet/pellet weight outside a per-mode range (3-120gr air, 20-700gr centrefire). Findings render in a new dismissible `#plausibilityWarning` banner above the Calculate button, styled like the existing BC(G7) warning. Dismiss just hides it for that render — a genuinely implausible value still gets flagged again next time Calculate is pressed, since nothing about the check depends on prior dismissals.

*Range Card.* A new "📋 Range Card" button next to Export CSV opens a full-screen, large-print modal (`showRangeCard()`/`hideRangeCard()`) listing every calculated range against its holdover in whatever drop unit is currently selected (mils/MOA/clicks/in/cm) — nothing else, in place of the full stat-card/chart/table layout. Zero and spot-range rows get a subtle tint and a ★/◆ marker, matching the main table's convention. Includes a Print button and a `@media print` rule that hides everything except the card itself.

*Colour-only signaling fixes.* Two spots relied on colour alone to convey status: the Muzzle Energy stat card's warn/danger tiers (text colour only, no label) and subsonic rows in the results table (red text/background, explained only by a one-time legend caption). Muzzle Energy now appends a colour-matched text tag ("⚠ High power" / "⚠ Very high power") beneath the energy figure whenever the tier is warn or danger — the default tier gets no tag, since there's nothing to warn about. Subsonic rows now append "(sub)" directly to the Mach cell's own text, so the flagged rows are identifiable without needing to distinguish red from the surrounding text. The Gyro Stability card was checked too but already carries a plain-text status label (Unstable/Marginal/Stable/etc.) alongside its colour, so no change was needed there.

Verified via the Node/vm harness: plausibility warnings correctly fire for an out-of-range BC and for a centrefire-implausible muzzle velocity, stay silent for normal values, reappear on the next calculate() after being dismissed, and use different thresholds per mode (a velocity that warns in Air Rifle mode is fine in Centrefire mode and vice versa); the Range Card renders one row per calculated distance in the currently active drop unit, with real Unicode characters (not stray HTML entities) in its subtitle, and closes cleanly; and both colour-only fixes render their text markers correctly.

**2026-07-05 — Added a tooltip for mils/MOA/clicks (they had none you could actually find).** The drop-unit toggle buttons in the results header had explanatory text on the same hover-only `title=` attribute that had just been fixed for the "?" badges elsewhere — easy to miss since it never showed on click, and there was no visible "?" indicating a tooltip existed at all. Removed the dead `title=` attributes from the mils/MOA/clicks buttons (inches/cm are self-explanatory and never had one) and added a single shared "?" icon next to the whole toggle group, using the same `showHelpTip()` click-popover as everywhere else, explaining all three abbreviations together rather than cramming a separate icon onto each small pill button.

Verified via the Node/vm harness: after running a calculation, the rendered results header contains the new combined help-tip icon wired to `showHelpTip()`, and no leftover `title=` attributes remain on those buttons.

**2026-07-05 — Fixed help tooltips not responding to a tap/click.** The "?" badges added in the previous entry relied on the native `title=""` attribute, which only appears on mouse hover — pressing/tapping one did nothing, since there was no click behavior wired up at all. Replaced with a real click-triggered popover: each icon now carries `data-tip="..."` plus `onclick="showHelpTip(this,event)"`, and a single shared `#helpTipPopover` element (positioned near whichever icon was tapped, via `getBoundingClientRect()`) shows the text. Tapping the same icon again, tapping a different icon, or clicking anywhere else on the page (via a `document`-level click listener) all behave correctly — the icon's own click handler stops propagation so it does not immediately re-trigger the dismiss listener. The icon itself was also enlarged slightly (13px to 15px) for easier tapping on touch screens.

Verified via the Node/vm harness: clicking an icon shows the popover with the right text; clicking it again closes it; clicking elsewhere closes it; switching from one icon to a different one swaps the content rather than getting stuck toggled; and a source scan confirms none of the five help-tip spots were left on the old hover-only `title=` pattern.

**2026-07-05 — Four workability/UX improvements (CervidBallistics.html).** First batch from a broader suggestions list; more may follow.

*Preset export/import.* "Export presets" downloads a JSON file of all custom presets (both sections); "Import presets" reads one back in. Import is additive only — `mergeImportedPresets(existing, imported)` is a pure function that never overwrites a preset whose name already exists in that section, and reports how many were added vs. skipped. Hidden built-ins and drag order are deliberately not included in the export, since those are per-device view preferences rather than data worth carrying between machines.

*Undo after deleting a preset.* Deleting a custom preset now snapshots that section's entire custom array and drag-order array before removing anything, and shows a "Deleted 'X'. Undo" bar for 8 seconds. Clicking Undo restores the exact prior state; letting it expire (or deleting something else, which replaces the pending one) makes the deletion permanent as before.

*Remembers last-used mode & unit system.* `setMode()` and `setUnitSystem()` now persist their choice (`ballistics_prefs_v1`), and the app reads it back at load, before the first render — so reopening the app returns you to Centrefire/Metric (or whatever you last had) instead of always resetting to Air Rifle/Imperial. Pure UI convenience; never touches preset data.

*Inline help tooltips.* Small "?" badges (native `title` tooltips) next to BC (G1), BC (G7), Drag Model, Twist rate, and the Gyro Stability (Sg) stat card, plus tooltips on the mils/MOA/clicks toggle buttons — one-line plain-language explanations of the jargon for anyone not already fluent in it. The Twist tooltip is placed as a sibling of `<label id="lblTwist">`, not nested inside it, since that label's `textContent` gets fully overwritten by `updateLabels()` on every unit toggle (the same class of bug fixed earlier for the Spot Range hint) — nesting it would have meant it silently vanished the first time someone switched units.

Verified via the Node/vm harness: `mergeImportedPresets()` correctly skips an existing name while adding new ones from both sections with accurate counts; `exportPresets()` builds its download without error; a full delete-then-undo cycle restores the exact custom array and order array, with the undo bar text, visibility, and cleanup all behaving correctly; `setMode()`/`setUnitSystem()` correctly persist and `loadPrefs()` reads back the right values; and the Twist label continues to convert correctly between "(in)" and "(mm)" on unit toggle with the tooltip intact, confirming the sibling placement avoided a repeat of the earlier wipe bug.

**2026-07-05 — Single Load Mode: visible toggle button + hidden-count fix (CervidBallistics_SimpleMode.html only).** Two follow-up fixes to the variant above, both scoped to that file.

The toggle was a 10px underlined text link (`.restore-link`, shared with "Sort by calibre"/"Restore defaults") — easy to miss. Replaced it with an actual `<button>` (`.simple-mode-btn`), a rounded pill that's grey/outlined when off and solid green when on, so its state is visible at a glance rather than something you have to read.

Also fixed a real miscount in the "N other saved loads are hidden" note: it was computed from the raw length of the stored custom-preset array, but the preset list has always silently collapsed same-named entries (via the pre-existing `usedC` de-dup in `getOrderedPresets()`, unrelated to Single Load Mode). If stale duplicate-named entries existed from earlier saves, the note could report more hidden loads than would actually appear when switching Single Load Mode back off. The count now de-dupes by name the same way `getOrderedPresets()` does, so the number always matches what you'll actually see.

Verified via the Node/vm harness: the button renders as a real `<button>` element and its text/`on` class flip correctly with each toggle; and a seeded case with a stale duplicate-named custom preset alongside two genuinely distinct ones now reports "1 other saved load is hidden" (matching the 1 real extra name) instead of overcounting.

**2026-07-05 — New variant: CervidBallistics_SimpleMode.html with a "Single Load Mode" toggle.** (Originally called "Simple mode" — renamed 2026-07-06 for clarity; see the entry at the top of this changelog.) The main `CervidBallistics.html` is unchanged. This is a separate file, copied from it, for anyone who wants one rifle / one load per section instead of managing a full preset list.

Added `simpleMode` (persisted via `ballistics_simpleMode_v1`, default off) and a "Single Load Mode: Off (turn on)" link in the Quick Presets header, alongside the existing Sort/Restore-defaults links (which hide themselves while Single Load Mode is on, since neither is meaningful with a single preset). Turning it on doesn't delete or hide anything in storage — `getOrderedPresets()` gains a final filter step that, when `simpleMode` is true, narrows the result to just one custom preset (preferring whichever one is currently loaded, else the most recently saved) and drops all built-ins from the render. If more custom presets exist than the one shown, a note under the grid says so (e.g. "1 other saved load is hidden until you turn Single Load Mode off") rather than silently losing track of them — turning the toggle back off immediately shows everything again, untouched.

Saving is the other half: `saveCurrentAsPreset()` and `saveTruingToPreset()` both now check `simpleMode` before creating a new entry. If a custom preset already exists for the section and the user isn't already editing that exact slot (via the ✏ icon, or via a matching `loadedPresetRef`), a confirm dialog asks whether to replace it; declining aborts the save with no changes written. Accepting overwrites that one slot instead of appending a new tile, so the section can never end up with more than one custom preset while Single Load Mode is on. The built-in-edit and stale-tracked-reference fallback paths in `saveTruingToPreset()` got the same treatment so they can't sneak past the one-slot rule either.

Verified via the Node/vm harness: default state unaffected (full built-in + custom list, note hidden); enabling with zero custom presets shows an empty grid with a prompt-to-save note; first save creates the one slot with no confirmation prompt; a second save with a different name triggers the replace-confirm and, when accepted, still leaves exactly one stored entry; declining the confirm leaves the existing entry completely unchanged; turning Single Load Mode back off restores the full list (built-ins plus the real custom preset) with nothing lost; pre-existing multiple custom presets (simulated as if saved before Single Load Mode was ever turned on) correctly collapse to showing just the most recent one with an accurate "N other hidden" count, and both are still present once toggled off; and the Trajectory Truing "Save to preset" fallback path (tracked preset renamed/removed elsewhere) correctly overwrites the remaining single slot rather than pushing a duplicate.

**2026-07-05 — Y-axis tick values on the trajectory chart.** The drop curve's vertical axis had a unit label ("Drop(in)"/"Drop(cm)") in the corner and four grey gridlines, but no actual numbers on those gridlines — you could see the shape of the curve but not read off how much drop any point represented. `drawChart()`'s gridline loop now computes the drop value at each of the 5 horizontal lines by inverting the existing `ty()` mapping (`dVal = minD + rng*(1-i/4)`) and draws it at the left margin (x=2, inside the padding, so it never collides with the plotted curve or its fill). Metric values round to whole cm; imperial values round to one decimal inch, matching the precision already used elsewhere for drop figures. A small guard converts any `-0` result to `0` so a value that rounds to exactly zero near the line-of-sight never displays as "-0".

Verified via the Node/vm harness (capturing `fillText` calls instead of real canvas output): 5 tick labels drawn per render, values correctly spanning the actual min/max drop of the plotted results, metric labels rendered as whole numbers, no "-0" in either unit system, and the existing axis title label unaffected.

**2026-07-05 — Lightweight second-point verification for Trajectory Truing.** Truing fits a single BC multiplier from one validated shot, which can mask a muzzle-velocity error that happens to look right at the truing distance but diverges further out. Rather than a full two-parameter (MV + BC) solve — which needs two accurate real-world shots and a coupled solver to be reliable — added a lightweight, advice-only check: a "Verify Second Point" sub-section inside Trajectory Truing with its own `fVerifyRange`/`fVerifyObs` inputs, separate from the truing inputs above them.

`verifyTruing()` does no fitting at all — it re-runs the existing `computeTrajectory()` at the new distance using whatever BC is currently in effect (trued or not, via `lastResults.params`), compares the predicted holdover against the shooter's observed value, and reports a qualitative verdict: "Holds up well" (≤5% delta), "Close, minor drift" (≤12%), or "Diverging — worth re-checking muzzle velocity" (>12%), plus the raw predicted/observed/delta numbers. If no truing has been applied yet, the result is still shown but flagged "(model is currently untrued)" so it reads as a sanity check on the raw model rather than implying a correction was made. `fVerifyRange`/`lblVerifyRange` convert between yd/m the same way `fTrueRange` does. `clearTruingState()` now also clears the verify inputs and status, so it resets correctly alongside the rest of truing state on preset switch, mode switch, and New Load.

Verified via the Node/vm harness: a self-consistent check (verifying against the model's own predicted value) reads as a perfect match; a 30%-inflated observed value correctly reads "Diverging"; verification works and is correctly labeled when run without any prior truing; empty/out-of-range inputs are handled without throwing; and the fields/status reset on preset switch along with the rest of the truing state.

**2026-07-04 — Header font +10%; "Sort by calibre" preset toggle.** Two small items. Header `<h1>` bumped from 18px to 19.8px. More substantially: added a non-destructive "Sort by calibre" link next to "Restore defaults" in the Quick Presets header. New per-mode state `presetSortMode` (`{air:'manual'|'calibre', cf:'manual'|'calibre'}`) — `getOrderedPresets()` sorts by each preset's existing `dia` field (already precise, no need to parse the free-text `calibr` string) ascending when in calibre mode, as a final step that never touches the persisted drag-and-drop order (`loadOrder`/`saveOrder`). Toggling back to Manual restores exactly what was there before, untouched. Dragging is disabled (`btn.draggable=false`) while calibre-sorted, since reordering a computed view doesn't mean anything and would otherwise silently corrupt the real manual order on drop. The link label and sort state are tracked independently per mode, synced on `setMode()`. Verified via harness: ascending sort order, manual arrangement surviving a round-trip toggle, dragging correctly inert while sorted, and Air/Centrefire maintaining independent sort states.

**2026-07-04 — Per-preset "restore to original" (scoped, unlike the global Restore defaults).** Previously the only undo was the "Restore defaults" link, which wipes every custom preset, hidden built-in, and ordering choice for the whole mode — no way to revert just the one preset a shooter had trued.

Added two optional markers on custom preset entries, set by `withRestoreMetadata()` and applied from both `saveCurrentAsPreset()` and `saveTruingToPreset()`:
- `_restoreBuiltinIdx` — this preset supersedes a built-in (created via the ✏ pencil-edit-a-built-in flow, or via "Save to preset" while a built-in was loaded). Restoring deletes the custom copy and un-hides the built-in, recovering the exact untouched original — no snapshot needed, since built-ins are hardcoded and never mutated.
- `_preTruingBackup` — `{field, value}` captured from `bakeTruingIntoFields()`'s return value the *first* time truing is baked into an already-custom preset. Never overwritten by later re-truings, so restoring always returns to the shooter's original baseline, not just the last truing. A deliberate manual edit (saving without active truing) drops this backup, since hand-editing establishes a fresh intentional baseline — built-in lineage (`_restoreBuiltinIdx`) always takes priority over a truing backup and is carried forward regardless.

A small ↺ icon (`.preset-restore`, top-left of the tile, mirroring the existing ✏/✕ top-right icons) appears only on custom presets carrying one of these markers. `restorePresetOriginal(ci)` branches on which marker is present, confirms, reverts, and — if the restored preset happens to be the one currently loaded — reloads it so the form refreshes immediately.

Verified via the harness: built-in-derived restore (copy removed, original un-hidden, form refreshed since it was loaded); backup surviving two successive re-truings with different targets before correctly restoring the very first value; a manual (non-truing) edit correctly dropping a stale backup while keeping the edit; and other presets/modes completely unaffected throughout.

**2026-07-04 — "Save to preset" button inside Trajectory Truing.** The previous fix (baking truing into `saveCurrentAsPreset()`) required going to the Presets section and typing a name, which — unless you'd separately clicked the ✏ pencil to enter edit mode — created a brand-new preset rather than updating the one you'd just trued. Not intuitive for the common case of "I trued the load I already have, save that back."

Added `loadedPresetRef` (`{type:'c'|'b', idx, name}` or `null`), set whenever a preset is loaded — by clicking its button, by the ✏ edit icon, or after `saveCurrentAsPreset()` — and cleared by `setMode()`/`clearLoadFields()`. A new button (`#btnSaveTruing`, "💾 Save to preset") lives inside the Trajectory Truing section itself, appears once truing is applied *and* a preset is tracked, and is labeled with that preset's actual name. Clicking `saveTruingToPreset()` bakes in the trued BC and amends that exact preset in place — no name prompt needed. If it was a built-in, it creates a custom copy under the same name and hides the original (same mechanism the ✏ built-in-edit flow already used). If the tracked custom preset was deleted or changed since it was loaded (verified by name match, not just index, matching how `getOrderedPresets()` already resolves identity), it safely falls back to saving as a new copy instead of silently overwriting the wrong slot, with a yellow status note explaining why.

Refactored `saveCurrentAsPreset()`'s ~30 lines of DOM-reading into `buildPresetEntryFromForm(name)` and the truing bake-in into `bakeTruingIntoFields()`, shared by both the original save flow and the new one — avoids duplicating logic between them.

Verified via the harness: button visibility/label tracking through preset clicks, edit-icon clicks, and post-save state; amending a custom preset in place (array length unchanged); the built-in hide+copy path; the button staying hidden with no preset loaded; clearing on preset/mode switch; and the stale-reference fallback (simulated by deleting the tracked preset out from under the app before clicking Save) correctly saving as a new copy instead of corrupting an unrelated entry.

**2026-07-04 — Presets can retain truing.** Previously `saveCurrentAsPreset()` always read `fBC`/`fBC7` straight from the DOM, which truing never touches (it only applies `bcMultiplier` at calculate-time) — saving a trued load silently discarded the calibration. Now, if `bcMultiplier` is active when "+ Save" is clicked, the trued value (`effectiveBc().bc × bcMultiplier`) is written into whichever field matches the currently active drag model (`fBC` for G1, `fBC7` for G7 — the other field is left untouched) before the preset is built, then `clearTruingState()` runs so the live form reflects a clean, already-calibrated number rather than a nominal value plus a multiplier. Saving without active truing is completely unchanged. Verified with the harness across G1-truing, G7-truing (real entered value), and untrued-save cases — reloading a trued-and-saved preset reproduces the identical calibrated trajectory with no leftover multiplier or "Trued" badge.

**2026-07-04 — Trajectory truing no longer leaks across loads.** `bcMultiplier` (line ~750) is a single global set by `applyTruing()` and applied unconditionally inside `calculate()`. Neither `loadPreset()`, `setMode()`, nor `clearLoadFields()` ("New Load") ever reset it, so truing one load and then opening a different preset — or just switching Air Rifle/Centrefire — silently kept applying the old multiplier to the new bullet's BC. The "🎯 Trued ×N" badge and BC Truing stat card kept showing too, making it look like the new load had also been trued.

Added `clearTruingState()` (resets `bcMultiplier` to 1, clears `trueStatus`, hides the Reset button, and clears the validation-range/observed-holdover inputs so a stale target doesn't linger either) and call it from all three load-changing entry points, plus refactored `resetTruing()` (the manual Reset button) to share the same helper. Verified with the Node/vm harness: truing applies correctly within a load, resets on preset switch/mode switch/New Load, and the manual Reset button still works unchanged.

**2026-07-04 — Dedicated BC(G7) field, joined to the model.** Superseded the single dynamically-relabeled BC field from the previous entry with two persistent fields: `fBC` ("BC (G1)", always required, always means G1) and `fBC7` ("BC (G7)", optional). Rationale: a single shared field whose meaning silently changed with the dropdown was itself the source of ambiguity — two fixed-meaning fields remove it structurally instead of patching around it.

New shared helper `effectiveBc()` is the single source of truth for "which BC number is actually in effect right now," used identically by `calculate()`, `applyTruing()`, and the BC Truing stat card display (previously that card re-read `fBC` from the DOM directly, which would have shown the wrong "before" value once G7 had its own field). Behavior: if Drag Model is G7 and `fBC7` has a real entered value, it's used directly ("joined to the model") — no estimate involved, and the results header shows the plain number (e.g. "BC 0.2430 (G7)"). If `fBC7` is blank, `calculate()` transparently falls back to `BC(G1) ÷ 1.9` and flags it in the header ("BC 0.2368 (G7, estimated from G1)") so an approximation is never silently indistinguishable from a real number. Drag Model G1 always uses `fBC` directly — no warning or fallback needed there, since that field's meaning never changes.

The warning banner is now unidirectional: it only fires switching *to* G7 with `fBC7` empty (switching to G1 needs no warning — that field is always unambiguous). Its button relabeled "≈ Estimate BC (G7) for me" and now fills `fBC7` (never overwrites `fBC`). `bc7` persists through custom presets (`saveCurrentAsPreset`/`loadPreset`), correctly clearing to blank when loading a preset that doesn't specify one, so values never leak across loads.

Verified via the Node/vm harness across 7 scenarios: estimate fallback + header flag, estimate-button fill, direct G7 entry overriding the estimate, no false warning on G1, preset save/load round-trip (including the no-bc7 clear case), and truing correctly basing off `effectiveBc()` rather than a stale DOM read.

**2026-07-04 — BC(G1)/BC(G7) mismatch guard.** `fDrag` (Drag Model) previously had no `onchange` handler at all — switching between G1 and G7 silently reused whatever number was in the BC field under the new standard, with the label still hardcoded "BC (G1)" regardless. Since G1 and G7 BCs for the same bullet are genuinely different numbers (no fixed ratio — it depends on the bullet's own form factor), this could silently feed a wildly wrong BC into the physics engine.

Fixed: `lblBC` now tracks the selected drag model (`updateBcLabel()`). Manually switching Drag Model fires `onDragModelChange()`, which shows a dismissable warning banner (`#bcWarning`) explaining the value isn't valid as-is, plus an "≈ Estimate for me" button (`estimateBcForNewStandard()`) that applies a rough `BC_STANDARD_RATIO = 1.9` conversion — explicitly caveated in the banner text as only reasonable for boat-tail centrefire bullets, not pellets or flat-base slugs. `loadPreset()` and `clearLoadFields()` sync the label and silently dismiss any stale warning (presets carry their own correctly-paired BC, so no warning should fire there). Verified with the Node/vm harness: label tracking, warning trigger only on genuine user-driven switches, both conversion directions, and silence on preset load/clear all checked out.

**2026-07-04 — Metric conversion audit + fixes.** Full audit confirmed muzzle velocity, scope height, zero/max/interval/spot range, altitude, wind speed, pressure, diameter, and length all convert correctly. Fixed six real gaps:
- Preset sidebar buttons now show grams/m·s⁻¹ in metric mode instead of always grains/fps (`renderPresets`)
- "Sound: X fps" in the Subsonic stat card now converts to m/s in metric mode (`renderResults`)
- Trajectory chart now plots `dropCm`/`energyJ` (not raw imperial values) when metric is active, and x-axis gridlines are computed in metres directly for clean 50/100/200 m steps instead of yard-derived numbers (`drawChart`)
- Twist rate ("Twist 1:X") now converts between inches and mm like diameter/length, with its own label id (`lblTwist`) and conversion wired through `setUnitSystem`, `loadPreset`, `saveCurrentAsPreset` (always stored in inches), and `calculate()`
- Fixed a regression where toggling units destroyed the "◆ single extra row" hint next to Spot Range — `updateLabels()` was blindly overwriting `label.textContent`, wiping the child hint `<span>`. Restructured so only the unit word (`#lblSpotUnit`) swaps
- Added `BASE_BOUNDS`/`applyBounds()` so input `min`/`max`/`step` rescale correctly when switching units (e.g. altitude's 20,000 ft ceiling becomes ~6,096 m, not a literal 20,000 m). `fPress` intentionally excluded — its static bounds already work for both inHg and hPa

Verified with a hand-rolled Node/vm DOM harness (no network available) exercising `calculate()`, `setUnitSystem()`, `loadPreset()`, and `saveCurrentAsPreset()` in both directions — round-trip conversions and stored (always-imperial) values all checked out with no runtime errors.

**2026-07-04 — Projectile Shape selector for gyro stability.** Slug loads were reading "Over-stabilised" in the Gyro Stability card even when real-world experience says they shouldn't. Root cause: `millerSg()` infers stability purely from length/diameter/weight, which implicitly assumes a solid, roughly uniform-density projectile — it can't tell a boat-tail (sheds mass toward the rear, needs less spin than a flat-base of equal L/D) or a diabolo pellet (mostly hollow skirt, much less of its length is real destabilising mass) from a plain cylinder.

Added a "Projectile Shape" dropdown (Flat-base / Boat-tail / Diabolo) next to Drag Model. Selecting a shape applies an approximate, community-sourced Sg correction via `SHAPE_SG_FACTOR` (`flatbase:1.0, boattail:1.08, diabolo:1.20`), and the equivalent `sqrt(factor)` scaling is applied to the Min Twist recommendation for consistency. The applied correction is shown directly in the Gyro Stability card (e.g. "Over-stabilised · Boat-tail ×1.08") so it's never a silent adjustment. Shape is saved/loaded with presets (`shape` field, defaults to `flatbase` for any preset that predates this feature, so existing behavior is unchanged unless the user explicitly picks a shape).

Explicitly **not** implemented: a rigorous moment-of-inertia (McCoy-style) recalculation, which would need actual bullet/slug geometry (nose profile, boat-tail angle, hollow-point depth) rather than a flat multiplier. The chosen factors (1.08 / 1.20) are reasonable, commonly-cited approximations, not derived from first principles — flagged clearly in the UI caption and worth revisiting if it turns out to under- or over-correct for a specific slug.

Tested via the same Node/vm harness: confirmed the factor changes the reported Sg and status text, Min Twist scales by `sqrt(factor)`, and shape round-trips correctly through save/load of custom presets.
